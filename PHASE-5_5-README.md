# فاز ۵.۵ — ریسپانسیو حرفه‌ای پنل ادمین

> این سند از بخش ۱۲.۱۷ نسخهٔ قدیمی پرامپت مادر بازسازی شده تا جزئیات
> پیاده‌سازی این فاز از حافظهٔ پروژه گم نشود.

**تکمیل:** ۶ سپتامبر ۲۰۲۶

## خلاصه

طبق اصل طراحی پنل ادمین (اولویت کارایی/تراکم اطلاعات، نه عکس‌های
تطبیقی — پنل ادمین اصلاً عکس تزئینی ندارد)، این فاز یک ممیزی کد در سطح
ساختار روی هر ۹ کامپوننت ادمین انجام داد (نه رنگ/تایپوگرافی؛ آن‌ها از
قبل درست بودند) و چهار دستهٔ مشکل واقعی پیدا و رفع کرد. **مثل ممیزی‌های
قبلی (فاز ۴.۶/۴.۸)، این یک ممیزی کد است، نه تست چشمی روی دستگاه واقعی
— تأیید نهایی پیکسل‌به‌پیکسل هر بریک‌پوینت هنوز با تست واقعی Zakir روی
گوشی/تبلت خودش است.**

## ۱. فرم‌های مودال روی گوشی‌های کوچک (۳۲۰–۳۷۵px)

هر ۹ مورد `grid-cols-2` بدون‌شرط در فرم‌های مودال (bus-manager×۲،
driver-manager، loyalty-manager×۲، route-manager، trip-scheduler×۳) به
`grid-cols-1 sm:grid-cols-2` تغییر کرد — یعنی زیر ۶۴۰px دو فیلد کنار هم
حالا زیر هم می‌آیند.

## ۲. تارگت‌های لمسی زیر ۴۴px

دقیقاً همان استاندارد فاز ۴.۸ (سایت پابلیک) اینجا هم اعمال شد:

- دکمهٔ همبرگر منوی موبایل و دکمهٔ بستن drawer در `admin-panel.tsx` —
  به الگوی `flex size-11 items-center justify-center rounded-md`
  تغییر کرد.
- دکمهٔ بستن (X) کامپوننت مشترک `Modal` در `admin-ui.tsx` — همین الگو،
  چون این کامپوننت مشترک همهٔ مودال‌های پنل است.
- **تصمیم عمدی برعکس:** دکمه‌های ویرایش/حذف داخل ردیف جدول‌ها
  (`iconBtnClass`, یک مربع ۳۲px) **عمداً دست‌نخورده ماندند** —
  بزرگ‌کردن آن‌ها به ۴۴px ارتفاع هر ردیف را در جدول‌های چگال (اولویت
  رسمی پنل ادمین) به‌شکل محسوس افزایش می‌داد؛ این استاندارد اصلاً برای
  اکشن‌های ردیفی/ثانویه نوشته نشده بود.

## ۳. محافظ ارتفاع سایدبار

`overflow-y-auto` به سایدبار دسکتاپ (`lg:flex`) و drawer موبایل اضافه
شد — با ۸ آیتم ناوبری فعلی، این یک محافظ پیشگیرانه است برای
صفحه‌نمایش‌های کوتاه، نه رفع یک باگ مشاهده‌شده.

## ۴. نشانگر اسکرول افقی جدول‌های پهن

یک کامپوننت مشترک تازه، `ScrollFade`، به `admin-ui.tsx` اضافه شد (عیناً
همان الگوی گرادیان محو دو-لبهٔ فاز ۴.۸) و روی جدول‌های **۶ ستون یا
بیشتر** وایر شد: `BookingsTable` (۹)، `TripScheduler` (۸)،
`DashboardView`→رزروهای اخیر (۷)، `LoyaltyManager`→کوپن‌ها (۷)،
`ReportsDashboard`→هر دو جدول (۵، برای یکدستی)، `BusManager` و
`RouteManager` (۶ هرکدام). **تصمیم عمدی:** `DriverManager`، جدول سطوح
`LoyaltyManager`، و جدول سفرهای پیش‌رو `DashboardView` عمداً بدون تغییر
ماندند — حتی در ۳۲۰px هم بدون اسکرول افقی جا می‌شوند.

از `start-0`/`end-0` منطقی (نه `left-0`/`right-0` فیزیکی) استفاده شد.

## بررسی‌شده و بدون تغییر رها شد (چون از قبل درست بود)

- نوار جست‌وجو/فیلتر `BookingsTable` از قبل `flex flex-col
  sm:flex-row` بود.
- گرید کارت‌های آماری از قبل درست استاک می‌شد؛ افزودن بریک‌پوینت‌های
  ۳xl/۴xl اینجا بی‌فایده بود چون با فقط ۴ کارت، ستون بیشتر فقط سلول
  خالی تولید می‌کرد.
- ورودی‌های عادی فرم و چک‌باکس‌ها دست‌نخورده ماندند — استاندارد ۴۴px
  مخصوص دکمه‌های آیکن‌محورِ مستقل بود، نه عناصر فرم استاندارد.

## فایل‌های تغییریافته (۱۰ فایل، بدون فایل جدید)

`components/admin/admin-ui.tsx`، `components/transport/admin-panel.tsx`،
و هفت مدیر: `bookings-table.tsx`, `bus-manager.tsx`,
`dashboard-view.tsx`, `driver-manager.tsx`, `loyalty-manager.tsx`,
`reports-dashboard.tsx`, `route-manager.tsx`, `trip-scheduler.tsx`.

## اعتبارسنجی

`tsc --noEmit` صفر خطای تازه. `next build` واقعی (Turbopack) روی هر ۲۹
مسیر موفق — فقط فچ فونت گوگل به‌خاطر محدودیت شبکهٔ sandbox شکست خورد.

## بدهی/نکات باز

- تست چشمی واقعی روی دستگاه‌های واقعی هنوز باقی است — این فاز فقط
  ممیزی و اصلاح ساختاری کد بود.
- اگر تعداد آیتم‌های ناوبری سایدبار در فازهای بعد بیشتر شود، ممکن است
  سایدبار نیاز به گروه‌بندی/کولاپس‌شدن پیدا کند.
