# فاز ۵.۱۰ — گزارش‌گیری و retention

**تکمیل:** ۷ سپتامبر ۲۰۲۶

## زمینه

دکمه‌های ۷/۳۰/۹۰ روز در `ReportsDashboard` (فاز ۵.۳) فقط shortcut
بودند؛ فیلدهای «از تاریخ»/«تا تاریخ» بدون `min` مستقیم روی bookings/
trips واقعی کوئری می‌زدند (یعنی تئوریاً از قبل می‌شد با تایپ دستی یک
تاریخ خیلی قدیمی کل تاریخچه را دید) و هیچ داده‌ای حذف/آرشیو نمی‌شد.

## تغییرات کد

### دکمهٔ چهارم «همهٔ بازه‌ها» (`ReportsDashboard`)

- `Preset` یک مقدار تازه گرفت: `"all"`.
- تابع تازهٔ `applyAllTime()`: قدیمی‌ترین `trips.service_date` را
  یک‌بار lazy fetch می‌کند (`order(service_date, asc).limit(1)`)،
  نتیجه را کش می‌کند (`earliestDate` state) تا کلیک‌های بعدی کوئری
  اضافه نزنند، و بازهٔ from/to را از آن تاریخ تا امروز تنظیم می‌کند.
  اگر هیچ سفری ثبت نشده باشد (پروژهٔ کاملاً خالی)، fallback به «امروز»
  می‌شود تا کاربر با خطا مواجه نشود.
- محور «قدیمی‌ترین» عمداً `trips.service_date` انتخاب شد، نه
  `bookings.created_at` — چون خودِ این گزارش از فاز ۵.۳ بر مبنای تاریخ
  سفر فیلتر می‌شود (نه تاریخ ثبت رزرو)؛ استفاده از یک محور متفاوت فقط
  برای دکمهٔ all-time، دو منبع حقیقت متفاوت در یک کامپوننت می‌ساخت.
- دکمه در حال fetch شدن (اولین کلیک) اسپینر و `disabled` نشان می‌دهد.
- کلید i18n تازه: `admin.reportsPanel.allTime` (فارسی/انگلیسی).

هیچ تغییری در منطق کوئری/محاسبهٔ درآمد یا export CSV لازم نبود — همان
state (`from`/`to`) که presetهای قبلی هم استفاده می‌کردند، مصرف شد.

## تصمیم مکتوب: سیاست نگهداری/بکاپ (بدون تغییر کد)

طبق درخواست دقیق این فاز، این فقط یک تصمیم مکتوب بود، نه قابلیت کد.
یادداشت کامل زیر بخش «Phase 11 — Production Deployment & Recovery» در
`ROAD-MAP.md` ثبت شد. خلاصه:

- **نگهداری:** فعلاً بدون حذف/آرشیو خودکار — نامحدود، برای حسابداری و
  همین گزارش all-time.
- **بکاپ (یافتهٔ بررسی‌شده، نه فرضی):** پروژهٔ Supabase این پروژه با
  `Supabase:get_organization` چک شد — پلن **Free** است. طبق مستندات
  رسمی Supabase، پلن Free هیچ بکاپ خودکار قابل‌بازیابی ندارد. یعنی
  امروز، با داده‌ٔ واقعی مشتری/رزرو/پرداخت، هیچ بکاپی گرفته نمی‌شود —
  این یک ریسک واقعی است که در ROAD-MAP صریح ثبت شد (نه صرفاً یک آیتم
  فرضی برای فاز ۱۱).
- تصمیم نهایی دربارهٔ ارتقای پلن (Pro ≈ ۷ روز بکاپ روزانه، یا افزودن
  PITR) و RPO/RTO دقیق، صریحاً برای فاز ۱۱ گذاشته شد — این یادداشت
  فقط جلوی فراموشی گپ فعلی را می‌گیرد.

## اعتبارسنجی

`tsc --noEmit`: صفر خطای تازه (همان baseline ۹ خطای از پیش‌موجود در
چهار فایل بی‌ربط، مثل فاز ۵.۹). `next build`: موفق روی هر ۲۹ روت
(فونت‌های Google موقتاً stub و بعد از build بازگردانده شدند).

## فایل‌های تغییریافته

`components/admin/reports-dashboard.tsx`، `lib/i18n.ts`، `ROAD-MAP.md`.
