# فاز ۳.۱ — طراحی جداول Supabase

> این سند مکمل فایل `phase-3.1-supabase-schema.sql` است و توضیح می‌دهد **چرا**
> هر جدول به این شکل طراحی شده، تا در چت‌های بعدی (فاز ۳.۲ به بعد) نیازی به
> توضیح مجدد نباشد. فایل SQL قابل اجرای مستقیم در Supabase SQL Editor است.

## ۱. فهرست جداول (۱۷ جدول)

| گروه | جداول |
|---|---|
| مرجع/ناوگان | `cities`, `routes`, `buses`, `drivers` |
| عملیات سفر | `trips`, `trip_seats` |
| رزرو و پرداخت | `bookings`, `booking_passengers`, `payments` |
| باشگاه مشتریان | `customers`, `loyalty_tiers`, `wallet_transactions`, `referrals` |
| کمپین‌ها | `coupons`, `coupon_redemptions` |
| دسترسی ادمین | `admins` |

همهٔ موارد خواسته‌شدهٔ صریح فازبندی درختی (بخش ۶ پرامپت مادر) پوشش داده
شده‌اند: «مسیر، بس، راننده، سفر، رزرو، مسافر، وضعیت پرداخت، customers،
loyalty_tiers، wallet_transactions، referrals، coupons».

## ۲. تصمیمات طراحی کلیدی و دلیل هرکدام

1. **`bus_id` و `driver_id` روی `trips` نال‌پذیرند.** طبق تصمیم جلسهٔ
   طرح‌ریزی، شرکت ممکن است پلاک/راننده را از قبل مشخص نکند.

2. **`total_seats_snapshot` روی خود `trips` کپی می‌شود** (نه فقط خواندن از
   `buses`). اگر بعداً بس یک سفرِ گذشته حذف یا تعویض شود، ظرفیت آن سفر در
   گزارش‌های تاریخی درست می‌ماند. ظرفیت *پیش‌فرض* بس جدید هنگام ساخت در
   پنل ادمین از همان دو عدد فعلی کد (`vip: 33`, `standard: 48`) پیشنهاد
   می‌شود، اما مقدار نهایی روی خود رکورد `buses.total_seats` است، نه
   هاردکد در منطق — دقیقاً طبق کامنت موجود در `lib/booking-data.ts`.

3. **`schedule_type` دو مقدار دارد: `fixed_time` / `fill_and_go`.** مطابق
   تصمیم جلسه («احتمال وجود چند مسیر با حرکت بر اساس پرشدن ظرفیت … نه
   الزامی برای هر مسیر»)، این یک ستون اختیاری در سطح *سفر* است، نه یک
   قابلیت اجباری در سطح مسیر.

4. **`trip_seats` به‌جای تولید نقشهٔ صندلی در لحظه (مثل نسخهٔ فعلی
   `buildCoach` در کد نمایشی)، واقعاً در دیتابیس ذخیره می‌شود.** این تنها
   راه جلوگیری واقعی از رزرو هم‌زمان یک صندلی توسط دو مسافر است. ستون
   `held_until` یک قفل موقت (چند دقیقه‌ای) هنگام تکمیل فرم پرداخت فراهم
   می‌کند تا رزرو اتمیک صندلی (نیازمندی صریح فاز ۴.۲) در Route Handler
   قابل‌پیاده‌سازی باشد.

5. **رزرو مهمان کاملاً پشتیبانی می‌شود.** `bookings.customer_id` نال‌پذیر
   است؛ `contact_name`/`contact_phone` مستقل ذخیره می‌شوند. با این حال،
   هر شمارهٔ تماس هنگام اولین رزرو مهمان می‌تواند در `customers` هم یک
   رکورد «ثبت‌نام‌نشده» (`is_registered = false`) بگیرد تا اگر مشتری بعداً
   واقعاً ثبت‌نام کرد، سابقهٔ سفرهایش بر اساس شماره تماس منتقل شود و از
   صفر شروع نشود. این منطق انتقال، پیاده‌سازی‌اش در فاز ۴.۵ است؛ فقط
   ساختار جدول این‌جا آماده شده.

6. **`booking_status` مقدار `completed` دارد** (که در نوع فعلی
   `AdminBooking` در کد نمایشی وجود ندارد) چون شمارش «سفر تکمیل‌شده» برای
   ارتقای سطح باشگاه مشتریان فقط باید بعد از انجام واقعی سفر اتفاق بیفتد،
   نه در لحظهٔ تأیید پرداخت. مقادیر `pending`/`confirmed`/`cancelled` عیناً
   با enum فعلی در `admin-data.ts` هم‌نام‌اند تا پنل ادمین موجود بدون
   remapping قابل اتصال باشد.

7. **مبلغ تخفیف سطح عضویت و تخفیف کوپن در دو ستون جدا نگه داشته می‌شوند**
   (`tier_discount_amount` و `coupon_discount_amount`)، نه یک ستون واحد
   `discount_amount`. دلیل: پرامپت مادر صریحاً می‌گوید قابلیت جمع‌شدن یا
   نشدنِ این دو «در فاز ۴.۲ دقیق مشخص می‌شود». نگه‌داشتن جدا یعنی وقتی آن
   قانون نهایی شد، نیازی به تغییر ساختار جدول (migration جدید) نیست، فقط
   منطق محاسبه در فرم رزرو تغییر می‌کند.

8. **`loyalty_tiers` یک جدول است، نه enum ثابت در کد**، چون پرامپت مادر
   صراحتاً می‌گوید آستانه و درصد باید «از پنل ادمین قابل‌تغییر باشد … مشتری
   باید بتواند بعداً خودش این اعداد را تنظیم کند». سه ردیف پیش‌فرض (برنز
   ۰/۵٪، نقره ۵ سفر/۱۰٪، طلا ۱۵ سفر/۱۷.۵٪) به‌عنوان seed اولیه درج شده‌اند
   و از پنل `LoyaltyManager` (فاز ۵.۴) قابل ویرایش‌اند.

9. **شمارش سفر برای سطح‌بندی، تجمعی مادام‌العمر است**
   (`customers.lifetime_completed_trips`)، دقیقاً طبق تصمیم بخش ۵ — بدون
   نیاز به cron محاسباتی سالانه.

10. **`wallet_transactions` یک دفتر تراکنش (ledger) است، نه فقط یک عدد
    موجودی.** هر واریز (کش‌بک، پاداش رفرال) و برداشت (استفاده در رزرو،
    اصلاح دستی ادمین) یک ردیف مجزا می‌گیرد. `customers.wallet_balance` یک
    مقدار cache شده از جمع این دفتر است (برای خواندن سریع در UI)، اما
    منبع حقیقت خودِ دفتر تراکنش‌هاست.

11. **`referrals.referred_customer_id` یکتا (`unique`) است** تا هر مشتری
    فقط یک‌بار به‌عنوان «دعوت‌شده» حساب شود و کسی نتواند چندبار با کدهای
    رفرال متفاوت پاداش بگیرد. خودِ مبلغ پاداش در `wallet_transactions`
    برای هر دو طرف ثبت می‌شود، نه در جدول `referrals`.

12. **`coupons` کاملاً مستقل از `loyalty_tiers` است** (سیستم مستقل کمپین
    فصلی طبق بخش ۵)، با پرچم `is_stackable_with_tier` که فعلاً فقط آماده
    نگه‌داشته شده تا منطق نهایی جمع‌شدن در فاز ۴.۲ رویش سوار شود.

13. **جدول `admins` در همین فاز ساخته شده** (نه در فاز ۳.۳)، صرفاً چون چند
    جدول دیگر (`payments.confirmed_by_admin_id`, `coupons.created_by_admin_id`)
    به آن نیاز به FK دارند. اتصال واقعی `auth_user_id` به Supabase Auth و
    نوشتن policyهای دسترسی بر اساس نقش، موضوع فاز ۳.۳ باقی می‌ماند.

## ۳. آنچه عمداً در این فاز انجام **نشده**

- **Row Level Security (RLS)** — فاز ۳.۲.
- **احراز هویت واقعی پنل ادمین** — فاز ۳.۳.
- **تریگرهای محاسباتی** (مثل به‌روزرسانی خودکار `wallet_balance` یا افزایش
  `lifetime_completed_trips` هنگام تغییر وضعیت رزرو به `completed`) — این‌ها
  منطق لایهٔ سرور هستند و در فاز ۴ (هم‌زمان با نوشتن Route Handlerهای واقعی
  رزرو) پیاده‌سازی می‌شوند، تا با قوانین نهایی محاسبهٔ تخفیف هم‌زمان طراحی
  شوند و دوباره‌کاری نشود.
- **قانون نهایی جمع‌شدن تخفیف سطح با کوپن** — عمداً به فاز ۴.۲ موکول شده.

## ۴. مرحلهٔ بعد

فاز ۳.۲: نوشتن سیاست‌های RLS برای هر جدول (به‌ویژه `bookings`,
`customers`, `wallet_transactions`, `payments`) به‌گونه‌ای که:
- مسافر مهمان بدون حساب کاربری فقط از طریق `booking_reference` + `contact_phone`
  به رزرو خودش دسترسی داشته باشد (نه از طریق RLS مبتنی بر `auth.uid()`).
- مشتری ثبت‌نام‌شده فقط رکوردهای خودش را ببیند.
- ادمین کل به همه‌چیز، ادمین محدود فقط به بخش‌های `allowed_sections` خودش.
