# فاز ۴.۳ — صفحهٔ پیگیری رزرو (اتصال کامل به دادهٔ واقعی Supabase)

## ۱. فایل‌های این تحویل

| فایل | نوع تغییر |
|---|---|
| `phase-4_3-schema-additions.sql` | مستندسازی — قبلاً از طریق MCP روی پروژهٔ زندهٔ Supabase اجرا شده |
| `src/lib/supabase/queries.ts` | افزایشی — تابع جدید `getBookingByReferenceAndPhone` اضافه شد |
| `src/app/api/bookings/lookup/route.ts` | جدید — جست‌وجوی رزرو با کد رهگیری + شمارهٔ تماس |
| `src/app/api/bookings/update-contact/route.ts` | جدید — ویرایش شمارهٔ تماس رزرو |
| `src/app/api/bookings/request-cancellation/route.ts` | جدید — درخواست کنسلی رزرو |
| `src/app/track/page.tsx` | جدید — اولین بار است که مسیر `/track` (که فوتر از قبل به آن لینک می‌داد) واقعاً به یک `page.tsx` وصل می‌شود |
| `src/components/transport/booking-lookup.tsx` | افزایشی — دادهٔ جعلیِ مبتنی بر hash حذف و با سه Route Handler بالا جایگزین شد؛ ساختار UI دست‌نخورده ماند |
| `src/lib/i18n.ts` | افزایشی — فقط چند کلید جدید در هر دو زبان (`track.searching/saving/cancelSubmitting/statusLabel/cancelNotAllowed/alreadyCancelled` و `admin.status.completed/refunded`) |

**فایل‌های عمداً دست‌نخورده:** `lib/booking-data.ts` (فقط `cityLabel`/`formatTime` مصرف شدند، بدون تغییر در خودِ فایل)، `site-header.tsx`/`site-footer.tsx` (فوتر از قبل لینک درست `/track` را داشت، نیازی به تغییر نبود).

## ۲. یک یافتهٔ امنیتیِ خارج از حیطهٔ برنامه‌ریزی‌شدهٔ این فاز (اصلاح‌شده)

قبل از نوشتن کد این فاز، طبق اصل «هیچ‌چیز بدون شواهد واقعی فرض نشود»، وضعیت واقعیِ توابع فاز ۴.۲ را مستقیم روی دیتابیس زنده (نه فقط با اعتماد به README قبلی) بررسی کردم:

- README فاز ۴.۲ ادعا کرده بود `hold_seats`/`release_seats`/`confirm_booking` «صراحتاً از anon/authenticated گرفته و فقط به service_role داده شدند».
- بررسی مستقیم `pg_proc.proacl` نشان داد این revoke **هرگز واقعاً اجرا نشده بود** — هر سه تابع همچنان `EXECUTE` مستقیم برای `anon` و `authenticated` داشتند.
- یعنی هرکسی فقط با همان `anon key` عمومی (که در باندل مرورگر افشا می‌شود) می‌توانست این سه تابع حساس را مستقیم از REST API عمومی Supabase صدا بزند — مثلاً صندلیِ رزروهای دیگران را جعلی `hold` یا حتی یک `confirm_booking` واقعی بدون عبور از اعتبارسنجی `app/api/bookings/confirm` بسازد.
- **این‌جا واقعاً اصلاح شد**: `revoke execute ... from public, anon, authenticated` + `grant execute ... to service_role` روی هر سه تابع اجرا و با کوئری مجدد تأیید شد.
- علاوه بر این، هشدار `function_search_path_mutable` روی دو تابع جدید همین فاز هم با `set search_path = public` برطرف شد. همین هشدار روی سه تابع قدیمی فاز ۴.۲ هم هنوز باقی است (ریسک پایین، نه بحرانی) — اگر خواستید در یک تحویل بعدی همان‌ها را هم اصلاح کنم.

## ۳. تصمیمات فنی و دلیل هرکدام

1. **مسیر واقعی `/track` انتخاب شد، نه `/my-booking` پیشنهادیِ سند مادر.** بخش ۴.۳ سند مادر مسیر `/my-booking` را «پیشنهادی» عنوان کرده بود، اما فوتر واقعی کد (`lib/i18n.ts` → `footer.quick`) از قبل به `/track` لینک می‌داد. طبق اصل «شواهد واقعی کد بر پیشنهاد سند ارجحیت دارد»، از `/track` استفاده شد تا لینک از قبل موجود در فوتر بدون هیچ تغییری کار کند.

2. **دو تابع جدید سمت دیتابیس (`update_booking_contact_phone`, `request_booking_cancellation`) ساخته شد، نه UPDATE مستقیم از Route Handler.** دقیقاً همان الگوی `hold_seats`/`confirm_booking` در فاز ۴.۲: تطابق کد رهگیری + شمارهٔ تماس در یک `WHERE` واحد و اتمیک بررسی می‌شود، نه یک `SELECT` جدا و بعد `UPDATE` جدا (که در آن فاصله می‌شد رزرو را تغییر داد).

3. **`getBookingByReferenceAndPhone` برای رزروِ نایافته/شمارهٔ تماسِ نادرست، دقیقاً همان خروجی (`null` → HTTP 404 با کد `NOT_FOUND`) را برمی‌گرداند.** تا نتوان با آزمایش شماره‌های مختلف روی یک کد رهگیریِ حدسی، معتبر بودن آن کد را تشخیص داد (حملهٔ enumeration).

4. **کنسلی خودکار فقط برای رزروهای هنوز `pending` مجاز است.** این یک تصمیم طراحی است که سند مادر دقیقش نکرده بود؛ خودم گرفتم چون: رزروِ `pending` یعنی هیچ اپراتوری در فاز ۵.۲ پرداخت را تأیید نکرده، پس کنسلی بی‌خطر است و صندلی بلافاصله آزاد می‌شود. رزروِ `confirmed` احتمالاً پول واقعی گرفته شده، پس کنسلی خودکار مسدود است و پیام `NOT_CANCELLABLE` کاربر را به تماس با دفتر ارجاع می‌دهد — تا کنسلی/بازپرداخت واقعی از مسیر ادمین (فاز ۵.۲) یا درگاه (فاز ۶) عبور کند.

5. **ستون یا enum جدیدی برای «درخواست کنسلی» اضافه نشد.** به‌جای فرضِ یک وضعیت میانی («در انتظار بررسی کنسلی») که در طراحی فاز ۳.۱ اصلاً وجود نداشت، از همان ستون‌های موجود (`status='cancelled'`, `cancelled_at`) استفاده شد — این دو ستون از فاز ۳.۱ دقیقاً برای همین منظور آماده بودند اما تا این فاز هیچ‌جا نوشته نمی‌شدند.

6. **پس از کنسلی، فقط `trip_seats` آزاد می‌شود؛ رکورد `payments` دست‌نخورده می‌ماند.** چون در وضعیت `pending`، پرداخت هم هنوز `pending` است (نه واقعاً دریافت‌شده)، نیازی به `refunded` کردن آن نیست؛ ادمین در فاز ۵.۲ رزروهای کنسل‌شده را می‌بیند و پرداختِ معلق مرتبط را خودش می‌بندد.

7. **دو کلید وضعیت `completed`/`refunded` به `dictionary.admin.status` اضافه شد** (قبلاً فقط `confirmed`/`pending`/`cancelled` داشت). چون `booking_status` واقعی دیتابیس این دو مقدار را هم دارد و صفحهٔ پیگیریِ عمومی ممکن است یک رزروِ تاریخیِ تکمیل‌شده یا بازپرداخت‌شده را هم نشان دهد — بدون این دو کلید، برچسب وضعیت `undefined` نمایش داده می‌شد.

## ۴. آنچه عمداً در این فاز انجام **نشده**

- **اطلاع‌رسانی پیامکی واقعی بعد از ویرایش تماس/کنسلی** — فاز ۷ (زیرساخت SMS). متن فعلی («تأییدیهٔ نهایی از طریق پیامک ارسال می‌شود») همان متنِ از قبل موجود در UI است و دقیق می‌ماند چون واقعاً چیزی غیر از این ادعا نمی‌کند.
- **کارتابل مدیریت کنسلی/تأیید پرداخت آفلاین در پنل ادمین** — فاز ۵.۲.
- **کنسلیِ رزروهای `confirmed` با بازپرداخت واقعی** — نیازمند درگاه HesabPay (فاز ۶) یا تأیید دستی ادمین (فاز ۵.۲)؛ عمداً از این صفحهٔ عمومی مسدود شد (بند ۴ بالا).
- **اصلاح هشدار `function_search_path_mutable` روی سه تابع قدیمیِ فاز ۴.۲** (`hold_seats`, `release_seats`, `confirm_booking`) — ریسک پایین است و خارج از حیطهٔ این فاز؛ اگر بخواهید در یک migration کوچک جداگانه انجامش می‌دهم.

## ۵. برای تست

۱. یک رزرو واقعی از جریان کامل فاز ۴.۲ بسازید (جست‌وجو → انتخاب صندلی → پرداخت) تا کد رهگیری و شمارهٔ تماس واقعی داشته باشید.
۲. به `/track` بروید، همان کد رهگیری + شمارهٔ تماس را وارد کنید → باید جزئیات رزرو واقعی (مسیر، ساعت، چوکی‌ها، وضعیت، مبلغ) را ببینید، نه دادهٔ تصادفی.
۳. یک شمارهٔ تماس نادرست امتحان کنید → باید «رزروی با این مشخصات یافت نشد» ببینید (نه خطای فنی).
۴. روی «ویرایش اطلاعات تماس» کلیک و شمارهٔ جدید ثبت کنید → با رفرش صفحه و جست‌وجوی مجدد با شمارهٔ جدید، باید همان رزرو یافت شود.
۵. روی «درخواست کنسلی» کلیک و تأیید کنید (روی رزروِ هنوز `pending`) → وضعیت باید فوراً «لغوشده» شود و دکمه‌های مدیریت رزرو دیگر نمایش داده نشوند.
۶. همان رزروِ کنسل‌شده را در پنل ادمین (فاز ۳.۳) یا مستقیم در Supabase بررسی کنید → صندلی‌های مرتبط باید به `available` برگشته باشند.

## ۶. مرحلهٔ بعد

فاز ۴.۴: صفحات معرفی/تماس/مسیرها/سوالات متداول.
