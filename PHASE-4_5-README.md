# فاز ۴.۵ — حساب کاربری مسافر و باشگاه مشتریان

## نکتهٔ مهم دربارهٔ این زیپ

مثل تحویل قبلی، این زیپ **کامل و انباشته** است (بدون پوشهٔ `src/` — طبق
اصلاحیهٔ قبلی، مستقیم زیر ریشهٔ `Frontend/`): فازهای ۴.۳ و ۴.۴ و ۴.۵ هر سه
داخلش هستند. کافی است همین یکی جایگزین شود.

## ۱. فایل‌های این تحویل (فقط بخش جدید فاز ۴.۵)

| فایل | نوع تغییر |
|---|---|
| `phase-4_5-schema-additions.sql` | مستندسازی — قبلاً از طریق MCP روی پروژهٔ زندهٔ Supabase اجرا شده |
| `lib/supabase/database.types.ts` | بازتولیدشده — چون امضای `confirm_booking` و توابع جدید عوض شده |
| `lib/supabase/queries.ts` | افزایشی — `getCustomerAccount`, `getCustomerBookingHistory` |
| `app/api/bookings/confirm/route.ts` | افزایشی — تشخیص خودکار مسافرِ واردشده و پاس‌دادن `customer_id` |
| `middleware.ts` | افزایشی — گسترش‌یافته برای محافظت از `/account/*` (مستقل از منطق ادمین) |
| `lib/i18n.ts` → `lib/i18n.ts` (بخش `account`) | افزایشی — کلیدهای جدید هر دو زبان |
| `lib/pending-profile.ts` | جدید — کمکی سمت کلاینت برای جریان ثبت‌نام |
| `components/transport/tier-badge.tsx` | جدید |
| `components/transport/wallet-card.tsx` | جدید |
| `components/transport/referral-card.tsx` | جدید |
| `components/transport/booking-history-list.tsx` | جدید |
| `components/transport/account-login.tsx` | جدید |
| `components/transport/account-signup.tsx` | جدید |
| `components/transport/account-complete-profile.tsx` | جدید |
| `components/transport/account-logout-button.tsx` | جدید |
| `components/transport/account-dashboard.tsx` | جدید |
| `components/transport/site-header.tsx` | افزایشی — دکمهٔ «ورود» به `/account` وصل شد (بند ۴ پایین) |
| `app/account/page.tsx` | جدید — داشبورد (Server Component) |
| `app/account/login/page.tsx` | جدید |
| `app/account/signup/page.tsx` | جدید |
| `app/account/complete-profile/page.tsx` | جدید |

**فایل‌های عمداً دست‌نخورده:** `components/transport/checkout-form.tsx` (توضیح در بند ۵)، `lib/booking-data.ts`، `app/admin/*` (منطق ادمین کاملاً مستقل ماند).

## ۲. تصمیمات فنی و دلیل هرکدام

1. **`signup_customer` یک تابع `SECURITY DEFINER` است، نه INSERT مستقیم از کلاینت.** هیچ policy INSERT روی `customers` برای `authenticated` وجود نداشت (طبق تصمیم صریح فاز ۳.۲). این تابع همیشه از `auth.uid()` خودِ کاربر واردشده استفاده می‌کند (نه یک uid دلخواه که از کلاینت پاس داده شود)، سطح برنز پیش‌فرض را تخصیص می‌دهد، کد معرفی ۶‌کاراکتریِ یکتا می‌سازد، و اگر کد معرفیِ معتبری داده شده باشد یک رکورد `referrals` با وضعیت `pending` می‌سازد. فقط به `authenticated` اجازهٔ اجرا دارد.

2. **تریگر `handle_booking_completed` روی `bookings` ساخته شد** تا وقتی وضعیت رزرو به `completed` تغییر کند: (الف) `lifetime_completed_trips` مشتری یک واحد اضافه شود، (ب) اگر آستانهٔ سطح بعدی رسیده باشد سطح ارتقا یابد، (ج) اگر این اولین سفر تکمیل‌شدهٔ یک مشتریِ دعوت‌شده باشد، پاداش رفرال به هر دو طرف (دعوت‌کننده/دعوت‌شونده) در کیف‌پول واریز شود. **این تریگر تا فاز ۵.۲ (که ادمین بتواند واقعاً یک رزرو را completed کند) عملاً بی‌اثر می‌ماند** — اما از همین حالا صحیح و آماده است، دقیقاً طبق یادداشت فاز ۳.۱ («این‌ها منطق لایهٔ سرور هستند و در فاز ۴ پیاده‌سازی می‌شوند»).

3. **مبلغ پاداش رفرال (۱۰۰ واحد) در کد هاردکد شد، نه یک ستون تنظیمات جدید.** برخلاف درصد سطوح باشگاه مشتریان (که از فاز ۳.۱ در جدول `loyalty_tiers` قابل‌تغییر طراحی شده بود)، هیچ جدول/ستون تنظیماتی برای «مبلغ پاداش رفرال» در فاز ۳.۱ پیش‌بینی نشده بود. این عدد به‌عنوان یک مقدار پیش‌فرض مستند (با کامنت صریح در تابع) نگه داشته شد تا وقتی `LoyaltyManager` (فاز ۵.۴) بسازید، به‌راحتی قابل‌تغییر به یک ستون واقعی باشد.

4. **`confirm_booking` بازتعریف کامل شد** (نه فقط افزوده) تا `p_customer_id` بگیرد و تخفیف سطح باشگاه مشتریان (`tier_discount_amount`) را خودکار از `loyalty_tiers.discount_percent` محاسبه کند — دقیقاً همان قانونی که فاز ۴.۲ به فاز ۴.۵ موکول کرده بود. جمع‌شدن با کوپن غیرمشروط است مگر کوپن صراحتاً `is_stackable_with_tier = false` باشد. نسخهٔ قدیمی تابع صریحاً `drop` شد (نه فقط `create or replace`) چون امضا تغییر کرده و دو overload هم‌زمان باعث خطای «ambiguous function» در PostgREST می‌شد.

5. **`checkout-form.tsx` عمداً دست‌نخورده ماند.** اتصال `customer_id` به رزرو، خودکار و شفاف در Route Handler (`app/api/bookings/confirm`) انجام می‌شود: اگر درخواست از یک مسافرِ واردشده بیاید، `customer_id`ِ واقعی‌اش را خودش پیدا و به `confirm_booking` پاس می‌دهد — بدون نیاز به هیچ تغییری در فرم پرداخت. یعنی از همین امروز، مسافرِ واردشده‌ای که رزرو می‌کند، تخفیف سطحش خودکار اعمال و رزرو به تاریخچهٔ حسابش وصل می‌شود. **آنچه هنوز اضافه نشده:** نمایش این تخفیف در خودِ فرم پرداخت (کاربر تخفیف را در صفحهٔ تأییدیه می‌بیند، نه پیش از تأیید نهایی) و امکان استفاده از موجودی کیف‌پول (`wallet_amount_used`) در چک‌اوت — هر دو نیازمند تغییر UI فرم پرداخت هستند که عمداً برای یک تحویل کوچک و امن‌تر بعدی نگه داشته شد (بند ۴ پایین).

6. **`middleware.ts` گسترش یافت، نه یک middleware دوم ساخته شد.** Next.js فقط یک فایل `middleware.ts` در ریشه اجازه می‌دهد؛ منطق `/admin/*` (فاز ۳.۳) و `/account/*` (فاز ۴.۵) در همان تابع، اما کاملاً مستقل از هم (یک مسافر واردشده هرگز از مسیر ادمین رد نمی‌شود و برعکس) نگه داشته شد.

7. **دکمهٔ عمومی «ورود» در هدر، از `/admin` به `/account` تغییر کرد.** این دکمه از فاز ۱ (خروجی v0.dev) همیشه به پنل ادمین اشاره می‌کرد — یک اشتباهِ باقی‌مانده که در حافظهٔ پروژه هم به‌عنوان «پلیس‌هولدر منتظر فاز بعد» ثبت شده بود. حالا که یک سیستم ورود واقعیِ مسافر وجود دارد، این اصلاح منطقی و ضروری بود. پنل ادمین همچنان از طریق آدرس مستقیم `/admin` در دسترس است، فقط دیگر در نوار ناوبری عمومی تبلیغ نمی‌شود (رویهٔ استاندارد).

8. **صفحات `/account/login`, `/account/signup`, `/account/complete-profile` مستقیماً با کلاینت مرورگر (`createClient` از `lib/supabase/client.ts`) کار می‌کنند، نه یک Route Handler جدید.** چون `signUp`/`signInWithPassword`/فراخوانی RPC، همگی عملیات استاندارد Supabase Auth هستند که کلاینت anon به‌طور طبیعی برایشان طراحی شده — دقیقاً همان الگویی که به‌احتمال زیاد `app/admin/login/page.tsx` در فاز ۳.۳ هم استفاده کرده.

9. **جریان «تأیید ایمیل» با احتیاط طراحی شد چون تنظیمات این پروژه Supabase از این چت قابل‌بررسی نبود.** اگر تأیید ایمیل فعال باشد، `auth.signUp()` بلافاصله نشست نمی‌دهد؛ اطلاعات فرم (شماره تماس/نام/کد معرفی) موقتاً در `sessionStorage` مرورگر نگه داشته می‌شود (`lib/pending-profile.ts`) تا بعد از تأیید و اولین ورود خودکار تکمیل شود. اگر مرورگر/دستگاه عوض شود یا این حافظه پاک شود، `/account/complete-profile` مسیر مطمئنِ جایگزین است. اگر تأیید ایمیل غیرفعال باشد (نشست بلافاصله می‌آید)، پروفایل همان لحظه ساخته می‌شود و این مسیر جایگزین اصلاً لازم نمی‌شود.

10. **خواندن پروفایل/کیف‌پول/رفرال/تاریخچهٔ رزرو، همه با کلاینت anon معمولی انجام می‌شود، نه service_role.** چون RLS فاز ۳.۲ از قبل دقیقاً برای این حالت طراحی شده بود (`customers_self_select`, `wallet_tx_owner_select`, `referrals_owner_select`, `bookings_owner_select` — همه با `auth.uid()`/`current_customer_id()`). نیازی به دور زدن RLS نبود.

## ۳. یافتهٔ فرعی هنگام بررسی

هنگام طراحی `handle_booking_completed`، متوجه شدم `payments`/`bookings` هیچ خودکارسازی‌ای برای تبدیل `confirmed`→`completed` ندارند (منطقی، چون این کار به تشخیص «سفر واقعاً انجام شد» نیاز دارد که فعلاً هیچ منبع دادهٔ خودکاری برایش نیست — نه GPS، نه چک‌این راننده). یعنی فعلاً این انتقال باید **دستی توسط ادمین** در فاز ۵.۲ انجام شود؛ تریگر همین که این انتقال دستی ثبت شود درست کار می‌کند.

## ۴. آنچه عمداً در این فاز انجام **نشده**

- **نمایش تخفیف سطح باشگاه مشتریان در خودِ فرم پرداخت** (`checkout-form.tsx`) پیش از تأیید نهایی — فعلاً فقط در صفحهٔ تأییدیه دیده می‌شود، چون خودِ `confirm_booking` آن را محاسبه می‌کند. افزودن پیش‌نمایش زنده در UI یک تحویل کوچک و مجزا خواهد بود.
- **استفاده از موجودی کیف‌پول (`wallet_amount_used`) در چک‌اوت** — ستون از فاز ۳.۱ آماده است، اما نیاز به یک فیلد/چک‌باکس جدید در `checkout-form.tsx` دارد که عمداً برای یک تحویل جداگانه نگه داشته شد (ریسک کمتر برای این تحویل).
- **انتقال تاریخچهٔ رزروهای مهمان به حساب تازه‌ساخته‌شده** (طبق یادداشت فاز ۳.۱، بند ۵) — چون `confirm_booking` هیچ‌وقت برای رزرو مهمان رکورد `customers` نساخته (تصمیم گرفته‌شده که این رفتار باقی بماند)، اصلاً رکورد «ثبت‌نام‌نشده»ای برای انتقال وجود ندارد. اگر این ویژگی را می‌خواهید، نیاز به یک تصمیم صریح جدید دارد (مثلاً: هنگام ثبت‌نام، اگر شماره تماس با booking های مهمان قبلی یکی بود، آن‌ها را به حساب جدید وصل کن).
- **`LoyaltyManager` در پنل ادمین** (تنظیم درصد سطوح/مبلغ پاداش از UI) — فاز ۵.۴.
- **صفحهٔ عمومی معرفی باشگاه مشتریان** (پیش از ثبت‌نام، برای توضیح مزایا) — در نقشهٔ مسیرهای سند مادر نبود؛ اگر بخواهید اضافه می‌کنم.

## ۵. برای تست

۱. به `/account` بروید (بدون ورود) → باید به `/account/login` هدایت شوید.
۲. روی «ثبت‌نام کنید» کلیک، فرم را با شماره تماس/ایمیل/رمز واقعی پر کنید → اگر تأیید ایمیل در این پروژه غیرفعال باشد، مستقیم به داشبورد می‌روید؛ اگر فعال باشد، پیام «ایمیل را تأیید کنید» را می‌بینید.
۳. در داشبورد باید ببینید: سطح «برنز» با ۵٪ تخفیف، موجودی کیف‌پول صفر، یک کد معرفی ۶‌کاراکتری، و تاریخچهٔ رزرو خالی (با دکمهٔ «جست‌وجوی سفر»).
۴. همان‌طور که وارد هستید، یک رزرو واقعی بسازید (جست‌وجو → صندلی → پرداخت) → در `bookings` دیتابیس چک کنید که `customer_id` این‌بار واقعاً پر شده و `tier_discount_amount` برابر ۵٪ subtotal است.
۵. به داشبورد برگردید → رزرو تازه باید در «تاریخچهٔ رزروها» دیده شود.
۶. برای تست رفرال: با یک ایمیل دوم ثبت‌نام کنید و کد معرفیِ حساب اول را وارد کنید → یک رکورد `pending` در جدول `referrals` باید ساخته شود. سپس دستی (چون فاز ۵.۲ هنوز نیست) در SQL Editor وضعیت یکی از رزروهای حساب دوم را به `completed` تغییر دهید → باید کیف‌پول هر دو حساب ۱۰۰ واحد افزایش یابد و `referrals.status` به `completed` برود.
۷. دکمهٔ «ورود» در هدر سایت را چک کنید → باید به `/account` برود، نه `/admin`.

## ۶. مرحلهٔ بعد

فاز ۴.۶: ریسپانسیو حرفه‌ای سایت پابلیک + عکس‌های تطبیقی چند دستگاهی.
