# فاز ۴.۸ (مرحلهٔ ج) — چک‌لیست تست دستی

عرض‌های تست: **۳۷۵ · ۷۶۸ · ۱۰۲۴ · ۱۹۲۰ · ۲۵۶۰px** — هرکدام در هر دو زبان (دری/انگلیسی).

## 🔴 تمرکز ویژه (بحرانی)

1. **#۲۳ — فلش RTL:** روی ۳۷۵px، زبان را به انگلیسی عوض کنید، صفحه را **رفرش کامل** (نه فقط ناوبری) کنید. نباید حتی یک فریم RTL دیده شود.
2. **#۱۷ — تقویم:** روی ۳۷۵px فرم جستجو را باز کنید و تاریخ را لمس کنید — باید یک modal وسط صفحه (نه popover چسبیده) باز شود، با دکمهٔ ضربدر برای بستن. روی ۱۰۲۴/۱۹۲۰px تقویم باید هنوز به‌صورت popover کنار input باز شود و اگر نزدیک لبهٔ صفحه بود خودش را جابه‌جا کند (flip/shift).
3. **#۱۳/۱۴ — نوار CTA ثابت:** در `seats` و `checkout` روی ۳۷۵/۷۶۸px اسکرول کنید — نوار «ادامه/پرداخت» باید همیشه پایین صفحه ثابت بماند. روی ۱۰۲۴px به بالا نباید نوار ثابت دیده شود (sidebar کنار محتوا کافی است).
4. **#۱۸ — هیرو موبایل:** روی ۳۷۵px بدون اسکرول، آیا کل ویجت جستجو (تا دکمهٔ search) دیده می‌شود؟

## 🟡 متوسط

5. **#۱ — account-dashboard:** روی ۷۶۸px سه کارت (سطح/کیف‌پول/رفرال) باید ۲ستونه باشند، نه تک‌ستونه.
6. **#۳ — destinations-grid:** روی نسخهٔ دری (RTL)، گرادیان محو اسکرول باید هنوز سمت چپ/راست درستش باشد (نه برعکس).
7. **#۴ — عکس غزنی:** فعلاً عکس کابل به‌جای غزنی نمایش داده می‌شود (مستند‌شده در کد). نیاز به تصمیم شما.
8. **#۲۰ — جدول routes-index:** روی ۳۷۵px جدول باید گرادیان محو دو لبه داشته باشد.
9. **#۲، #۱۲، #۱۹ — تاچ‌تارگت:** آیکون تقویم، صندلی‌ها، همبرگر منو — همه باید راحت با انگشت لمس شوند.
10. **#۲۴ — فوتر:** روی ۷۶۸px فوتر باید ۲ستونه باشد (نه مستقیم ۴ستونه یا تک‌ستونه).
11. **#۶ — کلاس `text-pretty`:** توضیح پایین‌تر را بخوانید («دربارهٔ text-pretty چه چیزی تست کنم؟»).

## 🟢 جزئی

12. **#۲۵ — why-us:** روی ۱۲۸۰/۱۶۰۰px گرید باید مرحله‌به‌مرحله (نه پرشی) ستون اضافه کند.
13. تأیید کنید حذف `city-mazar.png` و `components/admin/admin-panel.tsx` مشکلی ایجاد نکرده (نباید، چون هیچ‌جا import نمی‌شدند).
14. **[جدید] برش عکس روی مانیتور فوق‌عریض (۱۲.۸):** روی ۲۵۶۰px+ (و اگر مانیتور بزرگ‌تر دارید، حتی بزرگ‌تر) سه‌تا رو چک کنید — هیروی صفحهٔ اصلی، بخش ناوگان، هیروی درباره‌ما. باید خط کوه‌ها/درخشش غروب همیشه دیده بشه، نه فقط آسمان خالی.

## ⚪ نیاز به بررسی بصری مستقیم (بدون این‌ها تصمیم گرفته نشد)

14. **#۷/۱۱ — کنتراست زیرتیتر هیرو** روی موبایل هر دو زبان.
15. **#۸ — `env(safe-area-inset-*)`** روی گوشی‌های نوچ‌دار (iPhone).
16. **#۲۱ — گرید آمار about-page** در بازهٔ ۶۴۰-۷۶۷px با متن واقعی دیکشنری.

## ⏸️ نیاز به ارسال از شما

17. **#۱۰ — عکس تبلت/عریض هیرو:** فعلاً موقتاً از عکس صفحهٔ about (هم‌ابعاد) استفاده شد؛ هروقت عکس واقعی هیرو برای این دو ردهٔ دستگاه آماده شد، بفرستید تا جایگزین کنم (کد الان با کامنت `[PLACEHOLDER]` مشخص شده).

## دربارهٔ text-pretty چه چیزی تست کنم؟

`text-pretty` یک کلاس CSS آزمایشی (`text-wrap: pretty`) است که در ۱۲ فایل روی
متن‌های چندخطی استفاده شده (مثل زیرتیتر هیرو). قبلاً کلاس مشابهش
(`text-balance`) به‌خاطر یک باگ شناخته‌شده حذف شده بود: بعضی مرورگرها بعد از
اینکه فونت وب (Vazirmatn/Fraunces) کامل لود می‌شود، عرض متن را **دوباره
محاسبه نمی‌کنند** — یعنی چیدمان خط‌ها بر اساس فونت جایگزین (fallback) اولیه
ثابت می‌ماند و کمی نامرتب/نامتوازن به نظر می‌رسد، تا کاربر صفحه را
ریسایز یا رفرش کند.

**چیزی که باید ببینید:** آیا بعد از یک لود *کاملاً تازه* صفحه (نه رفرش
معمولی — بهتر است کش مرورگر را هم خالی کنید یا از حالت ناشناس/Incognito
استفاده کنید)، متن‌های چندخطی (مثل زیرتیتر هیروی صفحهٔ اصلی، متن معرفی در
about-page، متن‌های fleet-features و why-us) خط‌بندی نامتعادل یا عجیب دارند
یا نه — مخصوصاً چند ثانیهٔ اول قبل از اینکه فونت کامل لود شود.

**چطور اسکرین‌شات بگیرید:**
1. یک تب Incognito/ناشناس جدید باز کنید (تا کش قبلی فونت دخالت نکند).
2. سایت را باز کنید و **بلافاصله** (در همان لحظهٔ لود، قبل از اینکه فونت
   کامل جا بیفتد) یک اسکرین‌شات بگیرید.
3. چند ثانیه صبر کنید تا فونت کامل لود شود، دوباره از همان قسمت اسکرین‌شات
   بگیرید.
4. این دو اسکرین‌شات را با هم مقایسه کنید — اگر چیدمان خط‌ها فرق دارد (مثلاً
   یک کلمه از سطر دوم به سطر اول پرید یا برعکس) و بعد از لود فونت **خودش
   را اصلاح نمی‌کند**، همان باگ قبلی است.
5. این کار را هم روی موبایل (یا شبیه‌ساز موبایل در DevTools) و هم دسکتاپ،
   و ترجیحاً هم روی کروم هم فایرفاکس انجام دهید.

هر کدام از این دو اسکرین‌شات (قبل/بعد از لود فونت) را برایم بفرستید، یا فقط
نتیجه را بگویید («فرقی نداشت» یا «فلان‌جا خط‌بندی عجیب بود») — بر اساس آن
تصمیم می‌گیرم نگه‌دارم یا با `break-words` (مثل کاری که قبلاً برای
`text-balance` انجام شد) جایگزین کنم.
