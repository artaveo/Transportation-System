# پرامپت مادر پروژه — سیستم رزرو آنلاین ترانسپورت برون‌شهری

> این سند برای استفاده به‌عنوان زمینه اولیه (Seed Context) یک پروژه جدید نوشته شده است. تمام تصمیمات کلیدی که در جلسه طرح‌ریزی گرفته شده در اینجا ثبت شده تا هر چت جدید در همین پروژه، بدون نیاز به توضیح مجدد، دقیقاً بداند چه ساخته می‌شود و چرا.
>
> **آخرین به‌روزرسانی:** ۴ سپتامبر ۲۰۲۶ — پس از تکمیل فاز ۱ (طراحی ظاهر) و فاز ۲ (فریم‌ورک و معماری فنی)، تصمیم‌گیری دربارهٔ باشگاه مشتریان، افزودن الزامات ریسپانسیو حرفه‌ای/عکس‌های تطبیقی چند دستگاهی (۴.۶ و ۵.۵ — بخش ۸)، و افزودن دو فاز مستقل جدید در نقشهٔ راه: **فاز ۹ — تست و تضمین کیفیت تولید (QA & Security)** و **فاز ۱۰ — نظارت و مانیتورینگ (Observability)** پیش از استقرار نهایی (که به فاز ۱۱ منتقل شد — بخش‌های ۹ و ۱۰ همین سند). بخش‌های ۴، ۵، ۸، ۹ و ۱۰ نسبت به نسخهٔ قبلی جدید هستند؛ بقیهٔ سند فقط تکمیل شده، نه بازنویسی.

---

## ۱. معرفی پروژه

هدف، ساخت یک وبسایت تمام‌عیار رزرو آنلاین بلیت برای یک شرکت مشخص ترانسپورت مسافربری برون‌شهری در افغانستان است (نمونه‌های بازار: احمدشاه بابا، ابدالی، میرویس نیکه).

**نکته اصلاحی مهم درباره ناوگان:** برخلاف فرض اولیه، این شرکت‌ها از سدان‌های کوچک استفاده نمی‌کنند؛ ناوگان اصلی آن‌ها **بس‌های بزرگ بین‌شهری با ظرفیت تقریبی ۴۰ تا ۶۰ مسافر** است، دقیقاً در همان مقیاسی که پلتفرم‌های جهانی مانند FlixBus با آن کار می‌کنند. این موضوع مستقیماً روی طراحی نقشه صندلی، ظرفیت‌سنجی، و ساختار دیتابیس اثر می‌گذارد و باید در تمام فازهای بعدی رعایت شود.

پروژه برای فروش به یک مشتری واحد (همان شرکت) طراحی می‌شود، اما معماری باید از ابتدا اجازه توسعه بعدی به یک پلتفرم چند‌شرکتی را بدون بازنویسی بنیادین بدهد.

---

## ۲. تصمیمات نهایی‌شده (خلاصه جلسه طرح‌ریزی)

| موضوع | تصمیم |
|---|---|
| نوع ناوگان | بس بزرگ بین‌شهری، ظرفیت ۴۰-۶۰ نفر (نه سدان) |
| زمان‌بندی سفرها | ساعت حرکت مشخص و از پیش تعریف‌شده برای اکثر سفرها؛ احتمال وجود چند مسیر با حرکت بر اساس پرشدن ظرفیت نیز باید در طراحی دیتابیس پشتیبانی شود (نه الزامی برای هر مسیر) |
| تخصیص پلاک/راننده به هر سفر | فعلاً به‌عنوان قابلیت اصلی در نظر گرفته می‌شود؛ در صورتی‌که در عمل شرکت اطلاعات دقیق را از قبل نداشته باشد، فیلدهای مربوطه باید اختیاری (Nullable) طراحی شوند، نه اجباری |
| حساب کاربری مسافر | بدون الزام؛ رزرو با شماره تماس + کد رزرو (Guest Checkout مطابق استاندارد جهانی)؛ در صورت ثبت‌نام، مسافر وارد باشگاه مشتریان می‌شود — جزئیات کامل در بخش ۵ |
| پرداخت آنلاین | HesabPay به‌عنوان گزینه اصلی. دسترسی API نیازمند درخواست رسمی از HesabPay است که زمان‌بر خواهد بود؛ درخواست API باید از همان ابتدا و به‌صورت موازی با توسعه آغاز شود، اما اتصال فنی واقعی در آخرین فاز پروژه انجام می‌شود |
| پرداخت آفلاین | حتماً پشتیبانی شود (نقد در دفتر یا هنگام سوار شدن)؛ رزرو با وضعیت «در انتظار تأیید پرداخت» ثبت و توسط اپراتور در پنل ادمین تأیید می‌شود |
| اعلان پیامکی (SMS) | از نظر فنی در افغانستان کاملاً ممکن است؛ چند سرویس‌دهنده بین‌المللی (مانند D7 Networks، EasySendSMS، BudgetSMS) به تمام اپراتورهای داخلی (روشن، AWCC، اتصالات، سلام) متصل می‌شوند و تحت مقررات ATRA فعالیت می‌کنند. تصمیم نهایی درباره انتخاب سرویس‌دهنده و هزینه آن با مشتری (شرکت) است؛ فعلاً فقط باید زیرساخت نرم‌افزاری (یک لایه انتزاعی قابل تعویض) برای آن آماده شود |
| زبان | دو‌زبانه دری/انگلیسی از همان معماری پایه، نه افزونه بعدی؛ پشتیبانی هم‌زمان RTL/LTR |
| هاست و دامنه | تأیید شد: در مرحله توسعه از دامنه پیش‌فرض Vercel و پلن رایگان Supabase استفاده می‌شود. خرید دامنه اختصاصی و ارتقای هاست/دیتابیس (نه سرور کاملاً مستقل — این موضوع صریحاً در فاز ۲.۲ روشن شد)، پس از تأیید نهایی مشتری (شرکت) در فاز استقرار انجام خواهد شد. تمام مقادیر مرتبط با دامنه/URL باید از همان ابتدا در متغیرهای محیطی (Environment Variables) نگهداری شوند تا جابه‌جایی بعدی بدون تغییر کد ممکن باشد |
| طراحی فرانت‌اند | ظاهر با استفاده از v0.dev ساخته می‌شود. پرامپت کامل v0.dev در فاز ۱.۳ نوشته شد؛ فاز ۱.۴ (بازبینی/اصلاح خروجی) و ۱.۵ (نهایی‌سازی رنگ/برندینگ/فونت) هر دو تکمیل شده‌اند — جزئیات در بخش ۴.۱ |

---

## ۳. نقشه سایت و ساختار کامپوننت‌ها (فقط ساختار، بدون کد)

مقایسه با استانداردهای پلتفرم‌های جهانی مسافربری (FlixBus، Busbud، Omio) برای تعیین صفحات و کامپوننت‌های ضروری انجام شد. نتیجه به شرح زیر است:

### ۳.۱ صفحات سمت مسافر (Public)

1. **صفحه اصلی** — جستجوی سریع مسیر، معرفی خدمات ویژه، بنر تبلیغاتی شرکت
2. **صفحه نتایج جستجو** — لیست سفرهای موجود با فیلتر (ساعت، قیمت، ظرفیت خالی)
3. **صفحه انتخاب صندلی و رزرو** — نقشه صندلی بس + فرم اطلاعات مسافر + خلاصه سفارش
4. **صفحه پرداخت** — انتخاب بین پرداخت آنلاین (HesabPay) یا آفلاین (نقدی)
5. **صفحه تأییدیه رزرو** — نمایش/دانلود بلیت دیجیتال با کد QR
6. **صفحه پیگیری رزرو** — جستجو با کد رزرو یا شماره تماس (بدون نیاز به حساب کاربری)
7. **صفحه مسیرها و قیمت‌ها** — فهرست ثابت برای کاربرانی که فقط قیمت می‌پرسند
8. **صفحه درباره ما** — معرفی شرکت، ناوگان، امکانات بس‌ها
9. **صفحه تماس با ما** — دفاتر فروش در شهرهای مختلف
10. **صفحه سوالات متداول** — به‌ویژه سیاست کنسلی و بازپرداخت (استاندارد جهانی الزامی)
11. **صفحه حساب کاربری / باشگاه مشتریان** *(جدید — نتیجهٔ تکمیل فاز ۲)* — سطح عضویت، کیف‌پول، کد معرفی، تاریخچهٔ رزروها؛ فقط برای کاربران ثبت‌نام‌کرده

### ۳.۲ کامپوننت‌های مشترک سایت عمومی

- `Header` — لوگو، منو، سوییچ زبان دری/انگلیسی
- `Footer` — اطلاعات تماس، شبکه‌های اجتماعی، لینک‌های سریع
- `TripCard` — کارت نمایش هر سفر (ساعت حرکت/رسیدن، مدت، قیمت، ظرفیت باقیمانده)
- `SeatMap` — نقشه صندلی بس بزرگ (چیدمان چندردیفی با راهرو، وضعیت هر صندلی: خالی/رزروشده/انتخاب‌شده)
- `PassengerForm` — فرم اطلاعات مسافر برای هر صندلی انتخاب‌شده
- `OrderSummary` — خلاصه قیمت و اطلاعات سفر پیش از پرداخت
- `PaymentMethodSelector` — انتخاب روش پرداخت
- `DigitalTicket` — نمایش بلیت نهایی با کد QR
- `BookingLookup` — فرم جستجوی رزرو با کد/شماره تماس
- `TierBadge` *(جدید)* — نشان سطح عضویت (برنز/نقره/طلا)
- `WalletCard` *(جدید)* — نمایش موجودی کیف‌پول/کش‌بک
- `ReferralCard` *(جدید)* — کد معرفی و وضعیت پاداش
- `BookingHistoryList` *(جدید)* — تاریخچهٔ رزروهای حساب کاربری

### ۳.۳ پنل مدیریت (Admin)

مطابق الگوی دوسطحی که در پروژه قبلی پیاده‌سازی شد (ادمین کل + ادمین محدود دپارتمانی):

- `RouteManager` — مدیریت مسیرها
- `BusManager` — مدیریت بس‌ها و اطلاعات فنی/ظرفیت
- `DriverManager` — مدیریت رانندگان
- `TripScheduler` — تعریف سفر جدید (اتصال مسیر + بس + راننده + تاریخ/ساعت)، با فیلدهای بس/راننده اختیاری در صورت نیاز
- `BookingsTable` — جدول رزروها با فیلتر وضعیت پرداخت (پرداخت‌شده/در انتظار/کنسل‌شده)
- `PaymentConfirmationAction` — تأیید دستی پرداخت‌های آفلاین
- `ReportsDashboard` — گزارش درآمد، تعداد مسافر، نرخ اشغال صندلی به تفکیک مسیر/تاریخ
- `AdminRoleManager` — مدیریت سطح دسترسی ادمین‌ها
- `LoyaltyManager` *(جدید)* — تنظیم آستانه/درصد سطوح باشگاه مشتریان + صدور کد تخفیف/کوپن

---

## ۴. معماری فنی (نتیجهٔ فاز ۲)

### ۴.۱ فریم‌ورک فرانت‌اند — Next.js App Router (فاز ۲.۱ ✅)

خروجی v0.dev که در فازهای ۱.۳–۱.۵ ساخته و اصلاح شد، از پایه Next.js App Router است، نه یک SPA خام React. شواهد فنی تأییدشده از کد واقعی پروژه:

1. `package.json` → وابستگی اصلی `next: 16.3.3`، اسکریپت‌های `next dev/build/start`.
2. `tsconfig.json` → پلاگین رسمی `next` فعال است.
3. `components.json` (پیکربندی shadcn) → `"rsc": true`، سبک `base-nova`.
4. `app/layout.tsx` → فونت‌های Vazirmatn (سنس) و Fraunces (سریف) از طریق `next/font/google`، `@vercel/analytics/next`، و تنظیم `dir="rtl" lang="fa-AF"` روی تگ `<html>`.
5. `app/globals.css` → پالت رنگ اختصاصی «Night-road» (پس‌زمینهٔ تیرهٔ آبی‌سرمه‌ای `#16213e`، رنگ اصلی کهربایی `#e8a33d`) — نتیجهٔ نهایی‌شدهٔ فاز ۱.۵.
6. ۸ کامپوننت (`site-header`, `site-footer`, `hero-search`, `popular-routes`, `search-results`, `seat-selection`, `checkout-form`, `booking-confirmation`) مستقیماً از `next/link` و `next/navigation` استفاده می‌کنند.

**نتیجه:** Next.js تأیید و نهایی است. برتری آن برای این پروژه: SEO صفحات عمومی (مسیرها/قیمت‌ها، درباره ما، سوالات متداول)، مسیرهای پویای رزرو، و امکان لایهٔ سرور برای کلیدهای حساس بدون بک‌اند جداگانه.

> یادداشت فنی برای فاز ۴: تقریباً تمام کامپوننت‌های تعاملی فعلاً `"use client"` هستند (رفتار پیش‌فرض v0.dev)؛ فقط `page.tsx` و `layout.tsx` سرور-کامپوننت‌اند. توصیه می‌شود کامپوننت‌های لیستی (نتایج جستجو، مسیرهای محبوب) هنگام اتصال داده‌های واقعی Supabase به Server Component تبدیل شوند تا مزیت SEO واقعی به‌دست آید.

### ۴.۲ لایهٔ بک‌اند و داده — Supabase + Route Handlers (فاز ۲.۲ ✅)

**تصمیم: نیازی به بک‌اند اختصاصی Node.js/Express نیست.**

معماری سرور به‌صورت ترکیبی:

1. **Supabase (Postgres + RLS + Auth)** — منبع اصلی داده و احراز هویت (فاز ۳).
2. **Next.js Route Handlers / Server Actions** (`app/api/*`) — لایهٔ منطق حساس، هم‌محل با فرانت‌اند، بدون هاست جدا:
   - رزرو اتمیک صندلی (جلوگیری از رزرو هم‌زمان)
   - شروع/تأیید پرداخت HesabPay (کلید API هرگز در مرورگر افشا نمی‌شود)
   - فراخوانی لایهٔ انتزاعی SMS
   - محاسبه/اعمال تخفیف باشگاه مشتریان
3. **Supabase Edge Functions** — فقط برای موارد مستقل از دیپلوی فرانت‌اند: Cron یادآوری سفر (فاز ۷.۳)، تریگرهای پایگاه‌داده، و در صورت نیاز webhook مستقل پرداخت.

**دلیل رد Node/Express جدا:** هاست/دیپلوی/مانیتورینگ جداگانه برای یک مشتری واحد با ترافیک محدود سربار عملیاتی غیرضروری ایجاد می‌کند و با تصمیم فاز ۲.۱ هم‌افزایی ندارد.

**روشن‌سازی مهم دربارهٔ هاست آینده:** «سرور اختصاصی» در تحویل نهایی به مشتری، به‌معنای **ارتقای پلن رایگان به پولی + دامنهٔ اختصاصی** است (دقیقاً طبق فاز ۹.۱/۹.۲)، نه یک VPS کاملاً مستقل. چون تمام مقادیر دامنه/کلید در متغیرهای محیطی نگهداری می‌شوند، این ارتقا **بدون تغییر کد** انجام می‌شود.

### ۴.۳ ساختار پوشه‌بندی و نقشهٔ مسیرها (فاز ۲.۳ ✅)

**ساختار فعلی تأییدشده (استخراج از importهای واقعی کد):**

```
app/
  page.tsx              → صفحهٔ اصلی (رندر TransportHome) — تنها route واقعاً wired
  layout.tsx
  globals.css
components/
  transport/            ← ۲۲ کامپوننت با import نسبی به هم متصل (site-header, site-footer,
                            hero-search, date-picker, popular-routes, destinations-grid, why-us,
                            fleet-features, trust-stats, placeholder-badge, transport-home,
                            search-results, seat-selection, checkout-form, booking-confirmation,
                            booking-lookup, routes-index, about-page, contact-page, faq-page,
                            legal-page, luggage-policy, admin-panel)
  ui/
    button.tsx           ← shadcn (style: base-nova)
lib/
  i18n.ts, lang-context.tsx, utils.ts, booking-data.ts, admin-data.ts, date-utils.ts
```

**نقشهٔ مسیرها — استخراج‌شده مستقیماً از `router.push`/`href` در کد واقعی (نه فرضی):**

| کامپوننت | مسیر | منبع |
|---|---|---|
| `TransportHome` | `/` | ✅ در حال حاضر wired |
| `SearchResults` | `/search?from=&to=&date=` | استخراج از کد |
| `SeatSelection` | `/trips/[tripId]/seats?date=` | استخراج از کد |
| `CheckoutForm` | `/trips/[tripId]/checkout?seats=&date=` | استخراج از کد |
| `BookingConfirmation` | `/trips/[tripId]/confirmation` | استخراج از کد |

**مسیرهای پیشنهادی (هنوز در کد فراخوانی نشده‌اند):**

| کامپوننت | مسیر پیشنهادی |
|---|---|
| `BookingLookup` | `/my-booking` |
| `RoutesIndex` | `/routes` |
| `AboutPage` | `/about` |
| `ContactPage` | `/contact` |
| `FaqPage` | `/faq` |
| `LegalPage` | `/legal` |
| `LuggagePolicy` | `/luggage-policy` |
| `AdminPanel` | `/admin` (گروه محافظت‌شده) |
| حساب کاربری/باشگاه مشتریان *(جدید)* | `/account` |

**افزودنی‌های لازم برای فاز ۳ به بعد:**

```
app/
  api/                              ← جدید — لایهٔ سرور
    bookings/route.ts
    payments/hesabpay/{initiate,webhook}/route.ts
    sms/send/route.ts
    loyalty/{redeem-coupon,wallet}/route.ts
  account/page.tsx                  ← جدید — داشبورد باشگاه مشتریان
  admin/loyalty/page.tsx            ← جدید — تنظیم سطوح/کوپن‌ها

components/
  admin/                            ← جدید — admin-panel.tsx در فاز ۵ به این‌ها تفکیک می‌شود
    route-manager.tsx, bus-manager.tsx, driver-manager.tsx, trip-scheduler.tsx,
    bookings-table.tsx, payment-confirmation-action.tsx, reports-dashboard.tsx,
    admin-role-manager.tsx, loyalty-manager.tsx
  account/                          ← جدید
    tier-badge.tsx, wallet-card.tsx, referral-card.tsx, booking-history-list.tsx

lib/
  supabase/{client,server}.ts       ← کلاینت مرورگر / سرور
  sms/provider.ts + providers/*.ts  ← لایهٔ انتزاعی SMS (فاز ۷.۲)
  payments/hesabpay.ts
  loyalty/{tiers,wallet}.ts
```

**قوانین کلی نام‌گذاری برای فازهای بعدی:**

1. هر «کامپوننت-صفحه» موجود در `components/transport/` عیناً می‌ماند؛ فقط یک فایل نازک `app/<route>/page.tsx` آن را رندر می‌کند.
2. هیچ منطق سروری داخل کامپوننت‌های `"use client"` قرار نمی‌گیرد؛ کلیدهای حساس فقط در `app/api/*` یا ماژول‌های `lib/*` بدون `"use client"`.
3. جدول‌های Supabase در فاز ۳.۱ باید شامل این‌ها هم باشند: `customers`، `loyalty_tiers`، `wallet_transactions`، `referrals`، `coupons`.

---

## ۵. باشگاه مشتریان (وفاداری) — تصمیم تکمیلی

حساب کاربری مسافر همچنان کاملاً اختیاری است (Guest Checkout پابرجا). در صورت ثبت‌نام، مسافر وارد سیستم سطح‌بندی سه‌گانه می‌شود:

| سطح | آستانهٔ ورود | مزایا |
|---|---|---|
| **برنز** | از لحظهٔ ثبت‌نام | ۵٪ تخفیف دائمی (هدیهٔ خوش‌آمدگویی) |
| **نقره** | پس از ۵ سفر تکمیل‌شدهٔ تجمعی | ۱۰٪ تخفیف دائمی + لغو/تغییر بلیت با مهلت کوتاه‌تر از حالت عادی |
| **طلا** | پس از ۱۵ سفر تکمیل‌شدهٔ تجمعی | ۱۵-۲۰٪ تخفیف دائمی + دسترسی زودهنگام به سفرهای پرتقاضا + اولویت پشتیبانی |

**نکات طراحی:**

1. شمارش سفر به‌صورت **تجمعی مادام‌العمر** است (نه سالانهٔ چرخشی) — ساده‌تر برای نسخهٔ اول، بدون نیاز به cron محاسباتی.
2. آستانه‌ها و درصدهای بالا **باید از پنل ادمین (`LoyaltyManager`) قابل‌تغییر باشند**، نه hardcode در کد؛ مشتری باید بتواند بعداً خودش این اعداد را تنظیم کند.

**سه قابلیت تکمیلی تأییدشده (قابل جمع‌شدن با تخفیف سطح):**

1. **کیف‌پول/کش‌بک** — اعتبار قابل‌استفاده در رزروهای بعدی، جدا از درصد تخفیف سطح عضویت.
2. **کد معرفی دوستان (Referral)** — اعتبار برای هر دو طرف (دعوت‌کننده و دعوت‌شونده)، وارد همان کیف‌پول می‌شود.
3. **کد تخفیف/کوپن از پنل ادمین** — سیستم مستقل برای کمپین‌های فصلی؛ قابلیت جمع‌شدن یا نشدن با تخفیف سطح در فاز ۴.۲ (فرم رزرو) دقیق مشخص می‌شود.

این تصمیم مبتنی بر بررسی الگوهای رایج در پلتفرم‌های مشابه (نظیر برنامهٔ سطح‌بندی‌شدهٔ Go Rewards در Busbud و سیستم کش‌بک AbhiCash در AbhiBus) است و مستقیماً روی طراحی جداول فاز ۳.۱ اثر می‌گذارد (جداول جدید: `customers`، `loyalty_tiers`، `wallet_transactions`، `referrals`، `coupons`).

---

## ۶. فازبندی درختی پروژه

```
فاز ۱ — طراحی ظاهر (UI/UX)                                          ✅ تکمیل
├── ۱.۱ نقشه سایت و فهرست صفحات        ✅ انجام‌شده (بخش ۳ همین سند)
├── ۱.۲ فهرست کامپوننت‌ها به تفکیک صفحه ✅ انجام‌شده (بخش ۳ همین سند)
├── ۱.۳ نگارش پرامپت کامل برای v0.dev   ✅ انجام‌شده (سند claude/v0dev-frontend-prompt-en.md)
├── ۱.۴ بازبینی و اصلاح خروجی v0.dev    ✅ تکمیل (بر اساس بررسی کد واقعی — خروجی به کامپوننت‌های
│                                          یکپارچه با lib/i18n و lib/booking-data تبدیل شده)
└── ۱.۵ نهایی‌سازی رنگ/برندینگ/فونت     ✅ تکمیل (app/layout.tsx و app/globals.css —
    (سازگار با RTL دری)                    پالت «Night-road» + فونت‌های Vazirmatn/Fraunces)

فاز ۲ — انتخاب فریم‌ورک و معماری فنی                                 ✅ تکمیل
├── ۲.۱ مقایسه React+Vite در برابر Next.js  ✅ Next.js App Router تأیید شد (بخش ۴.۱)
├── ۲.۲ بک‌اند اختصاصی یا Supabase+Edge     ✅ Supabase + Route Handlers، بدون Express (بخش ۴.۲)
└── ۲.۳ ساختار پوشه‌بندی پروژه              ✅ نقشهٔ کامل مسیرها و پوشه‌ها مستند شد (بخش ۴.۳)

فاز ۳ — پایه‌های Supabase                                            ✅ تکمیل
├── ۳.۱ طراحی جداول (مسیر، بس، راننده، سفر، رزرو، مسافر، وضعیت پرداخت،
│        customers، loyalty_tiers، wallet_transactions، referrals، coupons)   ✅ تکمیل — ۱۶ جدول روی پروژهٔ زندهٔ Supabase (Transportation-System) ساخته شد
├── ۳.۲ سیاست‌های RLS و نقش‌های ادمین                                          ✅ تکمیل — RLS روی هر ۱۶ جدول فعال شد
└── ۳.۳ احراز هویت پنل ادمین (Supabase Auth)                                   ✅ تکمیل — middleware، لاگین ادمین، و اولین حساب super_admin وصل شد

فاز ۴ — توسعه سایت عمومی
├── ۴.۱ جستجوی مسیر و نمایش نتایج                                              ✅ تکمیل — اتصال کامل به Supabase (cities/routes/trips واقعی)؛ ستون‌های افزایشی buses.amenities و cities.display_order اضافه شد
├── ۴.۲ نقشه صندلی بس بزرگ و فرم رزرو                                          ✅ تکمیل — قفل اتمیک صندلی (hold_seats/confirm_booking سمت service_role)، ثبت رزرو واقعی، ستون‌های افزایشی booking_passengers.national_id/gender
├── ۴.۳ صفحه پیگیری رزرو                                                       ✅ تکمیل — اتصال کامل به Supabase (تطابق کد رهگیری+شماره تماس)، ویرایش شمارهٔ تماس و درخواست کنسلی واقعی؛ در همین فاز یک نقص امنیتی واقعی در توابع فاز ۴.۲ کشف و اصلاح شد (EXECUTE عمومی روی hold_seats/confirm_booking)
├── ۴.۴ صفحات معرفی/تماس/مسیرها/سوالات متداول                                  ✅ تکمیل — چهار مسیر واقعی وصل شد؛ صفحهٔ مسیرها به دادهٔ واقعی routes/trips وصل شد (به‌جای فهرست کاملاً ساختگی نسخهٔ mock)
├── ۴.۵ حساب کاربری مسافر و باشگاه مشتریان (تاریخچه، سطح، کیف‌پول، رفرال)       ✅ تکمیل — ثبت‌نام/ورود واقعی مسافر، تخفیف سطح در confirm_booking، تریگر ارتقای سطح/پاداش رفرال؛ استفاده از کیف‌پول در چک‌اوت و LoyaltyManager هنوز باقی مانده (فاز ۵.۴)
└── ۴.۶ ریسپانسیو حرفه‌ای سایت پابلیک + عکس‌های تطبیقی چند دستگاهی           ✅ تکمیل — بریک‌پوینت‌های ۳xl/۴xl (۱۶۰۰/۲۵۶۰px) به globals.css اضافه شد؛ کامپوننت عمومی `ResponsivePhoto` ساخته شد (قابل‌استفاده در کل سایت، نه فقط این سه عکس)؛ هیرو/ناوگان/درباره‌ما با کراپ‌های چندگانه وایر شدند؛ کانتینر تمام صفحات پابلیک اصلی (صفحه اصلی، نتایج جستجو، انتخاب صندلی، پرداخت، مسیرها، تماس، حساب کاربری) برای مانیتور عریض/فوق‌عریض اصلاح شد؛ فرم جستجوی هیرو یک چیدمان میانی برای تبلت گرفت؛ **علاوه بر این، یک ممیزی مستقل ریسپانسیو/RTL (۲۵ یافته) در ادامهٔ همین فاز اجرا شد — رفع فلش RTL سرور-رندر، بازنویسی تقویم با floating-ui، نوار CTA ثابت موبایل در انتخاب صندلی/پرداخت، جمع‌کردن فاصله‌گذاری هیروی موبایل، اصلاح گرادیان‌های RTL، تاچ‌تارگت‌های زیر ۴۴px، و چند رفع جزئی/متوسط دیگر — جزئیات کامل در بخش ۱۲.۴**
└── ۴.۷ تکمیل فلوی احراز هویت مسافر                                            ✅ تکمیل (بخش کد) — جزئیات کامل در بخش ۱۲.۱۱؛ تنظیم SMTP اختصاصی عمداً به‌تعویق افتاد (تصمیم Zakir، هزینهٔ Resend)

فاز ۵ — پنل مدیریت
├── ۵.۱ مدیریت مسیر/بس/راننده/سفر                                              ✅ تکمیل — چهار مدیر CRUD واقعی (RouteManager/BusManager/DriverManager/TripScheduler) روی Supabase واقعی؛ جزئیات کامل در بخش ۱۲.۱۳
├── ۵.۲ مدیریت رزرو و تأیید پرداخت آفلاین
├── ۵.۳ گزارش‌گیری آماری
├── ۵.۴ مدیریت باشگاه مشتریان (تنظیم آستانه/درصد سطوح، صدور کوپن) — جدید
└── ۵.۵ ریسپانسیو حرفه‌ای پنل ادمین — جدید (جزئیات بخش ۸)

فاز ۶ — پرداخت آنلاین (HesabPay)
├── ۶.۱ آماده‌سازی زیرساخت دیتابیس و وضعیت‌های پرداخت
├── ۶.۲ پیگیری درخواست API از HesabPay (اقدام موازی، شروع زودهنگام)
└── ۶.۳ اتصال نهایی درگاه پرداخت (آخرین گام فنی پروژه)

فاز ۷ — اعلان پیامکی (SMS)
├── ۷.۱ بررسی و انتخاب سرویس‌دهنده (تصمیم نهایی با مشتری)
├── ۷.۲ طراحی لایه انتزاعی سرویس پیامک (قابل تعویض بین ارائه‌دهنده‌ها)
└── ۷.۳ اتصال به رویدادهای تأیید رزرو/یادآوری سفر

فاز ۸ — دو‌زبانگی کامل (دری/انگلیسی)
├── ۸.۱ ساختار i18n و فایل‌های ترجمه
└── ۸.۲ تست کامل RTL/LTR در تمام کامپوننت‌ها

فاز ۹ — تست و تضمین کیفیت تولید (QA & Security) — جدید (جزئیات بخش ۹)
├── ۹.۱ Unit Testing (منطق محاسباتی: تخفیف سطح/کوپن، ظرفیت صندلی، قیمت‌گذاری)
├── ۹.۲ Integration Testing (تعامل واقعی Route Handler ↔ Supabase ↔ RLS)
├── ۹.۳ End-to-End Testing (کل مسیر کاربر: جستجو تا صدور بلیت، هم در دری هم انگلیسی)
├── ۹.۴ Concurrency Testing (رزرو هم‌زمان صندلی مشترک، race condition روی hold_seats/confirm_booking)
├── ۹.۵ Authorization Testing (سطوح دسترسی ادمین کل/دپارتمانی، RLS هر ۱۶ جدول، دسترسی مهمان به رزرو دیگران)
├── ۹.۶ Payment Testing (مسیرهای موفق/ناموفق/معلق HesabPay، پرداخت آفلاین، بازپرداخت، حالت‌های Webhook تکراری)
├── ۹.۷ Failure Recovery Testing (قطع اتصال میان‌راه، تایم‌اوت، رفتار سیستم هنگام خطای Supabase/HesabPay/SMS)
├── ۹.۸ Accessibility Testing (کیبورد، screen reader، کنتراست رنگ، هر دو جهت RTL/LTR)
├── ۹.۹ Performance Testing (زمان پاسخ صفحات پرترافیک، بار همزمان چند کاربر روی جستجو/رزرو)
└── ۹.۱۰ Security Hardening (Rate Limiting، ورودی‌های مخرب، تزریق SQL/XSS، بازبینی نهایی RLS و کلیدهای API)

فاز ۱۰ — نظارت و مانیتورینگ (Observability) — جدید (جزئیات بخش ۱۰)
├── ۱۰.۱ لاگ‌گذاری ساختاریافته و ردیابی خطا (Error Tracking)
├── ۱۰.۲ متریک‌های عملیاتی کلیدی (نرخ خطای رزرو/hold/پرداخت/پیگیری رزرو)
├── ۱۰.۳ مانیتورینگ کارایی API و شناسایی Bottleneck دیتابیس
└── ۱۰.۴ داشبورد و هشدار (Alerting) برای اپراتور/ادمین کل

فاز ۱۱ — استقرار نهایی (شمارهٔ قبلی: فاز ۹)
├── ۱۱.۱ خرید و اتصال دامنه اختصاصی
├── ۱۱.۲ ارتقای پلن Supabase (در صورت نیاز)
├── ۱۱.۳ پشتیبان‌گیری و بازیابی (Backup/Recovery) — جدید
├── ۱۱.۴ مستندسازی نهایی پروژه برای تحویل به مشتری — جدید
└── ۱۱.۵ تست نهایی end-to-end در محیط Production
```

هر یک از این زیرفازها قرار است در یک چت جداگانه درون همین پروژه انجام شود، بدون نیاز به تکرار زمینه؛ همین سند به‌عنوان مرجع مشترک تمام آن چت‌ها عمل می‌کند.

---

## ۷. اصول ثابت پروژه (از پروژه قبلی به ارث رسیده و باید رعایت شود)

- **قانون عدم جعل اطلاعات:** هیچ عدد، نام فیلد، یا رفتاری نباید بدون بررسی فایل واقعی فرض یا اختراع شود
- **تغییرات افزایشی:** هر تغییر ساختاری باید کاملاً افزایشی (Additive) باشد و عملکرد موجود را نشکند
- **انضباط نسخه فایل:** پیش از هر ویرایش، نسخه فعلی فایل باید تأیید شود
- **فهرست‌های شماره‌دار برای یافته‌ها:** ترجیح بر فهرست عددی نسبت به توضیح روایی
- **تحویل نهایی:** فایل zip شامل فقط پوشه `src/` با فایل‌های تغییریافته در مسیر صحیح
- **به‌روزرسانی پرامپت مادر در پایان هر فاز (از این به بعد):** همراه با تحویل فایل‌های تغییریافتهٔ هر زیرفاز، نسخهٔ به‌روزشدهٔ همین سند (`claudeproject-master-prompt.md`) هم باید ضمیمه شود — فقط با ✅ زدن همان زیرفاز در جدول بخش ۶ و یک توضیح کوتاه (یک خط) از کاری که واقعاً انجام شد یا تصمیم مهمی که گرفته شد؛ اگر توضیح کوتاهی لازم نبود، فقط ✅ کافی است. این کار بازنویسی یا تغییر بخش‌های دیگر سند را شامل نمی‌شود، مگر آن فاز صریحاً چیزی را در تصمیمات پایه تغییر داده باشد.

---

## ۸. الزامات ریسپانسیو حرفه‌ای و عکس‌های تطبیقی چند دستگاهی (تصمیم تکمیلی)

بررسی وضعیت فعلی نشان داد خروجی v0.dev فقط برای اندازهٔ لپ‌تاپ/دسکتاپ معمولی به‌درستی نمایش داده می‌شود؛ نه برای گوشی، نه برای تبلت، و نه برای مانیتورهای عریض (روی مانیتور عریض محتوا فقط وسط صفحه با فضای خالی سفید در دو طرف نمایش داده می‌شود، نه چیدمانی متناسب با عرض واقعی صفحه). این بخش الزامات دقیق رفع این مشکل را ثبت می‌کند تا در فازهای ۴.۶ و ۵.۵ (و هر فاز دیگری که تصویر/چیدمان جدید اضافه می‌کند) رعایت شود.

### ۸.۱ دامنهٔ دستگاه‌ها — نه فقط سه بریک‌پوینت ساده

ریسپانسیو بودن باید **واقعاً حرفه‌ای و کامل** باشد، نه فقط کوچک/بزرگ‌شدن نسبی. باید دقیقاً برای هر ردهٔ زیر چیدمان جداگانه بازبینی و در صورت نیاز بازطراحی شود:

1. گوشی‌های کوچک (حدود ۳۲۰–۳۷۵px)
2. گوشی‌های استاندارد/بزرگ (حدود ۳۹۰–۴۳۰px)
3. تبلت در حالت عمودی (حدود ۷۶۸px)
4. تبلت در حالت افقی / لپ‌تاپ کوچک (حدود ۱۰۲۴px)
5. لپ‌تاپ/دسکتاپ معمولی (حدود ۱۲۸۰–۱۴۴۰px) — همان چیزی که الان طراحی شده
6. مانیتور بزرگ/عریض (حدود ۱۶۰۰–۱۹۲۰px)
7. مانیتور فوق‌عریض یا تلویزیون هوشمند (۲۵۶۰px به بالا)

در هر ردهٔ بالا، صرفاً بزرگ/کوچک کردن نسبی (scale) کافی نیست؛ چیدمان باید در هر بریک‌پوینت **بازاندیشی** شود (دقیقاً همان اصلی که در پرامپت اصلی v0.dev — بخش Responsiveness — قبلاً به‌عنوان الزام نوشته شده بود، اما در عمل کامل اجرا نشده). به‌طور مشخص برای مانیتورهای عریض: محتوا نباید فقط با عرض ثابت لپ‌تاپ وسط صفحه بنشیند و اطراف آن سفید/خالی بماند؛ باید یا عرض محتوا به‌صورت متناسب افزایش یابد یا از فضای اضافی برای عناصر مکمل (مثلاً ستون‌های بیشتر در گرید کارت‌ها) استفاده شود.

### ۸.۲ عکس‌های تطبیقی (Responsive Images) — طراحی یکسان، فقط اندازهٔ متفاوت

هر عکسی که در سایت استفاده می‌شود یا در آینده اضافه خواهد شد (هیرو، بخش ناوگان، و هر بخش دیگری که بعداً عکس می‌گیرد)، باید **چند نسخه** داشته باشد — یکی برای هرکدام از ردیف‌های دستگاه در بخش ۸.۱ (حداقل: موبایل، تبلت، لپ‌تاپ/دسکتاپ، مانیتور عریض) — طوری که مرورگر بسته به اندازهٔ دستگاه کاربر، نسخهٔ مخصوص همان دستگاه را بارگذاری کند (نه یک عکس ثابت که فقط کوچک/بزرگ نمایش داده شود).

**نکتهٔ حیاتی دربارهٔ طراحی این نسخه‌ها:** نسخه‌های مختلف یک عکس باید **از نظر ظاهری کاملاً یکسان** باشند — همان ترکیب‌بندی، همان رنگ، همان سوژه — و فقط **نسبت ابعاد/اندازه** متناسب با آن دستگاه تغییر کند. هدف اصلاً بازطراحی تصویر نیست، فقط تطبیق اندازه/کراپ است.

**رویهٔ اجرا (باید حتماً رعایت شود):** در حال حاضر فقط دو عکس داریم (هیرو و بخش ناوگان) که هر دو با پرامپت v0.dev برای اندازهٔ لپ‌تاپ ساخته شده‌اند. هر وقت نوبت ساخت نسخه‌های موبایل/تبلت/مانیتور عریض این دو عکس رسید، Claude **نباید** پرامپت تصویر را از صفر بنویسد یا حدس بزند؛ باید صریحاً از کاربر (Zakir) بخواهد که پرامپت اصلی همان دو عکس (که برای نسخهٔ لپ‌تاپ استفاده شده) را برایش ارسال کند، و سپس فقط همان پرامپت را **ویرایش کند تا فقط ابعاد/نسبت تصویر تغییر کند** — بدون هیچ تغییری در ظاهر، ترکیب‌بندی یا سبک تصویر. همین رویه برای هر عکس جدیدی که بعداً به سایت اضافه می‌شود هم باید تکرار شود: اول یک نسخهٔ مرجع (لپ‌تاپ) طراحی می‌شود، بعد پرامپت همان نسخه فقط برای تغییر اندازه در دستگاه‌های دیگر ویرایش می‌شود.

### ۸.۳ محدودهٔ فاز ۴.۶ در برابر فاز ۵.۵

- **فاز ۴.۶** کل سایت پابلیک (صفحه اصلی، نتایج جستجو، انتخاب صندلی، پرداخت، تأییدیه، پیگیری رزرو، صفحات معرفی/تماس/مسیرها/سوالات متداول، حساب کاربری) را یک‌جا پوشش می‌دهد — بعد از این‌که تمام این صفحات در فازهای ۴.۱ تا ۴.۵ کامل ساخته شدند، تا کار دوباره‌کاری نشود.
- **فاز ۵.۵** پنل ادمین را جداگانه پوشش می‌دهد. طبق اصل طراحی از پیش تأییدشدهٔ پنل ادمین (اولویت با کارایی و تراکم اطلاعات، نه جلوهٔ بصری)، تمرکز اینجا بیشتر روی خوانایی جداول/فرم‌ها در اندازه‌های مختلف است تا عکس‌های تطبیقی (پنل ادمین عملاً عکس تزئینی ندارد).

---

### ۸.۴ بهینه‌سازی خودکار حجم عکس‌ها (تصمیم تکمیلی — ۵ سپتامبر ۲۰۲۶)

**مشکل کشف‌شده:** `next.config.mjs` از همان commit اول پروژه (خروجی پیش‌فرض
v0.dev، چون محیط پیش‌نمایش خودِ v0 این قابلیت را پشتیبانی نمی‌کند) با
`images: { unoptimized: true }` تنظیم شده بود — یعنی کل پایپ‌لاین رسمی
بهینه‌سازی تصویر Next.js/Vercel از ابتدا خاموش بود و عکس‌های PNG اتمسفریک
(هیرو/ناوگان/درباره‌ما) با حجم خام ۱.۵ تا ۲ مگابایت مستقیم به کاربر
می‌رسیدند — دقیقاً چیزی که روی اینترنت ضعیف لود صفحه را به‌شدت کند می‌کرد.

**راه‌حل (ریشه‌ای، نه رفع موردی):**

1. `images.unoptimized` حذف شد؛ پایپ‌لاین بهینه‌سازی Next.js فعال است.
2. کامپوننت مشترک `ResponsivePhoto` (بخش ۸.۲) بازنویسی شد تا هر `srcSet`
   را با `getImageProps` از `next/image` بسازد — یعنی هر عکسی که از این
   کامپوننت رد شود (هیرو، ناوگان، درباره‌ما، و هر بخش دیگری که بعداً
   عکس بگیرد) خودکار resize به چند اندازه + تبدیل فرمت مدرن (WebP/AVIF
   بسته به مرورگر کاربر) می‌شود، با `quality: 82` (کمی بالاتر از پیش‌فرض
   ۷۵ خودِ Next، عمداً برای جلوگیری از banding روی گرادیان‌های آسمان
   غروب). کارت مسیرهای محبوب (`popular-routes.tsx`) هم به `next/image`
   منتقل شد (تنها `<img>` خام باقی‌مانده در کل سایت، خودِ عنصر داخلی
   `ResponsivePhoto` است که برای الگوی `<picture>` لازم است).
3. **اندازه‌گیری واقعی (نه فرضی):** عکس هیروی دسکتاپ از ۲٫۰۷ مگابایت PNG
   به ۱۱۶ کیلوبایت WebP در همان ابعاد پیکسلی (۱۶۷۲×۹۴۱) رسید —
   **~۹۴٪ کاهش حجم**، تست‌شده مستقیم روی `/_next/image` با `curl` در همین
   چت (نه ادعای مستند بدون تست).
4. **future-proofing برای پنل ادمین:** `images.remotePatterns` در
   `next.config.mjs` از هم‌اکنون به دامنهٔ عمومی Supabase Storage همین
   پروژه (`ppbdsrcdnmckosqhhamm.supabase.co`) اجازه داده — یعنی هر وقت
   فاز ۵.۵ به بعد پنل ادمین امکان آپلود/تعویض عکس گرفت و عکس‌ها را از
   Supabase Storage سرو کرد، همان‌ها هم بدون هیچ تغییر کد اضافی از همین
   پایپ‌لاین بهینه‌سازی رد می‌شوند — لازم نیست هیچ‌کس قبل از آپلود عکس را
   دستی فشرده کند.
5. وابستگی جدید: `sharp` (برای بهینه‌سازی سمت سرور در محیط self-hosted؛
   روی Vercel خودِ پلتفرم بهینه‌سازی را انجام می‌دهد و `sharp` صرفاً برای
   سازگاری/تست محلی لازم است، ضرری هم ندارد).



نقشهٔ راهِ فازهای ۱ تا ۸ اساساً «feature roadmap» است: هر فاز یک قابلیت جدید اضافه می‌کند. اما یک سیستم رزرو واقعی که قرار است پول واقعی مسافران را جابه‌جا کند، تا وقتی این فاز به‌طور جدی انجام نشود، **آمادهٔ تولید (Production) نیست** — صرفِ «تیک‌زدن» فازهای ۱ تا ۸ کافی نیست. به همین دلیل تست/امنیت/کارایی به‌جای این‌که پراکنده و کم‌رنگ در فازهای دیگر باقی بماند، یک فاز کاملاً مستقل و جدی (فاز ۹) می‌شود، دقیقاً بعد از دو‌زبانگی (فاز ۸) و پیش از استقرار نهایی (فاز ۱۱).

### ۹.۱ تا ۹.۱۰ — انواع تست الزامی

1. **Unit Testing** — منطق محاسباتی خالص و بدون وابستگی به شبکه: محاسبهٔ تخفیف سطح عضویت/کوپن (و قانون جمع‌شدن یا نشدنشان طبق فاز ۴.۲)، شمارش ظرفیت صندلی، قیمت‌گذاری نهایی سفارش، منطق `held_until`.
2. **Integration Testing** — تعامل واقعی لایه‌های به‌هم‌وابسته: Route Handler ↔ Supabase (شامل رفتار واقعی RLS، نه فقط mock)، اتصال به HesabPay در محیط sandbox، اتصال به سرویس SMS.
3. **End-to-End (E2E) Testing** — کل مسیر واقعی یک مسافر از جستجوی مسیر تا دریافت بلیت دیجیتال، هم در حالت مهمان (Guest) هم کاربر ثبت‌نام‌شده، هم در دری (RTL) هم انگلیسی (LTR).
4. **Concurrency Testing** — دقیقاً سناریوی «دو مسافر هم‌زمان یک صندلی مشترک را رزرو می‌کنند» که در فاز ۴.۲ دستی تست شد؛ در این فاز باید به‌صورت خودکار و تکرارپذیر (نه فقط یک تست دستی با دو تب مرورگر) پوشش داده شود، همراه با سناریوهای مشابه دیگر (رزرو هم‌زمان آخرین صندلی خالی، تأیید هم‌زمان دو پرداخت برای یک رزرو).
5. **Authorization Testing** — تأیید عملی این‌که سیاست‌های RLS هر ۱۶ جدول (فاز ۳.۲) واقعاً همان‌طور که طراحی شده کار می‌کنند: یک ادمین دپارتمانی نتواند به داده‌های خارج از حیطه‌اش دسترسی پیدا کند، یک کاربر مهمان نتواند رزرو مسافر دیگری را با حدس‌زدن شناسه ببیند، `service_role` فقط از مسیرهای مجاز فراخوانی شود.
6. **Payment Testing** — تمام مسیرهای ممکن پرداخت: موفق، ناموفق، معلق (Pending)، Webhook تکراری/دیرهنگام HesabPay، تطبیق مبلغ واقعی با مبلغ رزرو، رفتار سیستم روی پرداخت آفلاین در انتظار تأیید ادمین.
7. **Failure Recovery Testing** — رفتار سیستم وقتی یک وابستگی خارجی (Supabase، HesabPay، سرویس SMS) قطع یا کند می‌شود؛ آیا صندلی قفل‌شده به‌درستی آزاد می‌شود، آیا کاربر پیام خطای قابل‌فهم می‌بیند، آیا رزرو در وضعیت نیمه‌کاره باقی نمی‌ماند.
8. **Accessibility Testing** — پیمایش کامل با کیبورد، سازگاری با screen reader، کنتراست رنگ کافی (به‌ویژه روی پالت تیرهٔ «Night-road»)، در هر دو جهت RTL و LTR.
9. **Performance Testing** — زمان پاسخ صفحات پرترافیک (صفحهٔ اصلی، نتایج جستجو) زیر بار همزمان چند کاربر، به‌خصوص در ساعات اوج فروش بلیت.
10. **Security Hardening** — Rate Limiting روی مسیرهای حساس (`/api/bookings`, `/api/payments/*`, لاگین ادمین)، بررسی مقاومت در برابر تزریق SQL/XSS، بازبینی نهایی این‌که هیچ کلید حساس (`SUPABASE_SERVICE_ROLE_KEY`, کلید HesabPay) در کد سمت کلاینت افشا نشده.

### ۹.۲ اصل کلی این فاز

همان اصل «عدم جعل اطلاعات» (بخش ۷) اینجا هم حاکم است: نتیجهٔ هر تست باید واقعاً اجرا و مستند شود (نه فرض شود که «احتمالاً درست کار می‌کند»). خروجی این فاز باید شامل فهرست موارد رفع‌شده و — مهم‌تر — فهرست صادقانهٔ مواردی باشد که کشف شده اما هنوز رفع نشده‌اند، دقیقاً به همان سبک که در فاز ۴.۳ یک نقص امنیتی واقعی (EXECUTE عمومی روی `hold_seats`/`confirm_booking`) کشف و مستند شد.

---

## ۱۰. فاز ۱۰ — نظارت و مانیتورینگ (Observability) (تصمیم تکمیلی)

یک سیستم Production بدون قابلیت مشاهدهٔ رفتار واقعی‌اش ناقص است؛ باید بعد از استقرار هم بشود فهمید سیستم دقیقاً چه می‌کند، نه فقط این‌که «کار می‌کند یا نه». این فاز بعد از QA (فاز ۹) و پیش از استقرار نهایی (فاز ۱۱) می‌آید، چون ابزار مانیتورینگ باید از همان روز اول Production فعال باشد، نه بعداً اضافه شود.

### ۱۰.۱ لاگ‌گذاری ساختاریافته و ردیابی خطا

هر خطای سمت سرور (Route Handler، Server Component، Supabase Function) باید به‌صورت ساختاریافته لاگ شود (نه فقط `console.log` پراکنده)، با اطلاعات کافی برای ردیابی بعدی (کد رزرو یا شناسهٔ درخواست، نوع خطا، لایه‌ای که خطا در آن رخ داده).

### ۱۰.۲ متریک‌های عملیاتی کلیدی که باید همیشه قابل‌مشاهده باشند

1. تعداد/نرخ **شکست رزرو** (booking failure)
2. تعداد/نرخ **شکست قفل موقت صندلی** (hold failure — شامل انقضای `held_until` و تلاش هم‌زمان رقابتی)
3. تعداد/نرخ **شکست پرداخت** (payment failure)، به تفکیک آنلاین (HesabPay) و آفلاین
4. تعداد/نرخ **شکست پیگیری رزرو** (lookup failure — کد رهگیری یا شماره تماس نامعتبر/عدم تطابق)

### ۱۰.۳ مانیتورینگ کارایی API و دیتابیس

مشخص‌کردن این‌که کدام مسیر API کند است و کجای دیتابیس گلوگاه (bottleneck) شده — از جمله با استفاده از ابزار `query_logs` که خودِ Supabase در دسترس قرار می‌دهد، برای بررسی کوئری‌های کند روی جداولی مثل `trips`/`trip_seats` که بیشترین ترافیک را دارند.

### ۱۰.۴ داشبورد و هشدار

نتایج بخش‌های ۱۰.۱ تا ۱۰.۳ باید در یک داشبورد قابل‌مشاهده برای ادمین کل جمع شوند و برای رخدادهای بحرانی (مثلاً نرخ شکست پرداخت بالاتر از حد عادی) هشدار مناسب تعریف شود، نه این‌که فقط در لاگ‌های خام دفن بماند.

---

## ۱۱. معیار کیفیت نهایی پروژه (یادآوری برای همهٔ فازهای بعدی)

تکمیل صرفِ فازهای Feature (۱ تا ۸) بدون جدی‌گرفتن فازهای ۹ و ۱۰، یک محصول قابل‌فروش اما شکننده تحویل می‌دهد. رعایت کامل موارد زیر است که پروژه را از «کار می‌کند» به «آمادهٔ Production واقعی» می‌رساند — این یادآوری باید در هر چت جدیدی که به فازهای ۹، ۱۰ یا ۱۱ می‌رسد، در نظر گرفته شود:

- Automated Testing (فاز ۹.۱–۹.۴)
- Security Hardening + Rate Limiting (فاز ۹.۱۰)
- Accessibility (فاز ۹.۸)
- Responsive QA (فاز ۴.۶ و ۵.۵)
- Performance Optimization (فاز ۹.۹)
- Monitoring / Error Tracking / Observability (فاز ۱۰)
- Real Payment Integration (فاز ۶، تست‌شده در فاز ۹.۶)
- Real Admin CRUD (فاز ۵)
- Production Deployment (فاز ۱۱.۱–۱۱.۲)
- Backup/Recovery (فاز ۱۱.۳)
- Documentation (فاز ۱۱.۴)

---

## ۱۲. یادداشت‌های اصلاحی بین‌فازی (خارج از شمارهٔ فاز رسمی)

این بخش برای رفع باگ‌ها/نکاتی است که حین بررسی یک فاز کشف می‌شوند اما به
خودِ آن فاز مربوط نیستند — تا در حافظهٔ پروژه گم نشوند.

### ۱۲.۱ باگ ترجمهٔ پیام خطا (رفع شد — ۴ سپتامبر ۲۰۲۶)

**کشف حین بررسی فاز ۴.۶.** پیام‌های خطای سمت کلاینت در ۶ فایل، به‌جای نگه‌داشتن
یک *کلید* ترجمه در state، مستقیماً رشتهٔ از‌قبل‌ترجمه‌شده را ذخیره می‌کردند؛
نتیجه این بود که اگر کاربر بعد از دیدن یک پیام خطا زبان سایت را عوض می‌کرد،
همان پیام قدیمی (به زبان قبلی) روی صفحه می‌ماند. فایل‌های اصلاح‌شده:
`account-login.tsx`، `account-signup.tsx`، `account-complete-profile.tsx`،
`checkout-form.tsx`، `seat-selection.tsx`، `booking-lookup.tsx`. الگوی درست
(ذخیرهٔ کلید خطا + resolve در لحظهٔ رندر) باید در فاز ۴.۷ و فازهای بعدی هم
برای هر state خطای جدید رعایت شود.

### ۱۲.۲ دکمهٔ «ورود» در هدر عمومی → پنل ادمین نیست (سوءتفاهم رفع شد)

Zakir پرسید چرا دکمهٔ ورود هدر دیگر به پنل ادمین نمی‌رود. بررسی `git log`
تأیید کرد این یک اصلاح عمدی و مستند در فاز ۴.۵ بوده (نه رگرسیون): قبل از
فاز ۴.۵، این دکمه به‌اشتباه (چون هنوز حساب مسافر وجود نداشت) به `/admin`
اشاره می‌کرد. پنل ادمین همچنان کاملاً فعال و از مسیر مستقیم `/admin`
در دسترس است؛ فقط دیگر در ناوبری عمومی سایت تبلیغ نمی‌شود.

### ۱۲.۳ سه باگ ریسپانسیو واقعی (رفع شد — ۵ سپتامبر ۲۰۲۶)

**کشف‌شده توسط Zakir از طریق تست واقعی روی موبایل (نه از طریق build/tsc — این ابزارها
مشکلات بصری/رندر مرورگر رو نمی‌بینن).** یادداشت مهم برای فازهای بعدی: تأیید نهایی
هر تغییر ریسپانسیو باید شامل تست چشمی روی دستگاه واقعی باشه، نه فقط کامپایل موفق.

1. **عکس ناوگان روی موبایل فقط یه چراغ ماشین نشون می‌داد.** علت: تصویر پس‌زمینه
   با `absolute inset-0` کل ارتفاع سکشن رو پر می‌کرد؛ چون ۴ کارت روی موبایل زیر هم
   قرار می‌گیرن، سکشن خیلی بلند و باریک می‌شد و عکس عمودی ۴:۵ به یه نوار نازک
   وسط (دقیقاً روی چراغ) کراپ می‌شد. رفع: عکس به یه نوار با ارتفاع ثابت
   (`h-[560px] sm:h-[460px] lg:h-auto`) محدود شد، نه کل ارتفاع متغیر سکشن.
   **یادداشت برای معماری کلی:** هر جای دیگهٔ سایت که پس‌زمینهٔ عکسی پشت محتوای
   بلند/انباشته (چند کارت زیر هم) قرار بگیره، باید همین الگو رعایت بشه.

2. **تیتر هیرو (انگلیسی، موبایل) گاهی از صفحه بیرون می‌زد** تا کاربر یکی‌دوبار
   کلیک/ریسایز می‌کرد. علت: کلاس `text-balance` (فیچر آزمایشی CSS) با فونت‌های
   next/font گاهی بعد از لود فونت واقعی دوباره محاسبه نمی‌شه. رفع: از هر ۴ تیتری
   که این کلاس رو داشتن (هیرو، ناوگان، چرا شبراو، درباره‌ما) حذف و با `break-words`
   جایگزین شد.

3. **تایم‌لاین «مسیرهای ما» روی موبایل نصفه‌نیمه به نظر می‌رسید.** اسکرول افقی
   کار می‌کرد ولی هیچ نشونهٔ بصری نبود؛ یه آیکون شهر یهو در لبهٔ صفحه قطع می‌شد.
   رفع: گرادیان محو در دو لبه (فقط موبایل) اضافه شد تا «قابل‌اسکرول‌بودن» رو
   بصری نشون بده.

**فایل‌های تغییریافته:** `fleet-features.tsx`، `hero-search.tsx`، `why-us.tsx`،
`about-page.tsx`، `destinations-grid.tsx`.

### ۱۲.۴ ادامهٔ فاز ۴.۶ — ممیزی مستقل و رفع ۲۵ یافتهٔ ریسپانسیو/RTL (تکمیل — ۵ سپتامبر ۲۰۲۶)

**زمینه:** این ادامهٔ فاز ۴.۶ است (یک مأموریت واحد: ریسپانسیو حرفه‌ای سایت
پابلیک)، نه یک فاز جداگانه. مراحل الف (ممیزی ۲۵ ردیفه) و ب (استخراج قواعد
منبع‌دار) در یک چت جداگانه انجام شد؛ این چت فقط مرحلهٔ ج (اجرا) را طبق فایل
خلاصهٔ خودکفای `PHASE-4_8-STAGE-C-HANDOFF.md` انجام داد، بدون تکرار ممیزی.

**🔴 بحرانی (۴ ردیف) — همه رفع شد:**
- **#۲۳ (فلاش RTL/LTR):** ترجیح زبان حالا موازی در یک کوکی (`shabraw-lang`)
  هم ذخیره می‌شود؛ `app/layout.tsx` به Server Component تبدیل شد و
  `dir`/`lang` را از همان کوکی در لحظهٔ رندر سرور می‌خواند (فایل جدید
  `lib/lang-cookie.ts`؛ ویرایش `lib/lang-context.tsx`).
- **#۱۷ (تقویم بیرون‌زده روی موبایل):** `date-picker.tsx` با
  `@floating-ui/react-dom` (middleware‌های flip/shift + autoUpdate) بازنویسی
  شد؛ زیر ۶۴۰px به‌جای popover چسبیده، یک modal تمام‌عرض وسط صفحه رندر
  می‌شود (وابستگی جدید در package.json).
- **#۱۳/۱۴ (دکمهٔ حیاتی بیرون از دید):** یک نوار CTA مستقل `fixed
  inset-x-0 bottom-0` (زیر `lg`) به `seat-selection.tsx` و
  `checkout-form.tsx` اضافه شد؛ دکمهٔ اصلی داخل sidebar فقط از `lg` به بالا
  نمایش داده می‌شود (sidebar هم فقط از `lg` به بالا `sticky` است).
- **#۱۸ (فرم جستجوی هیرو ناقص دیده می‌شد):** فاصله‌گذاری عمودی
  `hero-search.tsx` روی موبایل جمع‌تر شد. متن زیرتیتر/راهنما هم بعداً با
  تصمیم Zakir کوتاه شد — جزئیات در بخش ۱۲.۹.
- **#۱۰ (عکس تبلت/عریض هیرو):** پرامپت اصلی v0.dev این عکس در دسترس Zakir
  نیست. طبق تصمیم Zakir، به‌جای رفع کامل یا رفع‌نشده ماندن، موقتاً از عکس
  صفحهٔ درباره‌ما استفاده شد — `about-corridor-dusk-tablet.png`/`-wide.png`
  دقیقاً هم‌ابعاد پیکسلی نسخهٔ موبایل/دسکتاپ هیرو هستند (هر دو خانواده از
  همان پرامپت v0.dev ساخته شده بودند)، پس به‌عنوان placeholder موقت در
  `hero-search.tsx` وایر شدند (با کامنت `[PLACEHOLDER]` در کد). **باید
  هروقت عکس واقعی هیروی تبلت/عریض ساخته شد، این دو منبع در
  `hero-search.tsx` جایگزین شوند.**

**🟡 متوسط (۷ ردیف):**
- **#۱ (بریک‌پوینت `md:` غایب):** فقط مواردی که واقعاً یک گرید کارت/آمار را
  مستقیم از یک ستون به چند ستون می‌پراند رفع شد (`account-dashboard.tsx`
  رده `sm:grid-cols-2` گرفت). یک ممیزی کامل بصری ۳۰-فایلی انجام **نشد** —
  فقط تحلیل کد؛ `fleet-features.tsx` عمداً دست نخورد چون قبلاً در بحث
  Container-Query رد شده بود.
- **#۳ (گرادیان اسکرول destinations-grid):** به `left-0`/`right-0` فیزیکی
  تغییر کرد (محتوای داخلش `dir="ltr"` اجباری دارد).
- **#۴ (عکس مسیر کابل→غزنی):** عکس واقعی غزنی موجود نیست؛ فعلاً با کامنت
  `[PLACEHOLDER]` در `lib/i18n.ts` مستند شد — **نیاز به تصمیم Zakir**.
- **#۲۰ (جدول routes-index بدون نشونهٔ اسکرول):** گرادیان محو دو لبه اضافه
  شد (مشابه destinations-grid، ولی اینجا `start-0`/`end-0` منطقی درست است
  چون جدول dir فیزیکی اجباری ندارد).
- **#۲، #۱۲، #۱۹ (تاچ‌تارگت زیر ۴۴px):** دکمهٔ آیکون تقویم، `SeatButton`، و
  همبرگر منوی موبایل همه به `size-11` (۴۴px) رسیدند، بدون استثنای موبایل.
- **#۲۴ (گرید فوتر):** رده `sm:grid-cols-2` قبل از `md:grid-cols-4` اضافه شد.
- **#۶ (کلاس `text-pretty`):** **دست نخورد** — طبق راه‌حل مصوب، نیاز به تست
  عملی با اسکرین‌شات واقعی قبل از تصمیم نگه‌داشتن/حذف دارد؛ در چک‌لیست تست
  دستی Zakir گنجانده شد.

**🟢 جزئی (۳ ردیف) — همه رفع شد:**
- **#۵:** `public/images/city-mazar.png` (عکس یتیم) حذف شد.
- **#۲۲:** `components/admin/admin-panel.tsx` (نسخهٔ کاملاً یتیم/duplicate)
  حذف شد — فقط `components/transport/admin-panel.tsx` واقعی است.
- **#۲۵ (گرید why-us):** رده‌های `xl:grid-cols-4 2xl:grid-cols-5` بین
  `lg:grid-cols-3` و `3xl:grid-cols-6` اضافه شد.

**⚪ نیاز به بررسی بصری (۴ ردیف) — عمداً دست نخورد:** #۷، #۸، #۱۱، #۲۱ — طبق
تعریف خودِ فاز، این‌ها را نمی‌شود بدون اسکرین‌شات واقعی رفع کرد؛ در چک‌لیست
تست دستی Zakir گنجانده شدند.

**ردیف‌های رد‌شده (دست نخورد، طبق دستور صریح سند دست‌به‌دست):** #۱۵، #۱۶
(false-positive قبلی)، سوال Container-Query در fleet-features (قبلاً رد
شده)، و تمام یافته‌های admin-panel (خارج از دامنه — برای فاز ۵.۵ ماند).

**اعتبارسنجی:** `tsc --noEmit` صفر خطای جدید (۸ خطای از قبل موجود در
فایل‌های دست‌نخورده — `account-login.tsx`، `account-signup.tsx`،
`account-complete-profile.tsx`، `app/api/bookings/confirm/route.ts` — روی
چک‌اوت تازهٔ ریپو هم تکرارپذیر بود). `next build` واقعی روی ۲۵ مسیر موفق
اجرا شد (Turbopack) — در محیط build فقط فچ فونت از
`fonts.googleapis.com` به‌خاطر محدودیت شبکهٔ سندباکس این چت شکست خورد که
ربطی به کد ندارد و در Vercel رخ نمی‌دهد؛ برای تأیید، فونت‌ها موقتاً stub
شدند، build کامل با موفقیت تمام شد، سپس `next/font/google` واقعی بازگردانده
شد (بدون تغییر نهایی در کد تحویلی).

**وابستگی جدید:** `@floating-ui/react-dom` (برای date-picker، ردیف #۱۷).

**تست دستی حیاتی که Claude نمی‌تواند انجام دهد (نیاز به Zakir):** تست چشمی
واقعی روی دستگاه (طبق تجربهٔ ۱۲.۳) — عرض‌های ۳۷۵/۷۶۸/۱۰۲۴/۱۹۲۰/۲۵۶۰px، هر دو
زبان، با تمرکز ویژه روی مودال موبایل تقویم و نوار CTA ثابت.

### ۱۲.۵ کندی حس‌شدهٔ سایت، به‌خصوص هنگام جابه‌جایی بین صفحات (گزارش Zakir —
هنوز رفع نشده، فقط تشخیص)

**تشخیص (بر اساس بررسی واقعی کد، نه حدس):**

1. **مشکوک‌ترین علت — نبود هرگونه استراتژی cache/revalidate:** هیچ‌جای
   کد (`app/*/page.tsx`) از `revalidate`، `unstable_cache`، یا
   `export const dynamic = "force-static"` استفاده نشده؛ خروجی واقعی
   `next build` هر ۲۵ مسیر را `ƒ (Dynamic)` علامت زده — یعنی **هر بار**
   کاربر بین صفحات جابه‌جا می‌شود (حتی صفحاتی مثل درباره‌ما/تماس/مسیرها که
   محتوایشان به‌ندرت عوض می‌شود)، یک round-trip زندهٔ کامل به Supabase
   (region `ap-south-1`) اتفاق می‌افتد. صفحاتی که واقعاً باید همیشه زنده
   بمانند (نتایج جستجو، نقشهٔ صندلی، وضعیت پرداخت) درست است که دینامیک
   باشند؛ ولی صفحات محتوایی (درباره‌ما، تماس، سوالات متداول، فهرست
   مسیرها/قیمت‌ها، حتی لیست شهرهای صفحهٔ اصلی) کاندید خوبی برای ISR
   (`revalidate: 60` یا مشابه) هستند — این‌ها می‌توانند حس ناوبری را به‌شدت
   سریع‌تر کنند بدون این‌که چیزی «قدیمی» به نظر برسد.
2. **احتمال محیط توسعه (`next dev`) به‌جای build واقعی:** اگر Zakir این
   کندی را روی `pnpm dev` حس می‌کند (نه یک build/deploy واقعی)، بخشی از
   آن طبیعی است — Next.js در حالت dev هر route را فقط در اولین بازدید
   کامپایل می‌کند (به‌خصوص محسوس «وقتی بین صفحات جابه‌جا می‌شویم»، دقیقاً
   الگوی توصیف‌شده). قبل از هر نتیجه‌گیری دیگر باید با `next build &&
   next start` (یا دیپلوی واقعی Vercel) مقایسه شود.
3. **حجم عکس‌ها:** تا قبل از رفع بخش ۸.۴ (بهینه‌سازی خودکار عکس)، هر عکس
   ۱.۵-۲ مگابایتی مستقیم می‌رفت — این بخش رفع شده، ولی چون هنوز خودِ سایت
   دیپلوی/تست نهایی نشده، ارزش تأیید دوباره بعد از رفع را دارد.
4. **رنج وزن فونت‌های next/font/google:** هر دو فونت (`Vazirmatn`،
   `Fraunces`) با رنج کامل وزن ۱۰۰-۹۰۰ لود می‌شوند — روی لود اول صفحه اثر
   دارد (نه لزوماً روی ناوبری بین صفحات چون فونت در layout ریشه یک‌بار لود
   می‌شود)، ولی ارزش بررسی محدودکردن وزن‌های واقعاً استفاده‌شده را دارد.

**اقدام پیشنهادی برای فاز بعدی (فاز ۹.۹ Performance Testing یا یک فاز
مستقل کوچک قبل از آن):** ابتدا آیتم #۲ را رد کنید (تست با build واقعی)؛
اگر کندی باقی ماند، برای صفحات محتوایی ایستا (درباره‌ما/تماس/سوالات
متداول/مسیرها) `revalidate` یا static generation اضافه شود؛ صفحات
معاملاتی (جستجو/صندلی/پرداخت) دینامیک بمانند. سپس اندازه‌گیری واقعی
(Lighthouse یا مشابه) قبل/بعد ثبت شود — نه فرض.

### ۱۲.۶ باگ چک‌اوت #۱ — پاک‌شدن اطلاعات فرم مسافر هنگام رفتن به صفحهٔ
قوانین و بازگشت (گزارش Zakir — هنوز رفع نشده)

**علت واقعی (تأییدشده در کد):** در `checkout-form.tsx`، تمام اطلاعات فرم
(`passengers`, `phone`, `email`, `couponCode`, ...) با `useState` **فقط
در حافظهٔ خودِ کامپوننت** نگه‌داری می‌شود. لینک «قوانین و ضوابط» داخل همین
فرم به `/terms` می‌رود (`<Link href="/terms">`) — یک ناوبری واقعی به صفحهٔ
دیگر که کامپوننت `CheckoutForm` را کامل unmount می‌کند؛ وقتی کاربر با
دکمهٔ Back مرورگر برمی‌گردد، `CheckoutForm` از صفر mount می‌شود و همان
state اولیهٔ خالی را می‌گیرد — یعنی هیچ‌چیز واقعاً «ذخیره نشده بود که پاک
شود»، خودِ معماری فعلی هیچ حافظه‌ای بین این دو صفحه ندارد.

**راه‌حل‌های ممکن برای فاز بعدی (تصمیم نهایی با Zakir):** (الف) صفحهٔ
قوانین به‌جای ناوبری کامل، به‌صورت modal/drawer روی همان صفحهٔ چک‌اوت باز
شود (بدون unmount شدن فرم) — ساده‌ترین و کم‌ریسک‌ترین راه؛ یا (ب) مقادیر
فرم در `sessionStorage` (نه state خام، برای بقای بین ناوبری‌های همان تب)
ذخیره و در mount مجدد بازیابی شود. گزینهٔ (الف) برای این مورد خاص ترجیح
دارد چون کاربر واقعاً نیازی به ترک کامل صفحهٔ چک‌اوت ندارد.

### ۱۲.۷ باگ چک‌اوت #۲ — کلیک روی «ادامه/پرداخت» با فیلد خالی هیچ اتفاق
قابل‌مشاهده‌ای ندارد (گزارش Zakir — هنوز رفع نشده)

**علت واقعی (تأییدشده در کد):** برخلاف تصور اول، `handleSubmit` در
`checkout-form.tsx` واقعاً اعتبارسنجی می‌کند و state خطا (`errors`) را
درست تنظیم می‌کند؛ برای هر فیلد نام/شمارهٔ تماس/پذیرش قوانین خالی، یک
حاشیهٔ قرمز + متن خطا («این فیلد لازم است» و مشابه) در JSX رندر می‌شود.
مشکل واقعی این است که **هیچ scroll-to-error یا اسکرول خودکاری وجود
ندارد** — اگر کاربر روی موبایل تا انتهای فرم (نزدیک نوار CTA ثابت پایین
صفحه که در همین فاز اضافه شد، یا قبل از آن نزدیک دکمهٔ اصلی) اسکرول کرده
باشد و فیلد نام (نزدیک بالای فرم) خالی مانده باشد، پیام خطا کاملاً خارج
از دید رندر می‌شود — از نگاه کاربر دقیقاً «هیچ اتفاقی نمی‌افتد»، در حالی
که در واقع یک تغییر state نامرئی رخ داده.

**راه‌حل پیشنهادی برای فاز بعدی:** بعد از `setErrors(nextErrors)` در
صورت وجود خطا، با `scrollIntoView({ behavior: "smooth", block: "center"
})` روی اولین فیلد دارای خطا (یا ref مربوطه) اسکرول خودکار انجام شود؛
برای فیلدهای select/checkbox هم فوکوس برنامه‌ریزی‌شده اضافه شود تا هم
دیده شود هم برای صفحه‌خوان (screen reader) قابل‌تشخیص باشد.

### ۱۲.۸ باگ واقعی — عکس‌های پس‌زمینهٔ بخش‌های هیرو/درباره‌ما/ناوگان روی
مانیتورهای خیلی بزرگ عمودی بریده می‌شوند (گزارش Zakir با اسکرین‌شات — رفع شد
۵ سپتامبر ۲۰۲۶)

**علت واقعی (تأییدشده در کد، الگوی تکرارشونده در ۳ فایل):** در
`about-page.tsx`، `hero-search.tsx`، و بخشی از `fleet-features.tsx` (از
`lg` به بالا، یعنی `lg:h-auto`)، ارتفاع `<section>` توسط **محتوای متنی
داخلش** تعیین می‌شود (که عرضش با `max-w-4xl`/`max-w-2xl`/... محدود شده و
عملاً روی مانیتورهای خیلی عریض دیگر رشد نمی‌کند)، در حالی که عکس پس‌زمینه
با `<div className="absolute inset-0">` کل عرض واقعی صفحه (نه عرض محدودشدهٔ
محتوا) را پر می‌کند. نتیجه: روی یک مانیتور معمولی نسبت عرض‌به‌ارتفاعِ این
باکس نزدیک به نسبت خودِ عکس (`wide` crop، تقریباً ۲.۳:۱) است، ولی روی
مانیتور خیلی عریض (۲۵۶۰px به بالا) این باکس بدون اینکه ارتفاعش رشد کند فقط
عریض‌تر می‌شود — نسبتش مثلاً به ۴.۵:۱ یا بیشتر می‌رسد. چون `object-cover`
همیشه عکس را طوری بزرگ می‌کند که کل عرض باکس را بپوشاند، عملاً عکس خیلی
بزرگ‌تر از ارتفاع باکس می‌شود و مرورگر بخش زیادی از بالا/پایینش را می‌بُرد
(پیش‌فرض `object-position: center` — یعنی برش از هر دو طرف، ولی چون نقطهٔ
مهم تصویر (کوه‌ها/جاده) پایین‌تر از وسط تصویر است، حسِ نهایی این می‌شود که
از وسط به پایین دیده نمی‌شود). این دقیقاً برعکسِ باگ مشابهی است که در بخش
۱۲.۳ برای fleet-features روی **موبایل** رفع شد (آنجا باکس خیلی باریک/بلند
بود، اینجا خیلی عریض/کوتاه است)؛ یعنی الگوی زیرساختی «ارتفاع بخش را با یک
مقدار ثابت/مستقل از عرض واقعی صفحه کنترل کن، نه بگذار فقط با محتوا تعیین
شود» باید حالا برای انتهای طیف عریض هم اعمال شود.

**دامنهٔ تأییدشده در برابر مشکوک:**
- **تأییدشده با اسکرین‌شات Zakir:** `about-page.tsx` (بخش هیروی
  درباره‌ما).
- **به‌شدت مشکوک، هنوز با اسکرین‌شات تأیید نشده:** `hero-search.tsx`
  (هیچ کنترل ارتفاعی ندارد، پس همان الگو را دارد) و `fleet-features.tsx`
  از `lg` به بالا (`lg:h-auto` دقیقاً یعنی از این نقطه به بعد ارتفاع را به
  محتوا واگذار می‌کند، بدون هیچ جبرانی برای ۳xl/۴xl). باید هر دو هم روی
  مانیتور ۲۵۶۰px+ با اسکرین‌شات تأیید شوند، طبق اصل «بدون بررسی واقعی فرض
  نشود».

**راه‌حل نهایی پیاده‌شده — نسخهٔ دوم، دقیق (نه فقط min-height تقریبی):**

نسخهٔ اول (همین چت، قبل‌تر) یک `min-h` تقریبی روی ۳xl/۴xl گذاشت که فقط در
همون دو نقطه اثر داشت، نه در فاصله‌ها یا مانیتورهای بزرگ‌تر (مثل ۳۴۴۰px).
Zakir با مقایسهٔ مستقیم اسکرین‌شات با flixbus.com نشان داد که سایت‌های
واقعی این صنعت **هرگز** عکس را نمی‌برند — چون باکس عکس را دقیقاً هم‌نسبتِ
خودِ فایل عکس نگه می‌دارند (نه یک ارتفاع حدسی).

**راه‌حل نهایی:** با Pillow ابعاد دقیق هر ۴ کراپ اندازه‌گیری شد (هر سه
خانواده — هیرو/ناوگان/درباره‌ما — دقیقاً یک نسبت مشترک دارند، چون از یک
دسته تولید شده‌اند):
`mobile`=۱۱۲۲×۱۴۰۲ (۰.۸)، `tablet`=۱۴۴۸×۱۰۸۶ (۴:۳)، `desktop`=۱۶۷۲×۹۴۱
(≈۱۶:۹)، `wide`=۱۹۱۵×۸۲۱ (≈۲۱:۹). باکس عکس در هر سه فایل از
`absolute inset-0` (هم‌اندازهٔ کامل section) به `absolute inset-x-0 top-0`
+ `aspect-[<نسبت دقیق>]` پلکانی هم‌سو با بریک‌پوینت‌های خودِ ResponsivePhoto
تغییر کرد:
`aspect-[1122/1402] md:aspect-[1448/1086] lg:aspect-[1672/941]
3xl:aspect-[1915/821]`

**نتیجه:** عکس دیگر با کل ارتفاع section (که با محتوای متن/فرم تعیین
می‌شود) گره نخورده؛ فقط یک نوار با نسبت ثابت، چسبیده به بالای section است
— برش صفر، در **هر** عرضی (نه فقط دو نقطهٔ بریک‌پوینت)، دقیقاً همان الگویی
که FlixBus.com واقعاً استفاده می‌کند. اگر محتوای متن/فرم بلندتر از این
نوار باشد، به‌طور طبیعی روی پس‌زمینهٔ صافی که گرادیان به آن محو می‌شود ادامه
پیدا می‌کند — دقیقاً مثل کارتِ سفیدِ فرم جست‌وجوی FlixBus که خودش تا حدی
زیر عکس و تا حدی روی زمینهٔ ساده می‌نشیند.

این تغییر جایگزین `min-h` قبلی **و** جایگزین راه‌حل ارتفاع ثابت px موبایل
`fleet-features.tsx` (از ۱۲.۳) شد — یک الگوی یکسان و دقیق، به‌جای دو حدسِ
جداگانه برای دو سرِ طیف اندازه.

`objectPosition="center 70%"` (از راه‌حل قبلی) به‌عنوان محافظ برای هر گونه
اختلاف رندر نرم‌افزاری جزئی نگه داشته شد؛ چون نسبت‌ها الان دقیقند، عملاً
دیگر برشی برای جبران کردن وجود ندارد.

**اعتبارسنجی:** `tsc --noEmit` و `next build` واقعی هر دو بعد از این
تغییر دوباره صفر خطای جدید تأیید کردند. تأیید بصری نهایی روی مانیتور واقعی
۲۵۶۰px+ (و ایده‌آل: ۳۴۴۰px+ هم اگر در دسترس است) هنوز با Zakir است.

### ۱۲.۹ سادگی متن هیروی صفحهٔ اصلی — طبق استاندارد صنعت (تصمیم Zakir،
اعمال شد — ۵ سپتامبر ۲۰۲۶)

**زمینه:** ردیف #۱۸ (فاز ۴.۸/۴.۶) قبلاً فقط فاصله‌گذاری موبایل هیرو را جمع
کرد و صریحاً نوشت «کوتاه‌کردن خودِ متن نیاز به هماهنگی با Zakir دارد».
Zakir حالا با مقایسهٔ مستقیم با FlixBus.com (که فقط یک تیتر کوتاه، بدون
پاراگراف توضیحی، بالای فرم جست‌وجو دارد) این تصمیم را گرفت.

**تغییر اعمال‌شده در `lib/i18n.ts` (هر دو زبان fa/en):**
- `hero.subtitle`: از یک پاراگراف کامل (با فهرست کامل هر ۶ شهر توقف —
  غزنی/قلات/کندهار/هلمند/نیمروز/فراه، که همان اطلاعات دقیقاً چند سکشن
  پایین‌تر در `destinations-grid` هم دوباره نمایش داده می‌شود، یعنی تکراری
  هم بود) به یک جملهٔ کوتاه تک‌خطی خلاصه شد.
- `hero.helper` (داخل فرم): از یک جملهٔ رسمی کامل به یک خط کوتاه خلاصه شد؛
  چون فیلدهای فرم (مبدأ/مقصد/تاریخ سفر) خودشان با لیبل واضح مشخص‌اند، نیازی
  به توضیح مفصل نیست.
- `hero.kicker` و `hero.title` دست‌نخورده ماندند — این دو از اول کوتاه و
  استاندارد بودند، شلوغی فقط در دو پاراگراف بعدی بود.

**اثر جانبی مثبت:** چون متن حالا کوتاه‌تر است، ریسک باگ نیمه‌حل‌شدهٔ
`text-pretty` (ردیف #۶) روی همین متن عملاً کمتر می‌شود (خط کمتر برای
چیدمان دوباره پس از لود فونت) — هرچند این ردیف هنوز نیاز به تست چشمی مجزا
دارد (طبق ۱۲ چک‌لیست تست دستی).

### ۱۲.۱۰ اشتباه واژگانی — «صندلی» به‌جای «چوکی»، و پاکسازی «کریدور» از کل
متن سایت (گزارش Zakir — رفع شد ۵ سپتامبر ۲۰۲۶)

**اشتباه واقعی Claude، نه سوءتفاهم:** حین نوشتن متن کوتاه‌شدهٔ ۱۲.۹، کلمهٔ
«صندلی» (فارسی ایرانی) به‌جای «چوکی» (دری افغانی) نوشته شد — درست برخلاف
اصل صریح بخش ۷ همین سند («چوکی» نه «صندلی»؛ «بس» نه «اتوبوس») که از اول
پروژه رعایت می‌شد (۲۴ مورد «چوکی» در سراسر `lib/i18n.ts`، فقط همین یک مورد
جدید اشتباه بود). رفع شد.

**«کریدور» — یافتهٔ گسترده‌تر:** این کلمه (وام‌واژهٔ نامأنوس برای این
مفهوم، به‌جای «مسیر» که خودِ سایت هم زیاد استفاده می‌کند) در این چت نوشته
نشده بود؛ از قبل در ۸ جای مختلف `lib/i18n.ts` (هیرو، مسیرهای پرطرفدار،
صفحهٔ مسیرها، چرا ما، درباره‌ما، صفحهٔ تماس) + توضیحات متا (`app/layout.tsx`،
برای نتایج گوگل) وجود داشت. طبق درخواست Zakir، همهٔ این ۹ مورد به «مسیر»
تغییر کرد (با بازنویسی جزئی جمله در یکی دو مورد برای جلوگیری از تکرار
کلمهٔ «مسیر»، نه صرفاً جایگزینی خام).

**دامنهٔ عمدی این رفع:** فقط متن **کاربر-محور** (چیزی که مسافر واقعی روی
سایت می‌بیند یا در نتایج گوگل) پاکسازی شد. «کریدور»/«صندلی» در کامنت‌های
داخلی کد (مثلاً `lib/supabase/queries.ts`، `checkout-form.tsx` — توضیحات
فنی برای توسعه‌دهنده، نه محتوای سایت) دست‌نخورده ماندند؛ این‌ها هرگز به
کاربر نمایش داده نمی‌شوند. اگر Zakir بخواهد این‌ها هم یکدست شوند (برای
خوانایی خودِ کد، نه محصول)، باید صریح درخواست شود.

**درس برای فازهای بعدی:** هر متن جدیدی که برای سایت نوشته می‌شود — حتی
یک خط کوتاه — باید قبل از تحویل با واژه‌نامهٔ تثبیت‌شدهٔ بخش ۷ سند مادر
مطابقت داده شود، نه فقط با حافظه.

### ۱۲.۱۱ فاز ۴.۷ — فلوی «فراموشی رمز عبور» + آمادگی SMTP + آواتار هدر
(تکمیل شد — ۶ سپتامبر ۲۰۲۶)

**۱. بازیابی رمز عبور فراموش‌شده (Forgot Password) — الگوی رسمی
PKCE مستندات Supabase برای Next.js/@supabase/ssr:**

- `app/account/forgot-password/` — فرم ایمیل؛ `resetPasswordForEmail`
  فراخوانی می‌شود با `redirectTo` به `/auth/confirm?type=recovery&next=/account/reset-password`.
  پیام موفقیت **همیشه یکسان** نمایش داده می‌شود (چه ایمیل ثبت باشد چه
  نباشد) تا از افشای عضویت یک ایمیل (Email Enumeration) جلوگیری شود —
  دقیقاً رفتار خودِ Supabase Auth.
- `app/auth/confirm/route.ts` — Route Handler جدید (خارج از middleware،
  چون matcher فقط `/admin` و `/account` است) که `token_hash` داخل لینک
  ایمیل را با `supabase.auth.verifyOtp()` سمت سرور «نقد» می‌کند و نشست
  واقعی می‌سازد؛ لینک نامعتبر/منقضی → ریدایرکت به `/auth/auth-code-error`
  (صفحهٔ عمومی جدید با پیام قابل‌فهم، نه خطای فنی خام).
- `app/account/reset-password/` — فرم رمز عبور جدید؛ فقط با نشستی که
  همان مرحلهٔ قبل ساخته کار می‌کند (`updateUser({ password })`).
  `middleware.ts` برای این یک مسیر رفتار سوم دارد (نه کاملاً عمومی، نه
  «اگر نشست دارد ریدایرکت به داشبورد» مثل بقیهٔ صفحات مهمان‌محور /account)
  — تشخیص «نشست دارد یا نه» را خودِ کامپوننت (`getSession`) انجام
  می‌دهد و بین فرم واقعی و پیام «لینک نامعتبر» انتخاب می‌کند.
- لینک «رمز عبور را فراموش کرده‌اید؟» به `account-login.tsx` اضافه شد.
- کلیدهای i18n جدید (fa/en) برای کل این فلو — بخش `account` در `lib/i18n.ts`.

**نکتهٔ باقی‌مانده برای Zakir (خارج از کد):** خودِ قالب ایمیل «Reset
Password» در پنل Supabase (Authentication → Email Templates) باید
لینک `{{ .ConfirmationURL }}` پیش‌فرض را به الگوی
`{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=recovery&next=/account/reset-password`
تغییر دهد — این تغییر از طریق کد/API این پروژه قابل‌انجام نیست (فقط از
پنل Supabase)؛ راهنمای کامل قدم‌به‌قدم در همان چتی که این فاز انجام شد
داده شده.

**۲. SMTP — تصمیم Zakir: فعلاً به‌تعویق افتاد، فقط معماری آماده شد:**

سرویس Resend (یا هر SMTP دیگر) عمداً وصل **نشد** چون هزینه دارد و هنوز
با مشتری نهایی توافق نشده. راه‌حل: هیچ کد این پروژه به هیچ SMTP خاصی
وابسته نیست — `resetPasswordForEmail`/`signUp` فقط GoTrue (سرور Auth
خودِ Supabase) را صدا می‌زنند؛ اینکه GoTrue ایمیل را از طریق سرویس
پیش‌فرض داخلی Supabase بفرستد یا از طریق یک SMTP سفارشی (Resend/SES/...)،
یک **تنظیم داشبورد** است (Authentication → SMTP)، نه چیزی در کد. یعنی
وقتی بعداً با مشتری روی Resend (یا هر سرویس دیگر) توافق شد، کل تغییر فقط
پرکردن ۵ فیلد در پنل Supabase است — **بدون هیچ تغییر کد**، دقیقاً همان
اصل «Environment Variables/تنظیمات پنل، نه کد» که برای دامنه/هاست هم در
بخش ۴.۲ رعایت شده.

تا آن زمان، سرویس پیش‌فرض داخلی Supabase (رایگان) استفاده می‌شود — با
محدودیت‌های واقعی که باید به Zakir و مشتری روشن باشد: نرخ ارسال محدود
در ساعت، بدون SLA، و رفتن گاه‌به‌گاه به اسپم (دقیقاً همان نگرانی که در
ردیف ۴.۷ اصلی مطرح شده بود). برای تولید واقعی، مستندات رسمی Supabase هم
صراحتاً یک SMTP سفارشی را الزامی می‌دانند.

**۳. آواتار پروفایل در هدر عمومی (درخواست Zakir، الگو از FlixBus):**

دکمهٔ متنی سادهٔ «ورود» در `site-header.tsx` با یک آیکن دایره‌ای +‌ متن
جایگزین شد (کامپوننت مشترک جدید `components/ui/customer-avatar.tsx`) —
دقیقاً الگوی «آیکن پروفایل کنار Register/Sign in» در اسکرین‌شات FlixBus
که Zakir فرستاد:

- هدر حالا سمت کلاینت نشست را تشخیص می‌دهد (`supabase.auth.getUser` +
  `onAuthStateChange`) و در صورت وارد بودن، یک SELECT سبک از `customers`
  (`full_name, avatar_url`) می‌زند — طبق policy موجود `customers_self_select`
  (بخش ۳.۲ RLS)، بدون نیاز به Route Handler یا service_role جدید.
- ستون جدید `customers.avatar_url` (nullable) با یک مایگریشن اضافه شد —
  امروز همیشه `null` است چون **فیچر آپلود عکس ساخته نشده** (این خارج از
  دامنهٔ فاز ۴.۷ بود)؛ تا وقتی خالی است، آیکن سایه‌ای عمومی
  (silhouette) نشان داده می‌شود.
- طبق درخواست صریح Zakir («بعداً بجای اون پروفایل سایه، عکس خود طرف هم
  بتونه بیاد»): چون `next.config.mjs` از فاز ۴.۶ به بعد از قبل دامنهٔ
  Supabase Storage را در `images.remotePatterns` مجاز کرده، وقتی فیچر
  آپلود عکس در فازی بعدی ساخته شود و این ستون مقداردهی شود، هدر **بدون
  هیچ تغییر کدی** عکس واقعی را به‌جای آیکن سایه‌ای نشان می‌دهد — دقیقاً
  همان الگوی «فقط فایل/دیتا عوض شود» که در `ResponsivePhoto` (بخش ۸.۲)
  هم رعایت شده. خودِ فیچر آپلود عکس (Storage bucket، RLS آن، UI آپلود در
  `/account`) هنوز ساخته نشده — باید صریح در فازی بعدی درخواست شود.

**اعتبارسنجی:** `tsc --noEmit` صفر خطای جدید (۸ خطای از قبل موجود و
نامرتبط در `account-signup.tsx`/`account-login.tsx`/`account-complete-profile.tsx`/
`api/bookings/confirm` — مربوط به Args تایپ `signup_customer`، قبل از
این چت هم وجود داشتند، تأیید شده با `git diff`). `next build` واقعی در
همین محیط sandbox به‌خاطر بلاک‌بودن دسترسی شبکه به `fonts.googleapis.com`
(نه به‌خاطر کد این فاز) کامل نشد — باید یک‌بار در محیط واقعی Zakir
(با اتصال اینترنت کامل) اجرا و تأیید شود.

### ۱۲.۱۲ ⚠️ بدهی موقت فاز ۴.۷ — فلوی «فراموشی رمز عبور» با فرمت
implicit پیش‌فرض Supabase، نه فرمت استاندارد پروژه (تصمیم Zakir —
۶ سپتامبر ۲۰۲۶؛ **باید قبل از تحویل نهایی به مشتری استاندارد شود**)

**چرا این بدهی به‌وجود آمد:** طبق نسخهٔ فعلی پنل Supabase، بدون SMTP
اختصاصی، اصلاً اجازهٔ ویرایش محتوای قالب‌های ایمیل Auth داده نمی‌شود
(باکس «Set up custom SMTP to edit templates» — قبلاً حتی روی سرویس
پیش‌فرض هم می‌شد قالب را ویرایش کرد، این محدودیت تازه‌تر پنل است). چون
فاز ۴.۷ اصلی SMTP اختصاصی را عمداً به‌تعویق انداخت (هزینهٔ Resend، تصمیم
Zakir)، فرمت استاندارد پروژه (`token_hash` + `app/auth/confirm/route.ts`،
که در همین فاز ساخته شد) فعلاً **قابل‌استفاده نیست** — چون قالب ایمیل
پیش‌فرض Supabase از `{{ .ConfirmationURL }}` (implicit flow، توکن‌ها
داخل `#hash` آدرس) استفاده می‌کند، نه از `token_hash`.

**راه‌حل موقتی که Zakir انتخاب کرد:** به‌جای معطل‌ماندن کل فیچر تا وقتی
SMTP حل شود، فلوی «فراموشی رمز عبور» با همان قالب پیش‌فرض implicit کار
می‌کند:

- `account-forgot-password.tsx` → `redirectTo` مستقیم به
  `/account/reset-password` است (نه به `/auth/confirm`).
- `account-reset-password.tsx` → چون خواندن `#hash` توسط
  `@supabase/ssr` async است، علاوه بر `getSession()` اولیه، به
  `onAuthStateChange` هم گوش می‌دهد تا رقابت زمانی رخ ندهد؛ بعد از
  ساخته‌شدن نشست، `#hash` حاوی توکن را با `history.replaceState` از نوار
  آدرس پاک می‌کند (نکتهٔ امنیتی — توکن نباید در تاریخچهٔ مرورگر بماند).
- `app/auth/confirm/route.ts` و `app/auth/auth-code-error/page.tsx`
  **حذف نشدند** — کاملاً آماده و دست‌نخورده مانده‌اند، فقط فعلاً توسط
  هیچ لینک ایمیلی صدا زده نمی‌شوند.

**⚠️ قدم‌های دقیق استانداردسازی، قبل از تحویل نهایی به مشتری (یا هر
زمان که SMTP اختصاصی وصل شد):**

۱. یک SMTP اختصاصی در پنل Supabase وصل شود (Authentication → Emails →
   دکمهٔ «Set up SMTP» — Resend یا هر سرویس دیگری که با مشتری توافق شد).
۲. بعد از وصل‌شدن SMTP، قفل ویرایش قالب باز می‌شود؛ برو به
   Authentication → Emails → Reset Password، محتوای HTML را با این
   جایگزین کن:
   ```html
   <h2>بازیابی رمز عبور</h2>
   <p>برای تعیین رمز عبور جدید، روی لینک زیر کلیک کنید:</p>
   <p><a href="{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=recovery&next=/account/reset-password">بازیابی رمز عبور</a></p>
   <p>اگر این درخواست را شما نفرستاده‌اید، این ایمیل را نادیده بگیرید.</p>
   ```
۳. مطمئن شو Site URL (همان بخش Authentication → URL Configuration) روی
   دامنهٔ واقعی نهایی تنظیم شده باشد (نه لوکال‌هاست، نه دامنهٔ پیش‌فرض
   موقت Vercel اگر دامنهٔ اختصاصی خریداری شده).
۴. در کد، فقط یک خط باید عوض شود — `account-forgot-password.tsx`:
   ```ts
   const redirectTo = `${window.location.origin}/account/reset-password`
   ```
   برگردد به:
   ```ts
   const redirectTo = `${window.location.origin}/auth/confirm?type=recovery&next=/account/reset-password`
   ```
۵. `account-reset-password.tsx` نیازی به تغییر اجباری ندارد (نسخهٔ
   دومرحله‌ای فعلی‌اش با نشست کوکی‌محور هم درست کار می‌کند)، اما اگر
   خواستیم ساده‌ترش کنیم، می‌شود به یک `getSession()` تک‌مرحله‌ای (بدون
   `onAuthStateChange`) برگشت — چون دیگر خواندن async از `#hash` وجود
   ندارد.
۶. یک تست واقعی کامل (درخواست بازیابی → دریافت ایمیل واقعی → کلیک لینک
   → تعیین رمز جدید → ورود موفق) قبل از تحویل نهایی الزامی است.

**این ردیف باید در چک‌لیست QA فاز ۹ (بخش امنیت/Auth) هم دوباره چک شود —
تا وقتی این ۶ قدم انجام نشده، فلوی فراموشی رمز عبور از نظر امنیتی
«قابل‌قبول برای توسعه/دمو» است، نه «آمادهٔ تحویل نهایی به مشتری».**

### ۱۲.۱۳ فاز ۵.۱ — مدیریت مسیر/بس/راننده/سفر (تکمیل — ۶ سپتامبر ۲۰۲۶)

**خلاصه:** چهار مدیر CRUD واقعی ساخته شد و جایگزین جدول‌های خواندنیِ
دادهٔ ساختگی (`lib/admin-data.ts`) در تب‌های «مسیرها»/«ناوگان»/«سفرها»
شدند؛ تب «رانندگان» به‌عنوان بخش تازه به ناوبری پنل اضافه شد (قبلاً اصلاً
وجود نداشت). همه از کلاینت anon مرورگر (`lib/supabase/client.ts`) با
نشست ادمین موجود استفاده می‌کنند — بدون نیاز به service_role یا Route
Handler جدید، چون RLS فاز ۳.۲ از قبل دقیقاً برای همین حالت طراحی شده بود
(`has_admin_section('routes'|'fleet'|'trips')` مستقیماً روی جدول‌های
`cities/routes`, `buses/drivers`, `trips/trip_seats`).

**فایل‌های تازه (`components/admin/`):**
- `admin-ui.tsx` — ابزارهای مشترک چهار مدیر (Modal، ConfirmDialog،
  ErrorBanner، کلاس‌های Tailwind دکمه/اینپوت، تشخیص خطای Postgres
  unique/foreign-key به‌جای پیام خام).
- `route-manager.tsx` — CRUD مسیرها؛ دراپ‌داون مبدأ/مقصد مستقیماً از
  جدول واقعی `cities` (نه لیست ثابت `lib/i18n.ts`)، اعتبارسنجی
  مبدأ≠مقصد، خطای دوستانه برای جفت تکراری (unique violation) و برای حذف
  مسیری که سفر دارد (foreign-key violation، پیشنهاد غیرفعال‌سازی به‌جای حذف).
- `bus-manager.tsx` — CRUD بس‌ها؛ انتخاب نوع بس ظرفیت پیش‌فرض را از
  `DEFAULT_BUS_CAPACITY` پیشنهاد می‌دهد (فقط هنگام ساخت، نه ویرایش)،
  چک‌باکس امکانات (همان ۵ کلید `AmenityKey`)، وضعیت سه‌حالته
  active/maintenance/retired.
- `driver-manager.tsx` — CRUD رانندگان؛ خطای دوستانه برای شمارهٔ تماس
  تکراری.
- `trip-scheduler.tsx` — بخش پیچیده‌ترین: ساخت/ویرایش سفر با انتخاب
  مسیر (فقط مسیرهای فعال هنگام ساخت)، بس/راننده اختیاری، `DatePicker`ِ
  همان کامپوننت فاز ۴ (بدون بازسازی)، نوع حرکت (ساعت مشخص/بر اساس
  تکمیل ظرفیت). **تولید نقشهٔ چوکی**: چون هیچ تابع/تریگر Postgres برای
  این کار وجود نداشت، همان الگوی دقیق seed دادهٔ فاز ۴.۱
  (`phase-4_1-demo-seed-data.sql`) — VIP: ستون A چپ + B,C راست (۳ در هر
  ردیف)، استاندارد: A,B چپ + C,D راست (۴ در هر ردیف) — به TypeScript
  (`buildSeatRows`) منتقل شد. ساخت سفر دو مرحله‌ای است (insert در
  `trips`، سپس insert دسته‌ای در `trip_seats`)؛ اگر مرحلهٔ دوم شکست
  بخورد، ردیف `trips` نیمه‌کاره پاک می‌شود تا داده ناقص نماند.
  **بعد از ساخت سفر، تعداد/چیدمان چوکی دیگر قابل‌ویرایش نیست** (فقط
  مسیر/بس/راننده/تاریخ/ساعت/قیمت/وضعیت) — چون `trip_seats` از قبل
  ساخته و شاید رزرو شده. حذف سفر دارای رزرو (`bookings.trip_id
  references trips(id) on delete restrict`) با پیام دوستانه رد می‌شود
  («وضعیتش را به لغوشده تغییر دهید»)، نه خطای خام Postgres.

**تغییرات فایل‌های موجود:**
- `components/transport/admin-panel.tsx` — تب «رانندگان» اضافه شد؛
  تب‌های «مسیرها»/«ناوگان»/«سفرها» حالا مدیرهای بالا را رندر می‌کنند
  (نه `RoutesView`/`BusesView`/`TripsView` قدیمی که حذف شدند). داشبورد و
  تب «رزروها» **عمداً** هنوز روی `lib/admin-data.ts` ساختگی مانده‌اند —
  به ترتیب فاز ۵.۲ (رزرو/تأیید پرداخت) و ۵.۳ (گزارش‌گیری) واقعی می‌شوند؛
  ترکیب‌کردن داده واقعی/ساختگی در همان داشبورد در این فاز به‌عمد رد شد.
- `lib/admin-data.ts` — `getAdminBuses`/`getAdminRoutes` حذف شدند
  (دیگر جایی استفاده نمی‌شوند)؛ `getAdminTrips`/`getAdminBookings`/
  `getAdminStats` فقط برای ویجت‌های داشبورد/تب رزروها باقی ماندند.
- `lib/i18n.ts` — کلید `admin.nav.drivers` و بخش‌های تازهٔ
  `admin.manage`/`admin.routes`/`admin.buses`/`admin.busStatus`/
  `admin.drivers`/`admin.driverStatus`/`admin.scheduler`/
  `admin.tripStatus` به هر دو زبان (fa/en) اضافه شد.
- **حذف نهاییِ بدهی قدیمی:** `components/admin/admin-panel.tsx`
  (نسخهٔ یتیم/duplicate که در `DELETE-THESE-FILES.md` فاز قبل به
  Zakir برای حذف دستی معرفی شده بود ولی هنوز در ریپو مانده بود) در همین
  فاز حذف شد — همان پوشه حالا فقط میزبان مدیرهای واقعی CRUD است.

**تأیید کیفیت:** `npx tsc --noEmit` صفر خطای تازه (۴ خطای از قبل
موجود در `app/api/bookings/confirm/route.ts` و سه فایل `account-*.tsx`
دست‌نخورده ماندند — بدهی قدیمی، خارج از دامنهٔ این فاز). `next build`
در همین محیط ابزار Claude فقط به‌خاطر محدودیت شبکهٔ sandbox (بدون
دسترسی به `fonts.googleapis.com` برای فونت‌های Vazirmatn/Fraunces)
شکست خورد؛ این یک محدودیت محیط ابزار است، نه خطای کد — روی هر محیط با
دسترسی اینترنت عادی (یا هر بار قبلی که همین build اجرا شده) این مرحله
مشکلی ندارد.

**بدهی/نکات باز برای فازهای بعد:**
- امکان مدیریت شهرها (`cities`) به‌صورت مستقل در UI ساخته نشد — چون
  کریدور واقعی ثابت است (۸ شهر تأییدشده)؛ اگر بعداً نیاز به افزودن شهر
  جدید شد، این باید صریحاً به RouteManager اضافه شود.
- `TripScheduler` گزینهٔ «مسیر غیرفعال» را فقط هنگام **ویرایش** یک سفر
  قدیمی نشان می‌دهد (تا مقدار فعلی گم نشود)، نه هنگام ساخت سفر جدید —
  تصمیم عمدی.
