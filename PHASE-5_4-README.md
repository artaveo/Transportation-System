# فاز ۵.۴ — مدیریت باشگاه مشتریان

> این سند از بخش ۱۲.۱۶ نسخهٔ قدیمی پرامپت مادر بازسازی شده تا جزئیات
> پیاده‌سازی این فاز از حافظهٔ پروژه گم نشود.

**تکمیل:** ۶ سپتامبر ۲۰۲۶

## خلاصه

تب «باشگاه مشتریان»ی جدید (`LoyaltyManager`) با سه بخش مستقل اضافه شد،
به‌علاوهٔ رفع یک بدهی مستندِ از‌پیش‌شناخته‌شده (بخش ۲ پایین).

## ۱. سطوح عضویت

ویرایش آستانهٔ سفر (`min_completed_trips`)، درصد تخفیف
(`discount_percent`)، و فعال/غیرفعال هر یک از سه سطح ثابت
(برنز/نقره/طلا) — از طریق یک مودال ویرایش سادهٔ روی جدول
`loyalty_tiers` (RLS از فاز ۳.۲ از قبل کافی بود، بدون تغییر). **تصمیم
عمدی:** نام و کلید سطح (`tier_key`, `name_fa`, `name_en`) و افزودن/حذف
سطح در این فاز ویرایش‌پذیر نشدند — چون این نام‌ها در متن‌های ثابت
جاهای دیگر سایت هم به کار رفته‌اند و افزودن سطح چهارم یک تصمیم
کسب‌وکاری تازه است. اگر آستانهٔ ویرایش‌شده با ترتیب طبیعی سطوح دیگر
ناسازگار شود، یک هشدار غیرمسدودکننده نمایش داده می‌شود (نه جلوگیری از
ذخیره).

## ۲. پاداش کد معرفی (رفع بدهی مستند فاز ۴.۵)

موقع بررسی این فاز، کامنت صریح داخل `handle_booking_completed()` (فاز
۴.۵) پیدا شد: «تا وقتی LoyaltyManager (فاز ۵.۴) این عدد را قابل‌تغییر
نکند» — یعنی پاداش ۱۰۰ افغانی هر دو طرف رفرال از ابتدا **عمداً به‌عنوان
بدهی همین فاز مستند شده بود**. برای رفع آن (`phase-5_4-schema-additions.sql`،
از قبل روی Supabase زنده اجرا شده):

1. جدول تازهٔ `loyalty_settings` — الگوی **singleton** (کلید اصلی از
   نوع `boolean` با `check (id)`، همیشه دقیقاً یک ردیف با `id = true`)
   — شامل ستون `referral_reward_amount numeric(10,2)`. مقدار seed
   دقیقاً برابر همان عدد ثابت قبلی (۱۰۰) است تا رفتار سیستم برای
   رزروهای فعلی بی‌صدا عوض نشود.
2. RLS فعال + یک policy (`has_admin_section('loyalty')`) — بدون خواندن
   عمومی.
3. `handle_booking_completed()` با `create or replace` بازتعریف شد —
   تنها تغییر: `v_referral_reward` حالا از `loyalty_settings` خوانده
   می‌شود (با `coalesce(..., 100)` برای ایمنی).

## ۳. کدهای تخفیف

CRUD کامل روی `coupons` — کد، نوع تخفیف (درصدی/مبلغ ثابت)، مقدار،
قابل‌جمع‌شدن با تخفیف سطح، سقف تعداد استفاده (اختیاری)، بازهٔ اعتبار
(اختیاری).

**نکتهٔ فنی حیاتی:** کد کوپن هنگام ذخیره همیشه با حروف بزرگ لاتین و
trim-شده ذخیره می‌شود (`code.trim().toUpperCase()`) — دقیقاً چون تابع
`confirm_booking` (فاز ۴.۵) کد را با `upper(trim(p_coupon_code))`
جست‌وجو می‌کند؛ بدون این تطبیق، کوپن‌های ساخته‌شده از پنل در عمل هرگز
در چک‌اوت پیدا نمی‌شدند.

**محافظ حذف:** `coupon_redemptions.coupon_id` با `on delete cascade`
تعریف شده — یعنی حذف یک کوپنِ استفاده‌شده از نظر دیتابیس بدون خطا
انجام می‌شود ولی سابقهٔ استفاده‌اش را هم بی‌صدا پاک می‌کند. یک محافظ
سمت کلاینت اضافه شد: اگر `used_count > 0`، پیش از تأیید حذف، پیام
دوستانه‌ای نمایش داده می‌شود که به‌جای حذف، غیرفعال‌سازی پیشنهاد
می‌کند.

## فایل‌های جدید

- `components/admin/loyalty-manager.tsx`
- `phase-5_4-schema-additions.sql`

## اعتبارسنجی

`Supabase:get_advisors` (امنیتی) بدون هشدار تازه. `tsc --noEmit` صفر
خطای تازه. `next build` روی هر ۲۹ مسیر موفق.

## بدهی/نکات باز

- امکان اعطای دستی سطح به یک مشتری خاص ساخته نشد — خارج از دامنهٔ این
  فاز.
- گزارش عملکرد کدهای تخفیف (کدام کوپن بیشترین تخفیف را داده) ساخته
  نشد — می‌تواند بعداً به گزارش‌گیری اضافه شود. **(این دقیقاً موضوع
  فاز ۵.۱۱ — بازطراحی کامل سیستم کوپن — است.)**
