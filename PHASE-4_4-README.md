# فاز ۴.۴ — صفحات معرفی/تماس/مسیرها/سوالات متداول

## نکتهٔ مهم دربارهٔ این زیپ

چون فاز ۴.۳ را هنوز روی ریشهٔ پروژه merge نکرده بودید، این زیپ **کامل و
انباشته** است: هم فایل‌های فاز ۴.۳ (پیگیری رزرو) را دوباره شامل می‌شود، هم
فایل‌های جدید فاز ۴.۴ را. یعنی دیگر نیازی به merge کردن دو زیپ جدا نیست —
همین یکی را جایگزین کل پوشهٔ `src/` کنید.

## ۱. فایل‌های این تحویل

| فایل | نوع تغییر | فاز |
|---|---|---|
| `src/lib/supabase/queries.ts` | افزایشی — `getBookingByReferenceAndPhone` (۴.۳) + `getRoutesOverview` (۴.۴) | ۴.۳ + ۴.۴ |
| `src/lib/i18n.ts` | افزایشی — کلیدهای پیگیری رزرو (۴.۳) + بازنویسی `routesPage.priceNote` (۴.۴) | ۴.۳ + ۴.۴ |
| `src/app/api/bookings/lookup/route.ts` | جدید | ۴.۳ |
| `src/app/api/bookings/update-contact/route.ts` | جدید | ۴.۳ |
| `src/app/api/bookings/request-cancellation/route.ts` | جدید | ۴.۳ |
| `src/app/track/page.tsx` | جدید | ۴.۳ |
| `src/components/transport/booking-lookup.tsx` | افزایشی | ۴.۳ |
| `src/app/routes/page.tsx` | جدید — Server Component، دادهٔ واقعی مسیرها | ۴.۴ |
| `src/components/transport/routes-index.tsx` | افزایشی — حالا props می‌گیرد (`routes`, `cities`)، دیگر خودش `getAllRoutes()` صدا نمی‌زند | ۴.۴ |
| `src/app/contact/page.tsx` | جدید — فقط اتصال مسیر، بدون نیاز به داده | ۴.۴ |
| `src/app/faq/page.tsx` | جدید — فقط اتصال مسیر، بدون نیاز به داده | ۴.۴ |
| `src/app/about/page.tsx` | جدید/تأییدی — توضیح در بند ۳ | ۴.۴ |

**فایل‌های عمداً دست‌نخورده:** `lib/booking-data.ts` (تابع `getAllRoutes()` هنوز در فایل هست ولی دیگر از `routes-index.tsx` صدا زده نمی‌شود؛ حذفش نکردم چون مصرف‌کنندهٔ دیگری نداشتن را تأیید نکرده بودم — اگر خواستید در یک پاکسازی کوچک حذفش می‌کنم)، `about-page.tsx`/`contact-page.tsx`/`faq-page.tsx` (خودِ کامپوننت‌ها تغییر نکردند، فقط `page.tsx` اطرافشان اضافه شد).

## ۲. تصمیمات فنی و دلیل هرکدام

1. **`/routes` به دادهٔ واقعی Supabase وصل شد، نه به لیست ثابت `getAllRoutes()`.** آن تابع در `lib/booking-data.ts` هر ۲۸ جفتِ ممکنِ شهرها را با مدت سفر/قیمت/تعداد سرویسِ کاملاً ساختگی (مبتنی بر hash) می‌ساخت. با بررسی مستقیم دیتابیس زنده، الان **مسیرهای واقعاً ثبت‌شده** (کابل↔کندهار، کابل↔هرات) با `typical_duration_minutes` واقعی وجود دارند. طبق همان الگوی فاز ۴.۱ (search)، `/routes` یک Server Component شد که `getRoutesOverview()` را می‌خواند.

2. **قیمت و تعداد سرویس هر مسیر، از خودِ سفرهای ثبت‌شده مشتق می‌شود (`min(price_per_seat)` و شمارش سفرهای آینده)، نه یک عدد ثابت روی مسیر.** چون در طراحی فاز ۳.۱ قیمت روی `trips` است، نه `routes`. این دو عدد هنوز پشت همان نشان [PLACEHOLDER] نمایش داده می‌شوند — نه چون ساختگی‌اند (دیگر نیستند)، بلکه چون از دادهٔ seed آزمایشی می‌آیند و شرکت هنوز فهرست نهایی قیمت را تأیید نکرده. متن این توضیح در `routesPage.priceNote` به‌روزرسانی شد تا این تفاوت (واقعی-ولی-غیرنهایی، نه کاملاً ساختگی) را دقیق منعکس کند.

3. **فقط مسیرهایی نمایش داده می‌شوند که واقعاً در جدول `routes` ثبت شده‌اند (الان ۲ مسیر، ۴ ردیف جهت‌دار)، نه فهرست کامل ۲۸ جفتِ نظری.** این با تصمیم صریح فاز ۴.۱ («routes/buses/trips زنده عمداً خالی ماندند تا داده واقعی شرکت وارد شود») هم‌خوان است؛ نمایش مسیرهایی که هنوز در ادمین تعریف نشده‌اند، گمراه‌کننده‌تر از فهرست کوتاه‌تر ولی واقعی است.

4. **فیلتر «استان مبدأ یا مقصد» حالا از `getActiveCities()` (همان تابع فاز ۴.۱) پر می‌شود، نه لیست ثابت `lib/i18n.ts`.** برای هم‌خوانی با بقیهٔ صفحات متصل‌شده.

5. **صفحات `/about`, `/contact`, `/faq` فقط اتصال مسیر گرفتند، بدون هیچ تغییری در خودِ کامپوننت یا داده.** این سه صفحه کاملاً محتوای ثابت (متن شرکت، دفاتر، سوالات متداول) هستند که هیچ جدولی در فاز ۳.۱ برایشان طراحی نشده بود — طبق اصل «بدون شواهد، چیزی اختراع نشود»، برایشان جدول/API جدید نساختم.

6. **فرم «ارسال پیام» در `/contact` عمداً دست‌نخورده ماند (هنوز فقط یک تأیید نمایشی سمت کلاینت است، بدون ذخیرهٔ واقعی).** هیچ جدولی برای پیام‌های تماس در طراحی فاز ۳.۱ وجود ندارد و هیچ بخش مدیریتی برای دیدن این پیام‌ها هم در فازبندی فاز ۵ (پنل ادمین) پیش‌بینی نشده. ساختن یک جدول تازه فقط برای این فرم، تصمیمی است که باید صریحاً از شما تأیید بگیرم، نه چیزی که خودم فرض کنم.

## ۳. یک نکتهٔ باز — `/about` را خودتان بررسی کنید

در فایل‌های ریشهٔ پروژه (خارج از این چت)، یک `page.tsx` دیدم که دقیقاً `AboutPage` را رندر می‌کند — یعنی به‌نظر می‌رسد `/about` از قبل در کدِ واقعی شما وصل بوده، جدا از این‌که سند مادر آن را «هنوز وایر نشده» فهرست کرده بود. من نسخهٔ خودم را (با یک خط `metadata` اضافه) در همین تحویل گنجاندم — اگر همان فایل از قبل دقیقاً همین را انجام می‌داد، جایگزین‌کردنش بی‌ضرر است؛ اگر چیز دیگری بود، لطفاً قبل از merge یک نگاه بیندازید.

## ۴. آنچه عمداً در این فاز انجام **نشده**

- **ذخیرهٔ واقعی فرم تماس** (بند ۶ بالا).
- **اعداد آمار صفحهٔ درباره‌ٔ ما / صفحهٔ اصلی** (`about.stats`, `trustStats`) — هنوز همان placeholder («—») باقی ماندند. «شهر تحت پوشش» از نظر فنی قابل‌محاسبه از `cities` واقعی بود (۸ شهر)، اما «سال‌های فعالیت» و «سرویس روزانه» واقعاً business fact هستند که جایی ذخیره نشده‌اند؛ برای یکدست ماندنِ کل بلوک آماری، دست به هیچ‌کدام نزدم — اگر خواستید فقط شمارهٔ شهرها را واقعی کنم، به‌راحتی قابل‌انجام است.
- **صفحات `/legal` (شرایط/حریم خصوصی) و `/luggage-policy`** — این‌ها در عنوان فاز ۴.۴ («معرفی/تماس/مسیرها/سوالات متداول») نیامده بودند، هرچند فوتر از قبل به `/terms`, `/privacy`, `/luggage-policy` لینک می‌دهد و کامپوننت‌هایشان (`legal-page.tsx`, `luggage-policy.tsx`) از قبل ساخته شده‌اند. اگر می‌خواهید همین‌جا این‌ها را هم وصل کنم (کار کوچکی است، فقط اتصال مسیر مثل about/contact/faq)، بگویید تا اضافه کنم.
- **حذف `getAllRoutes()` از `lib/booking-data.ts`** — دیگر جایی صدا زده نمی‌شود، اما چون تأیید نکردم مصرف‌کنندهٔ دیگری (مثلاً در یک فایل که این چت به آن دسترسی نداشت) ندارد، حذفش نکردم.

## ۵. برای تست

۱. به `/routes` بروید → باید فقط ۲ مسیر واقعی (کابل↔کندهار، کابل↔هرات) را ببینید، با مدت سفر واقعی و قیمت/تعداد سرویسِ مشتق‌شده از سفرهای seed، نه یک جدول ۲۸ ردیفی.
۲. فیلتر مبدأ را روی شهری بدون مسیر ثبت‌شده (مثلاً غزنی) بگذارید → باید پیام «سرویسی یافت نشد» ببینید، نه خطا یا جدول خالی بی‌توضیح.
۳. به `/about`, `/contact`, `/faq` بروید → باید هرکدام بدون خطا رندر شوند (محتوای این سه صفحه از قبل نوشته شده بود، فقط مسیرشان جدید است).

## ۶. مرحلهٔ بعد

فاز ۴.۵: حساب کاربری مسافر و باشگاه مشتریان.
