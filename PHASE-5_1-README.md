# فاز ۵.۱ — مدیریت مسیر/بس/راننده/سفر

> این سند از بخش ۱۲.۱۳ نسخهٔ قدیمی پرامپت مادر (`claudeproject-master-prompt.md`)
> بازسازی شده تا جزئیات پیاده‌سازی این فاز، بعد از جایگزینی آن سند با
> `ROAD-MAP.md`، از حافظهٔ پروژه گم نشود.

**تکمیل:** ۶ سپتامبر ۲۰۲۶

## خلاصه

چهار مدیر CRUD واقعی ساخته شد و جایگزین جدول‌های خواندنیِ دادهٔ ساختگی
(`lib/admin-data.ts`) در تب‌های «مسیرها»/«ناوگان»/«سفرها» شدند؛ تب
«رانندگان» به‌عنوان بخش تازه به ناوبری پنل اضافه شد (قبلاً اصلاً وجود
نداشت). همه از کلاینت anon مرورگر (`lib/supabase/client.ts`) با نشست
ادمین موجود استفاده می‌کنند — بدون نیاز به service_role یا Route Handler
جدید، چون RLS فاز ۳.۲ از قبل دقیقاً برای همین حالت طراحی شده بود
(`has_admin_section('routes'|'fleet'|'trips')` مستقیماً روی جدول‌های
`cities/routes`, `buses/drivers`, `trips/trip_seats`).

## فایل‌های تازه (`components/admin/`)

- `admin-ui.tsx` — ابزارهای مشترک چهار مدیر (Modal، ConfirmDialog،
  ErrorBanner، کلاس‌های Tailwind دکمه/اینپوت، تشخیص خطای Postgres
  unique/foreign-key به‌جای پیام خام).
- `route-manager.tsx` — CRUD مسیرها؛ دراپ‌داون مبدأ/مقصد مستقیماً از
  جدول واقعی `cities` (نه لیست ثابت `lib/i18n.ts`)، اعتبارسنجی
  مبدأ≠مقصد، خطای دوستانه برای جفت تکراری (unique violation) و برای
  حذف مسیری که سفر دارد (foreign-key violation، پیشنهاد غیرفعال‌سازی
  به‌جای حذف).
- `bus-manager.tsx` — CRUD بس‌ها؛ انتخاب نوع بس ظرفیت پیش‌فرض را از
  `DEFAULT_BUS_CAPACITY` پیشنهاد می‌دهد (فقط هنگام ساخت، نه ویرایش)،
  چک‌باکس امکانات (همان ۵ کلید `AmenityKey`)، وضعیت سه‌حالته
  active/maintenance/retired.
- `driver-manager.tsx` — CRUD رانندگان؛ خطای دوستانه برای شمارهٔ تماس
  تکراری.
- `trip-scheduler.tsx` — بخش پیچیده‌ترین: ساخت/ویرایش سفر با انتخاب
  مسیر (فقط مسیرهای فعال هنگام ساخت)، بس/راننده اختیاری،
  `DatePicker`ِ همان کامپوننت فاز ۴ (بدون بازسازی)، نوع حرکت (ساعت
  مشخص/بر اساس تکمیل ظرفیت).

  **تولید نقشهٔ چوکی:** چون هیچ تابع/تریگر Postgres برای این کار وجود
  نداشت، همان الگوی دقیق seed دادهٔ فاز ۴.۱
  (`phase-4_1-demo-seed-data.sql`) — VIP: ستون A چپ + B,C راست (۳ در
  هر ردیف)، استاندارد: A,B چپ + C,D راست (۴ در هر ردیف) — به
  TypeScript (`buildSeatRows`) منتقل شد. ساخت سفر دو مرحله‌ای است
  (insert در `trips`، سپس insert دسته‌ای در `trip_seats`)؛ اگر مرحلهٔ
  دوم شکست بخورد، ردیف `trips` نیمه‌کاره پاک می‌شود تا داده ناقص نماند.

  **بعد از ساخت سفر، تعداد/چیدمان چوکی دیگر قابل‌ویرایش نیست** (فقط
  مسیر/بس/راننده/تاریخ/ساعت/قیمت/وضعیت) — چون `trip_seats` از قبل
  ساخته و شاید رزرو شده. حذف سفر دارای رزرو
  (`bookings.trip_id references trips(id) on delete restrict`) با
  پیام دوستانه رد می‌شود («وضعیتش را به لغوشده تغییر دهید»)، نه خطای
  خام Postgres.

## تغییرات فایل‌های موجود

- `components/transport/admin-panel.tsx` — تب «رانندگان» اضافه شد؛
  تب‌های «مسیرها»/«ناوگان»/«سفرها» حالا مدیرهای بالا را رندر می‌کنند
  (نه `RoutesView`/`BusesView`/`TripsView` قدیمی که حذف شدند).
  داشبورد و تب «رزروها» **عمداً** هنوز روی `lib/admin-data.ts` ساختگی
  مانده‌اند — به ترتیب فاز ۵.۲ (رزرو/تأیید پرداخت) و ۵.۳ (گزارش‌گیری)
  واقعی می‌شوند؛ ترکیب‌کردن داده واقعی/ساختگی در همان داشبورد در این
  فاز به‌عمد رد شد.
- `lib/admin-data.ts` — `getAdminBuses`/`getAdminRoutes` حذف شدند
  (دیگر جایی استفاده نمی‌شوند)؛ `getAdminTrips`/`getAdminBookings`/
  `getAdminStats` فقط برای ویجت‌های داشبورد/تب رزروها باقی ماندند.
- `lib/i18n.ts` — کلید `admin.nav.drivers` و بخش‌های تازهٔ
  `admin.manage`/`admin.routes`/`admin.buses`/`admin.busStatus`/
  `admin.drivers`/`admin.driverStatus`/`admin.scheduler`/
  `admin.tripStatus` به هر دو زبان (fa/en) اضافه شد.
- **حذف نهاییِ بدهی قدیمی:** `components/admin/admin-panel.tsx`
  (نسخهٔ یتیم/duplicate که در `DELETE-THESE-FILES.md` فاز قبل به
  Zakir برای حذف دستی معرفی شده بود ولی هنوز در ریپو مانده بود) در
  همین فاز حذف شد — همان پوشه حالا فقط میزبان مدیرهای واقعی CRUD است.

## اعتبارسنجی

`npx tsc --noEmit` صفر خطای تازه (۴ خطای از قبل موجود در
`app/api/bookings/confirm/route.ts` و سه فایل `account-*.tsx`
دست‌نخورده ماندند — بدهی قدیمی، خارج از دامنهٔ این فاز). `next build`
در محیط ابزار Claude فقط به‌خاطر محدودیت شبکهٔ sandbox (بدون دسترسی به
`fonts.googleapis.com`) شکست خورد؛ محدودیت محیط ابزار است، نه خطای کد.

## بدهی/نکات باز برای فازهای بعد

- امکان مدیریت شهرها (`cities`) به‌صورت مستقل در UI ساخته نشد — چون
  کریدور واقعی ثابت است (۸ شهر تأییدشده)؛ اگر بعداً نیاز به افزودن شهر
  جدید شد، این باید صریحاً به RouteManager اضافه شود. **(این دقیقاً
  موضوع فاز ۵.۹ است.)**
- `TripScheduler` گزینهٔ «مسیر غیرفعال» را فقط هنگام **ویرایش** یک سفر
  قدیمی نشان می‌دهد (تا مقدار فعلی گم نشود)، نه هنگام ساخت سفر جدید —
  تصمیم عمدی.
