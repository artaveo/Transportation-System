# فاز ۵.۷ — معماری وضعیت عملیاتی سفرها

> این سند از بخش ۱۲.۱۹ نسخهٔ قدیمی پرامپت مادر بازسازی شده تا جزئیات
> پیاده‌سازی این فاز از حافظهٔ پروژه گم نشود.

**تکمیل:** ۶ سپتامبر ۲۰۲۶

## گزارش Zakir

تب «سرویس‌ها» مشخص نمی‌کرد کدام سفر جدید/حرکت‌کرده/رسیده/به‌تعویق‌افتاده
است؛ خواستهٔ او یک معماری استاندارد با در نظر گرفتن استثناها (سفری که
در تاریخ خودش حرکت نمی‌کند نباید خودکار «حرکت‌کرده» حساب شود).

## یافتهٔ کلیدی (از فاز ۵.۶)

`trips.status` از فاز ۳.۱ همین الان enum کامل داشت: `scheduled |
boarding | departed | completed | cancelled` — یعنی نیازی به تغییر
enum نبود؛ مشکل واقعی صرفاً UI بود.

## تصمیم معماری

(طبق استاندارد صنعت حمل‌ونقل/هوانوردی — چرخهٔ
scheduled→boarding→departed→(en‑route)→arrived با delayed/cancelled
به‌عنوان استثنای قابل‌وقوع در هر نقطه):

- **انتقال وضعیت هرگز خودکار بر اساس ساعت سیستم نیست** — فقط با یک
  کلیک اپراتور (دکمهٔ «حرکت کرد» / «رسید به مقصد») یا از طریق دراپ‌داون
  ویرایش دستی ثبت می‌شود.
- **«تأخیر» یک وضعیت ذخیره‌شده نیست، یک نشانگر محاسبه‌شده است:** اگر
  سفر هنوز `scheduled`/`boarding` باشد ولی ۱۵ دقیقه از ساعت
  برنامه‌ریزی‌شدهٔ حرکتش گذشته باشد، یک بج نارنجی «تأخیر احتمالی» کنار
  وضعیتش نشان داده می‌شود (بدون تغییر واقعی status). سفرهای
  `fill_and_go` (بدون ساعت حرکت ثابت) از این محاسبه معاف‌اند.
- دو ستون تازهٔ `trips.departed_at`/`trips.arrived_at` (nullable
  timestamptz) اضافه شد — زیرساخت آماده برای گزارش «نرخ به‌موقع بودن»
  در فازهای بعد.
- **لغو سفر رزروهای موجودش را خودکار کنسل نمی‌کند** (تصمیم عمدی — مسیر
  بازپرداخت واقعی هنوز وجود ندارد). دیالوگ تأیید لغو تعداد رزروهای
  فعال (pending/confirmed) آن سفر را نشان می‌دهد تا اپراتور بداند باید
  جداگانه با مسافران تماس بگیرد.

## تغییرات UI (`TripScheduler`)

1. **تب‌های فیلتر** بالای جدول: همه/امروز/پیش‌رو/حرکت‌کرده/رسیده/لغوشده
   (پیش‌فرض «امروز»).
2. **ستون ظرفیت تازه** («۱۲ / ۴۸») از شمارش `trip_seats.status='booked'`
   در برابر `total_seats_snapshot`.
3. **بج وضعیت رنگی** (حرکت‌کرده=آبی، رسیده=سبز، لغوشده=قرمز) + بج
   جداگانهٔ تأخیر.
4. **دکمه‌های اکشن سریع** روی هر ردیف: «حرکت کرد» (فقط اگر
   scheduled/boarding)، «رسید به مقصد» (فقط اگر departed)، «لغو سفر»
   (اگر هنوز terminal نشده).
5. مودال ویرایش دستی هم به‌روز شد: اگر وضعیت از همان‌جا به
   حرکت‌کرده/رسیده تغییر کند، `departed_at`/`arrived_at` هم خودکار ثبت
   می‌شود.
6. ترتیب پیش‌فرض لیست از نزولی به **صعودی** (نزدیک‌ترین سفر اول) تغییر
   کرد — چون این جدول حالا یک نمای عملیاتی هم هست.

## دیتابیس

فقط دو ستون افزایشی (`departed_at`, `arrived_at` nullable) — هیچ
تابع/RLS موجودی تغییر نکرد؛ تغییر وضعیت مستقیماً از طریق
`supabase.from('trips').update(...)` سمت کلاینت انجام می‌شود، چون RLS
بخش `'trips'` از فاز ۳.۲ همین الان `for all` (شامل update) را برای
ادمین‌های دارای دسترسی 'trips' مجاز کرده.

## اعتبارسنجی

`get_advisors` (security) صفر هشدار جدید. `tsc --noEmit` صفر خطای
تازه. `next build` روی هر ۲۹ مسیر موفق.

## بدهی/نکات باز برای فازهای بعد

- لغو سفر هنوز رزروهای مرتبط را کنسل/بازپرداخت نمی‌کند — نیازمند فاز
  ۶ (HesabPay/بازپرداخت) قبل از این‌که بشود این را خودکار کرد.
- نشانگر تأخیر فقط client-side است (بدون اعلان/SMS به مسافر) — اتصال
  به فاز پیامک موضوعی جداست.
- «boarding» به‌عنوان یک مرحلهٔ میانی در enum هست ولی دکمهٔ اکشن سریع
  جداگانه‌ای برایش ساخته نشد (فقط از دراپ‌داون دستی قابل‌تنظیم است) —
  چون Zakir سه سطل اصلی (حرکت‌کرده/جا دارد/رسیده) را خواسته بود.

## فایل‌های تغییریافته

`lib/i18n.ts`، `components/admin/trip-scheduler.tsx`،
`lib/supabase/database.types.ts` (regenerate).
