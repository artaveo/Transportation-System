# خروجی نهایی این چت — فاز ۴.۶ (کامل) + یک باگ‌فیکس بین‌فازی

## بخش الف — فاز ۴.۶: ریسپانسیو حرفه‌ای سایت پابلیک + عکس‌های تطبیقی (✅ تکمیل)

جزئیات کامل در پاسخ‌های قبلی داده شد؛ خلاصه:

1. **زیرساخت بریک‌پوینت** (`app/globals.css`): افزودن `--breakpoint-3xl`
   (۱۶۰۰px) و `--breakpoint-4xl` (۲۵۶۰px).
2. **کامپوننت عمومی جدید** `components/ui/responsive-photo.tsx` — قابل‌استفاده
   در کل معماری پروژه، نه فقط سه بخش فعلی؛ اگر فقط یک عکس داده شود مثل
   `<img>` ساده عمل می‌کند.
3. **عکس‌های تطبیقی وایر شده**: هیرو (موبایل+دسکتاپ)، ناوگان و درباره‌ما
   (هر دو با هر ۴ نسخه: موبایل/تبلت/دسکتاپ/مانیتور عریض).
4. **اصلاح کانتینر برای مانیتور عریض/فوق‌عریض** در ۱۴ کامپوننت: هدر، فوتر،
   صفحه اصلی (۶ بخش)، نتایج جستجو، انتخاب صندلی، پرداخت، مسیرها، تماس،
   حساب کاربری.
5. **اصلاح چیدمان فرم جستجوی هیرو** برای تبلت (بریک‌پوینت میانی `md`).
6. صفحات تک‌ستونی (پیگیری رزرو، تأییدیه، ورود/ثبت‌نام، سوالات متداول،
   قوانین) عمداً دست‌نخورده ماندند — عرض محدودشان طراحی درست است.

## بخش ب — باگ‌فیکس بین‌فازی: ترجمهٔ پیام‌های خطا (رفع شد)

**مشکل:** پیام‌های خطا در ۶ فایل، رشتهٔ از‌قبل‌ترجمه‌شده را در state ذخیره
می‌کردند نه یک کلید؛ نتیجه این بود که تعویض زبان بعد از دیدن خطا، پیام
قدیمی را به همان زبان قبلی نگه می‌داشت.

**فایل‌های اصلاح‌شده:**
`account-login.tsx`، `account-signup.tsx`، `account-complete-profile.tsx`،
`checkout-form.tsx`، `seat-selection.tsx`، `booking-lookup.tsx`

**راه‌حل:** state حالا کلید خطا را نگه می‌دارد (مثلاً `"errorGeneric"`) و
ترجمه در لحظهٔ رندر (`t.account[serverError]`) resolve می‌شود — پس با هر
تغییر زبان، بلافاصله به‌روز می‌شود. برای `booking-lookup.tsx` که خطاهای
کنسلی از دو فضای نام مختلف (`t.track` و `t.checkout`) می‌آیند، یک تابع
کمکی کوچک (`cancelErrorText`) اضافه شد.

## بخش ج — سوءتفاهم روشن‌شده (بدون تغییر کد)

دکمهٔ «ورود» در هدر عمومی → پنل ادمین نمی‌رود چون در **فاز ۴.۵** عمداً به
`/account` تغییر کرد (مستند در PHASE-4_5-README.md، بند ۷). پنل ادمین
دست‌نخورده و کاملاً فعال است، فقط از `/admin` مستقیم در دسترس است.

## بخش د — به‌روزرسانی سند مادر

`claudeproject-master-prompt.md` به‌روزشده در همین بسته هست:

- ✅ خوردن فاز ۴.۶ در جدول بخش ۶ (با توضیح یک‌خطی).
- افزودن **فاز ۴.۷ (جدید، هنوز شروع نشده)** به نقشهٔ راه: بازیابی رمز
  عبور فراموش‌شده + بررسی/تنظیم SMTP اختصاصی.
- افزودن **بخش ۱۲ (یادداشت‌های اصلاحی بین‌فازی)** مستندکنندهٔ باگ‌فیکس
  ترجمهٔ خطا و توضیح دکمهٔ ورود.

## بخش ه — راهنمای تنظیم SMTP (اقدام دستی شما در Supabase Dashboard)

این بخش کد نیست — تنظیماتی است که خودتان در پنل Supabase انجام می‌دهید:

1. یک سرویس‌دهنده SMTP انتخاب کنید (Resend یا SendGrid — هر دو پلن رایگان
   کافی برای شروع دارند).
2. دامنهٔ ارسال را در پنل آن سرویس تأیید کنید (رکوردهای SPF/DKIM در DNS
   دامنه‌تان) — بدون این مرحله ایمیل‌ها اغلب اسپم می‌شوند.
3. در Supabase Dashboard: **Authentication → Emails → SMTP Settings**.
4. سوییچ «Enable Custom SMTP» را روشن کنید و Sender email/name +
   host/port/username/password را از سرویس‌دهنده وارد کنید، سپس Save.
5. با «Add user → Send invitation» یا ثبت‌نام واقعی در `/account/signup`
   تست کنید.
6. اگر نرسید: Sender email را با دامنهٔ تأییدشده مطابقت دهید، و بخش
   Activity/Logs سرویس‌دهنده را چک کنید تا مشخص شود مشکل از Supabase است
   یا از سمت گیرنده.

## بخش و — تست انجام‌شده

- `npx tsc --noEmit`: صفر خطای جدید در کل جلسه (همان ۹ خطای preexinting
  در فایل‌های `account-*`/`api/bookings/confirm` که این فاز اصلاً دست
  نزده، هم قبل و هم بعد از تغییرات یکسان ماند).
- `next build`: کد تا مرحلهٔ کامپایل بدون خطا پیش رفت؛ فقط به‌خاطر بلاک
  شبکهٔ محیط تست به `fonts.googleapis.com` متوقف شد (نه باگ کد).

## باقی‌ماندهٔ روی میز (برای چت‌های بعدی)

- فاز ۴.۷: Forgot Password (کدنویسی) + تنظیم SMTP (اقدام دستی شما).
- نسخهٔ تبلت/مانیتور عریض عکس هیرو (هروقت آماده شد).
- فاز ۵.۵: ریسپانسیو پنل ادمین (خارج از دامنهٔ فاز ۴.۶).
