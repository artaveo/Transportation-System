# فاز ۵.۸ — ستون‌های جدول رزروها

> این سند از بخش ۱۲.۲۰ نسخهٔ قدیمی پرامپت مادر بازسازی شده تا جزئیات
> پیاده‌سازی این فاز از حافظهٔ پروژه گم نشود.

**تکمیل:** ۶ سپتامبر ۲۰۲۶

## گزارش Zakir

ستون «چوکی» گمراه‌کننده بود (فقط عدد کل صندلی، نه شمارهٔ صندلی)؛ هیچ
ستونی نشان نمی‌داد کدام مسافر کدام چوکی(ها) را گرفته است.

## تغییرات (`BookingsTable` + `lib/i18n.ts`)

1. برچسب ستون `t.admin.cols.seats` از «چوکی» به **«تعداد چوکی»** تغییر
   کرد (فقط فارسی — انگلیسی «Seats» به‌عنوان برچسب یک شمارنده از قبل
   بدون ابهام بود). این برچسب مشترک بین `BookingsTable` و
   `DashboardView` است؛ هر دو درست‌تر شدند.
2. ستون تازهٔ **«چوکی‌های انتخابی»**: از join جدید
   `booking_passengers(trip_seats(seat_number))` در همان کوئری موجود
   خوانده و به‌ترتیب طبیعی (`1A, 2B, ...`) نمایش داده می‌شود. دادهٔ
   لازم از قبل در دیتابیس بود (فاز ۴.۵)، فقط در کوئری این جدول انتخاب
   نمی‌شد.
3. ستون تازهٔ **«تاریخ ثبت»**: از ستون `bookings.created_at` که از قبل
   fetch می‌شد ولی هیچ‌جای جدول نمایش داده نمی‌شد.
4. هیچ تغییری در RLS/دیتابیس لازم نبود — `trip_seats_public_select`
   (`for select using (true)`) و `booking_passengers_owner_select`
   (`has_admin_section('bookings')`) از فاز ۳.۲ همین الان اجازهٔ خواندن
   این داده را به پنل ادمین می‌دادند.

## اعتبارسنجی

تست مستقیم روی دادهٔ زندهٔ Supabase (join واقعی
bookings/booking_passengers/trip_seats) نتیجهٔ درست داد. `tsc --noEmit`
صفر خطای تازه. `next build` روی هر ۲۹ مسیر موفق.

## بدهی/نکات باز

جدول الان ۱۱ ستون + عملیات دارد (فراتر از فاز ۵.۵ که ScrollFade را روی
≥۶ ستون وایر کرده بود، پس مشکل اسکرول افقی از قبل حل شده)؛ فیلتر سرچ
هنوز روی نام/تلفن/کد رهگیری است (شمارهٔ چوکی به آن اضافه نشد چون
درخواست نشده بود).

## فایل‌های تغییریافته

`lib/i18n.ts`، `components/admin/bookings-table.tsx`.
