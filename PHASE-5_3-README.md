# فاز ۵.۳ — گزارش‌گیری آماری

> این سند از بخش ۱۲.۱۵ نسخهٔ قدیمی پرامپت مادر بازسازی شده تا جزئیات
> پیاده‌سازی این فاز از حافظهٔ پروژه گم نشود.

**تکمیل:** ۶ سپتامبر ۲۰۲۶

## خلاصه

تب «گزارش‌ها»ی جدید (`ReportsDashboard`) به پنل ادمین اضافه شد. بدون
نیاز به جدول یا سیاست RLS جدید؛ از همان
`has_admin_section('trips'|'bookings'|'routes')` فاز ۳.۲ استفاده
می‌کند، چون هیچ داده‌ای غیر از `trips`/`trip_seats`/`bookings`/
`routes`/`cities` لازم نداشت.

**فایل جدید:** `components/admin/reports-dashboard.tsx`.

## منطق دادهٔ اصلی

یک کوئری روی `trips` در بازهٔ `service_date` انتخابی (با embed تودرتوی
`route→cities`, `trip_seats(status)`, و
`bookings(seats_count, total_amount, status)`)، سپس تجمیع سمت کلاینت
به دو شکل:

1. **تفکیک بر اساس مسیر** — درآمد، مسافران، تعداد سفر، و اشغال
   (وزن‌دار: مجموع صندلی‌های رزروشده ÷ مجموع ظرفیت، نه میانگین سادهٔ
   درصدها) برای هر مسیر، مرتب‌شده بر اساس درآمد نزولی.
2. **تفکیک بر اساس تاریخ سفر** — همان پنج ستون، به ازای هر روز در بازه.

محور گزارش عمداً **تاریخ سفر** (`trips.service_date`) است، نه تاریخ
ثبت رزرو (`bookings.created_at`) — چون این گزارش عملکرد واقعی شبکه در
هر روز سفر را می‌سنجد، نه فعالیت لحظه‌ای رزرو (که `DashboardView` فاز
۵.۲ از قبل با `created_at` پوشش می‌دهد؛ دو محور مکمل‌اند، نه تکراری).

## هم‌راستایی عمدی با فاز ۵.۲

(تا دو گزارش پنل عدد متفاوت ندهند):

- «درآمد قابل‌محاسبه» = هر رزروِ غیرلغوشده (`pending`/`confirmed`/
  `completed`/`refunded`) — دقیقاً همان قاعدهٔ
  `DashboardView.fetchDayStats`.
- اشغال صندلی از خودِ `trip_seats.status = 'booked'` خوانده می‌شود
  (وضعیت واقعی صندلی، مستقل از وضعیت پرداخت)، نه از شمارش رزروها.

## فیلترها

بازهٔ زمانی (دکمه‌های آماده ۷/۳۰/۹۰ روز گذشته + دو فیلد
`<input type="date">` برای بازهٔ دلخواه — چون پنل ادمین اولویتش
کارایی/تراکم اطلاعات است، نه تقویم شمسی تعاملی مثل سایت پابلیک؛
`DatePicker` مشترک اینجا عمداً استفاده نشد چون آن کامپوننت صراحتاً
تاریخ‌های گذشته را غیرفعال می‌کند) + یک دراپ‌داون فیلتر مسیر (فهرست از
خودِ جدول `routes`، بدون محدودیت `is_active`).

## قابلیت افزوده (تصمیم Claude با ریسک پایین)

دکمهٔ «خروجی CSV» برای هر دو جدول — تولید کاملاً سمت مرورگر، با BOM
یونیکد در ابتدای فایل تا اکسل متن دری را درست نمایش دهد. نام فایل شامل
بازهٔ تاریخ انتخابی است.

## محدودیت شناخته‌شده (مستند، نه باگ — میراث RLS فاز ۳.۲)

اگر یک ادمین محدود دپارتمانی دسترسی بخش `'bookings'` نداشته باشد، RLS
ردیف‌های `bookings` تودرتو را بی‌صدا فیلتر می‌کند و درآمد/مسافر آن مسیر
صفر دیده می‌شود؛ همین‌طور بدون دسترسی بخش `'routes'`، نام مسیرهای
غیرفعال به‌جای متن واقعی «—» نمایش داده می‌شود. رفع کامل آن (اگر لازم
شد) باید یک تصمیم RLS مجزا و صریح باشد.

## کلیدهای i18n تازه

`admin.nav.reports` و بخش کامل `admin.reportsPanel` (هر دو زبان).

## اعتبارسنجی

`tsc --noEmit` صفر خطای تازه. `next build` روی هر ۲۹ مسیر موفق (فقط فچ
فونت گوگل به‌خاطر محدودیت شبکهٔ sandbox شکست خورد).

## بدهی/نکات باز

- نمودار بصری (خط/میله) برای روند روزانه ساخته نشد — فقط جدول عددی؛
  نیازمند یک کتابخانهٔ نمودار که فعلاً در پروژه نیست.
- مقایسهٔ دوره‌ای («این ۳۰ روز در برابر ۳۰ روز قبلش») ساخته نشد — همان
  تصمیم فاز ۵.۲ («با حجم دادهٔ محدود، عدد مقایسه‌ای گمراه‌کننده‌تر از
  مفید است») اینجا هم عمداً تکرار شد. **(این دقیقاً موضوع فاز ۵.۱۰
  است.)**
