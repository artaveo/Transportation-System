# فاز ۴.۱ — جست‌وجوی مسیر و نمایش نتایج (اتصال به دادهٔ واقعی Supabase)

## ۱. فایل‌های این تحویل

| فایل | نوع تغییر |
|---|---|
| `phase-4_1-schema-additions.sql` | مستندسازی — قبلاً از طریق MCP روی پروژهٔ زندهٔ Supabase اجرا شده |
| `phase-4_1-demo-seed-data.sql` | جدید — **اجرا نشده**؛ فقط تحویل داده شده تا خودتان تصمیم بگیرید اجرایش کنید |
| `lib/supabase/queries.ts` | جدید — لایهٔ دسترسی به دادهٔ واقعی (`getActiveCities`, `searchTrips`) |
| `app/search/page.tsx` | جدید — اولین بار است که مسیر `/search` واقعاً به یک `page.tsx` وصل می‌شود |
| `app/page.tsx` | افزایشی — از Client به Server Component تبدیل شد؛ فقط `getActiveCities()` را می‌خواند و به `TransportHome` پاس می‌دهد |
| `components/transport/transport-home.tsx` | افزایشی — یک prop (`cities`) اضافه و به `HeroSearch` پاس داده شد؛ هیچ بخش دیگری تغییر نکرد |
| `components/transport/hero-search.tsx` | افزایشی — لیست ثابت شهرهای `lib/i18n.ts` با prop شهرهای واقعی جایگزین شد |
| `components/transport/search-results.tsx` | افزایشی — داده‌ی mock (`getTrips`) حذف و با `trips`/`cities` واقعی از props جایگزین شد؛ منطق فیلتر/مرتب‌سازی/UI دست‌نخورده ماند |
| `lib/i18n.ts` | افزایشی — فقط یک کلید جدید (`search.flexibleDeparture`) در هر دو زبان اضافه شد |

**فایل‌های عمداً دست‌نخورده:** `lib/booking-data.ts` (هنوز مصرف‌کنندهٔ دیگری دارد: `seat-selection`, `checkout-form`, `booking-confirmation`, `routes-index`, `destinations-grid`, `admin-panel` — همه مربوط به فازهای بعدی)، `popular-routes.tsx` (بخش تزئینی/بازاریابی صفحهٔ اصلی، خارج از حیطهٔ «جست‌وجو و نتایج»).

## ۲. تصمیمات فنی و دلیل هرکدام (طبق شواهد واقعی بررسی‌شده)

1. **دو ستون افزایشی به اسکیما اضافه شد** (`buses.amenities`, `cities.display_order`) — طبق تأیید صریح شما در این چت. دلیل: `buses.amenities` در فاز ۳.۱ اصلاً طراحی نشده بود (فیلتر امکانات فقط خروجی v0.dev بود، بدون پشتوانهٔ دیتابیس)؛ `cities.display_order` هم برای حفظ ترتیب واقعی کریدور (کابل=۱ ... هرات=۸) لازم بود چون در طراحی فاز ۳.۱ چنین ستونی نبود.
2. **۸ شهر واقعیِ تأییدشده در `lib/i18n.ts`** مستقیماً در جدول `cities` seed شدند (نه داده ساختگی). `routes`/`buses`/`trips` زنده **عمداً خالی ماندند** — طبق انتخاب شما در این چت، تا زمانی که داده واقعی شرکت وارد شود، جست‌وجو به‌درستی «سرویسی یافت نشد» نشان می‌دهد، نه اعداد جعلی.
3. **اسکریپت دادهٔ نمایشی جدا** (`phase-4_1-demo-seed-data.sql`) نوشته شد اما **اجرا نشد** — دقیقاً طبق درخواست صریح شما، خودتان تصمیم می‌گیرید کِی/کجا اجرایش کنید. اجرای مجدد امن است (idempotent).
4. **جست‌وجو مستقیماً از Server Component به Supabase وصل شد** (نه از طریق یک Route Handler جدید در `app/api/*`)، چون طبق بخش ۴.۲ سند مادر، Route Handler فقط برای منطق حساس (رزرو اتمیک، پرداخت) لازم است؛ خواندن `cities`/`routes`/`buses`/`trips`/`trip_seats` از قبل در فاز ۳.۲ برای `anon` عمومی و قابل‌خواندن تعریف شده (RLS `for select using (true)` روی `trips`/`trip_seats`، `is_active = true` روی `cities`/`routes`).
5. **`app/page.tsx` به Server Component تبدیل شد** تا `getActiveCities()` سمت سرور اجرا شود — دقیقاً طبق یادداشت فنی صریح بخش ۴.۱ سند مادر («توصیه می‌شود کامپوننت‌های لیستی هنگام اتصال داده واقعی Supabase به Server Component تبدیل شوند»). خودِ `TransportHome`/`HeroSearch` همچنان `"use client"` باقی ماندند چون به `useLang()`/state تعاملی نیاز دارند؛ فقط دادهٔ شهرها بالادست (سرور) خوانده و پایین‌دستی پاس داده می‌شود.
6. **`app/search/page.tsx` هم یک Server Component است** که `searchTrips()` را قبل از رندر اجرا می‌کند (HTML اولیه شامل نتایج واقعی است — مزیت SEO مطابق همان توصیه)؛ `SearchResults` خودش `"use client"` می‌ماند چون فیلتر/مرتب‌سازی تعاملی (قیمت، زمان حرکت، امکانات) کاملاً سمت کلاینت اجرا می‌شود — فقط دیگر خودش داده mock نمی‌سازد، دادهٔ آماده را به‌عنوان prop می‌گیرد.
7. **جدول `routes` جهت‌دار است** (`Kabul→Herat` رکوردی جدا از `Herat→Kabul`)، دقیقاً طبق `constraint routes_unique_pair unique (origin_city_id, destination_city_id)` در فاز ۳.۱ — پس `searchTrips` مسیر را بر اساس جهت دقیق جست‌وجو می‌کند، نه بدون‌جهت.
8. **پشتیبانی از `schedule_type = 'fill_and_go'`** (بدون ساعت حرکت ثابت) اضافه شد، چون ستون `departure_time` در فاز ۳.۱ صراحتاً نال‌پذیر طراحی شده بود. چنین سفری در UI به‌جای ساعت، برچسب «بر اساس تکمیل ظرفیت» نشان می‌دهد و در فیلتر «زمان حرکت» با هیچ بازه‌ای مطابقت داده نمی‌شود (چون واقعاً نامشخص است).
9. **اگر کاربر تاریخ انتخاب نکند**، نتیجه شامل همهٔ سفرهای از امروز به بعد است (نه یک تاریخ ثابت فرضی)؛ چون در این حالت چند تاریخ مختلف در یک لیست ظاهر می‌شود، هر کارت سفر تاریخ خودش را هم نشان می‌دهد.
10. **`seatsLeft` وقتی نقشهٔ صندلی (`trip_seats`) هنوز برای یک سفر تولید نشده**، برابر کل ظرفیت (`total_seats_snapshot`) در نظر گرفته می‌شود، نه صفر — چون نبودِ رکورد صندلی یعنی «هنوز تولید نشده»، نه «همه پر».

## ۳. آنچه عمداً در این فاز انجام **نشده**

- **اتصال `SeatSelection`/`CheckoutForm`/`BookingConfirmation` به داده واقعی** — فاز ۴.۲. دکمهٔ «انتخاب چوکی» اکنون به شناسهٔ واقعی UUID سفر لینک می‌شود، اما چون `seat-selection.tsx` هنوز از `buildCoach()`ی مبتنی بر فرمت `fromEn-toEn-index` استفاده می‌کند، تا قبل از انجام فاز ۴.۲ کلیک روی آن به‌درستی کار نمی‌کند — این یک محدودیت شناخته‌شده و منتظرِ فاز بعدی است، نه باگ این تحویل.
- **`PopularRoutes` (صفحهٔ اصلی) به داده واقعی وصل نشد** — همچنان بخش تزئینی/بازاریابی با تصاویر و اعداد ثابت `lib/i18n.ts` است؛ خارج از حیطهٔ «جست‌وجو و نتایج».
- **Route Handler اختصاصی برای جست‌وجو** ساخته نشد — طبق تصمیم بند ۴ بالا، نیازی به آن نبود (فقط خواندن عمومی).
- **قفل موقت صندلی (`held_until`)** و رزرو اتمیک — این‌ها منطق فاز ۴.۲ هستند، به‌عمد در فاز ۴.۱ (جست‌وجو، نه رزرو) دست زده نشد.

## ۴. برای تست

۱. اگر می‌خواهید نتایج واقعی ببینید (نه «سرویسی یافت نشد»)، `phase-4_1-demo-seed-data.sql` را در SQL Editor پروژهٔ `Transportation-System` اجرا کنید.
۲. صفحهٔ اصلی → جست‌وجوی کابل → هرات (یا کندهار) → باید سفرهای ۳ روز آینده را ببینید.
۳. فیلتر «امکانات» را روی بس VIP امتحان کنید — چون امکانات حالا واقعاً از ستون `buses.amenities` می‌آید.

## ۵. مرحلهٔ بعد

فاز ۴.۲: نقشهٔ صندلی بس بزرگ و فرم رزرو — اتصال `SeatSelection`/`CheckoutForm` به `trip_seats` واقعی + قفل موقت صندلی + قانون نهایی جمع‌شدن تخفیف سطح/کوپن.
