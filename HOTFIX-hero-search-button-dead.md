# هات‌فیکس: دکمهٔ «جستجوی سفر» روی برخی مرورگرها/لپ‌تاپ‌ها جواب نمی‌داد

**تاریخ:** ۱۸ سنبله ۱۴۰۵ (۹ سپتامبر ۲۰۲۶)
**نوع:** باگ بحرانی — Debt تاریخی / افزوده‌شده پس از audit (بعد از فاز ۵.۱۴، قبل از ۵.۱۵)

## علائم گزارش‌شده

روی یک لپ‌تاپ دیگر، بعد از انتخاب مبدأ/مقصد/تاریخ در هومپیج، کلیک روی دکمهٔ
«جستجوی سفر» هیچ اتفاقی نمی‌انداخت (نه ناوبری به `/search`، نه خطای قابل‌مشاهده).
روی لپ‌تاپ Zakir، همان نسخهٔ آنلاین (Vercel) کاملاً درست کار می‌کرد — یعنی
دیپلوی مشکل نداشت؛ مشکل کاملاً سمت کلاینت/مرورگر بود.

## علت ریشه‌ای

`lib/lang-context.tsx` → `LangProvider` (که در `app/layout.tsx` دور کل
`<body>` پیچیده شده — یعنی روی **همهٔ صفحات** اجرا می‌شود، نه فقط هومپیج)
در یک `useEffect` که در همان اولین هیدریت صفحه اجرا می‌شود، بدون `try/catch`
این خط را صدا می‌زد:

```ts
window.localStorage.setItem("shabraw-lang", lang)
```

روی هر مرورگر/پروفایلی که دسترسی به `localStorage` را مسدود کرده باشد —
حالت خصوصی سخت‌گیرانهٔ Safari، تنظیم «Block all cookies»، پالیسی مرورگر
سازمانی، برخی افزونه‌های حریم‌خصوصی — این خط یک `SecurityError` پرتاب
می‌کند. چون در کل ریپو حتی یک App Router error boundary (`error.tsx` /
`global-error.tsx`) وجود نداشت، این خطا کل فرایند هیدریت React را از همان
اولین لحظه متوقف می‌کرد:

- HTML رندرشدهٔ سرور همچنان روی صفحه می‌ماند → ظاهر صفحه کاملاً سالم به
  نظر می‌رسید (برای همین select های مبدأ/مقصد هم چون native هستند ظاهراً
  «کار می‌کردند»).
- اما هیچ‌کدام از event handler های React (از جمله `onSubmit` فرم هیرو)
  هرگز attach نمی‌شدند → کلیک روی دکمهٔ جستجو عملاً هیچ کاری نمی‌کرد.

نکتهٔ جالب: همین ریسک قبلاً در `lib/pending-profile.ts` (استفاده از
`sessionStorage`) شناسایی و با `try/catch` رفع شده بود (کامنت آن فایل صریحاً
همین سناریو را توضیح می‌دهد) — اما همان الگو در `lang-context.tsx` رعایت
نشده بود. یعنی جا‌افتادگی بوده، نه تصمیم عمدی.

## رفع

1. **`lib/lang-context.tsx`** — نوشتن `localStorage` و `document.cookie` هر
   دو در `try/catch` مجزا قرار گرفتند (بی‌ضرر در صورت شکست؛ فقط یعنی
   همگام‌سازی زبان بین تب‌ها/لود بعدی از کوکی انجام نمی‌شود، نه کرش کل سایت).
2. **`app/error.tsx`** (جدید) — error boundary سطح صفحات؛ هر خطای
   پیش‌بینی‌نشدهٔ دیگر زیر `LangProvider` را می‌گیرد و به‌جای صفحهٔ سفید یک
   پیام + دکمهٔ «تلاش دوباره» نشان می‌دهد.
3. **`app/global-error.tsx`** (جدید) — سطح دوم؛ خطاهای داخل خودِ
   `app/layout.tsx`/`LangProvider` (دقیقاً همان نقطه‌ای که این باگ در آن
   بود) را می‌گیرد. طبق قرارداد Next.js، چون جای کل RootLayout می‌نشیند،
   خودش `<html>`/`<body>` را جدا تعریف می‌کند (بدون وابستگی به فونت‌ها/
   context عادی سایت، چون خودِ آن‌ها ممکن است منشأ خطا باشند).

## فایل‌های تغییرکرده

- `lib/lang-context.tsx` (edit)
- `app/error.tsx` (جدید)
- `app/global-error.tsx` (جدید)

## تست

- `tsc --noEmit`: دقیقاً همان ۹ خطای baseline از قبل (۰ خطای جدید).
- `next build`: موفق، همهٔ ۳۱ route (از جمله `/error` و `/global-error`
  که به‌صورت ضمنی اضافه شدند) بدون خطا build شدند.

## نکتهٔ باقی‌مانده برای بعد

این فقط یک نمونهٔ کشف‌شده بود؛ چون تا الان هیچ error boundary ای در پروژه
نبود، ممکن است crash های دیگری (هنوز رخ‌نداده یا هنوز گزارش‌نشده) با همین
الگو (خطای بی‌صدا در یک provider/effect سطح‌بالا → کل هیدریت متوقف) وجود
داشته باشند. `app/error.tsx`/`global-error.tsx` از این به بعد حداقل جلوی
«صفحهٔ کاملاً بی‌جواب و بی‌پیام» را می‌گیرد، اما بررسی دوره‌ای سایر
`window.*`/`localStorage`/`document.cookie` های بدون `try/catch` (اگر در
آینده جای جدیدی اضافه شد) پیشنهاد می‌شود.
