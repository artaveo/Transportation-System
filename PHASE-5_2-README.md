# فاز ۵.۲ — مدیریت رزرو و تأیید پرداخت آفلاین

> این سند از بخش ۱۲.۱۴ نسخهٔ قدیمی پرامپت مادر بازسازی شده تا جزئیات
> پیاده‌سازی این فاز از حافظهٔ پروژه گم نشود.

**تکمیل:** ۶ سپتامبر ۲۰۲۶

## علت شروع این فاز زودتر از حد معمول

Zakir صراحتاً اعلام کرد که دیتای ساختگی پنل ادمین («صد تا دیتا که
معلوم نیست از کجا اومدن») باید حذف شود — چهل ردیف رزروی که
`lib/admin-data.ts` با نام‌های فارسیِ شبه‌تصادفی می‌ساخت، در حالی‌که ۶
رزرو واقعی (از تست‌های خودِ Zakir روی سفرهای seed دادهٔ فاز ۴.۱، کریدور
کابل↔کندهار) از قبل در دیتابیس زنده موجود بود. **`lib/admin-data.ts` در
همین فاز کاملاً حذف شد** — هیچ داده‌ای در پنل ادمین دیگر ساختگی نیست.

## فایل‌های تازه

- `components/admin/bookings-table.tsx` — جایگزین کامل تب «رزروها».
  مستقیماً از جدول `bookings` (با join به `trips→routes→cities` و
  آخرین ردیف `payments`) می‌خواند؛ جست‌وجو روی نام/تلفن/کد رهگیری،
  فیلتر روی هر ۵ وضعیت واقعی enum `booking_status` (نه فقط سه‌تای
  قبلی). **PaymentConfirmationAction به‌صورت دکمهٔ درون‌ردیفی** پیاده
  شد (نه صفحهٔ جدا) — فقط برای رزروهای آفلاینِ pending نمایش داده
  می‌شود.
- `components/admin/dashboard-view.tsx` — جایگزین کامل داشبورد ساختگی.
  ۴ کارت آمار (رزرو امروز، درآمد امروز، میانگین اشغال امروز، سفرهای
  فعال امروز) مستقیماً محاسبه می‌شوند؛ **درصد «نسبت به دیروز» عمداً
  حذف شد** — با حجم دادهٔ واقعیِ فعلاً محدود، یک عدد مقایسه‌ای ساختگی
  گمراه‌کننده‌تر از مفید بود؛ گزارش روندی واقعی به فاز ۵.۳ موکول شد.
  لیست‌های «رزروهای اخیر» و «سرویس‌های پیشِ‌رو» هم از همان دو جدول
  واقعی.

## سمت دیتابیس

(`phase-5_2-schema-additions.sql`، از قبل روی Supabase زندهٔ پروژه از
طریق MCP اجرا شده) — دو تابع اتمیک `SECURITY DEFINER` تازه:

- `admin_confirm_offline_payment(p_booking_id)` — `payments.status` و
  `bookings.status` را با هم به `confirmed` می‌برد (فقط اگر
  `payment_method = 'offline'` و رزرو `pending` باشد)؛ مجوز داخلی:
  `has_admin_section('payments')`.
- `admin_cancel_booking(p_booking_id)` — `trip_seats` مرتبط را به
  `available` برمی‌گرداند و `bookings.status` را `cancelled` می‌کند؛
  مجوز داخلی: `has_admin_section('bookings')`.

**چرا این دو تابع به‌جای update مستقیم جدول از کلاینت:** آزادسازی
صندلی با بخش ادمین `'trips'` گیت شده، نه `'bookings'` (نگاشت بخش‌ها در
`phase-3.2-rls-policies.sql`). اگر بعداً ادمین محدودی فقط با دسترسی
`'bookings'` ساخته شود، نباید برای لغو یک رزرو مجبور به داشتن دسترسی
جداگانهٔ `'trips'` هم باشد؛ این دو تابع خودشان هر دو جدول را در یک
تراکنش اتمیک به‌روزرسانی می‌کنند — دقیقاً همان اصل معماری
`hold_seats`/`confirm_booking` در فاز ۴.۲. `lib/supabase/database.types.ts`
هم با `Supabase:generate_typescript_types` دوباره ساخته شد.

## کلیدهای i18n تازه

`admin.paymentStatus` و `admin.bookingsPanel` (هر دو زبان).

## بدهی/تصمیم آگاهانه

۲۴ سفر seed دادهٔ فاز ۴.۱ (بس‌های `DEMO-VIP-1`/`DEMO-STD-1`، کریدور
کابل↔هرات و کابل↔کندهار) **دست‌نخورده ماندند** — چون ۶ رزرو واقعیِ
Zakir دقیقاً به همین سفرها وصل‌اند (حذفشان هم به‌خاطر
`bookings.trip_id references trips(id) on delete restrict` اصلاً
ممکن نیست تا رزروهایشان لغو/حذف نشوند). اگر Zakir بخواهد این سفرهای
seed را به‌طور کامل جمع کند، باید صریحاً درخواست بدهد.

## اعتبارسنجی

`tsc --noEmit` صفر خطای تازه (همان ۸ خطای قدیمیِ خارج از دامنه در
`app/api/bookings/confirm/route.ts` و `account-*.tsx` دست‌نخورده
ماندند).

## یادداشت تکراری

فایل یتیم `components/admin/admin-panel.tsx` (که در فاز ۴.۶ و ۵.۱ برای
حذف علامت خورده بود) هنوز در ریپوی گیت‌هاب موجود بود؛ در زیپ همین فاز
حذف شده بود — کافی بود این یک خط زده شود: `git rm components/admin/admin-panel.tsx`.
