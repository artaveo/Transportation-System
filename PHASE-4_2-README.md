# فاز ۴.۲ — نقشهٔ صندلی بس بزرگ و فرم رزرو (اتصال کامل به Supabase)

## ۱. فایل‌های این تحویل

| فایل | نوع تغییر |
|---|---|
| `lib/supabase/database.types.ts` | جدید — تایپ‌های تولیدشده از `Supabase:generate_typescript_types` |
| `lib/supabase/client.ts`, `lib/supabase/server.ts` | افزایشی — با `Database` typed شدند |
| `lib/supabase/service.ts` | جدید — کلاینت `service_role` (فقط سمت سرور) |
| `lib/supabase/queries.ts` | افزایشی — دو تابع جدید: `getTripWithSeats`, `getBookingByReference` |
| `lib/booking-data.ts` | افزایشی — فقط ثابت جدید `MAX_SEATS_PER_BOOKING` اضافه شد |
| `lib/i18n.ts` | افزایشی — چند کلید جدید (seats/checkout/confirm) در هر دو زبان |
| `app/api/bookings/hold/route.ts` | جدید — قفل اتمیک صندلی |
| `app/api/bookings/release/route.ts` | جدید — آزادسازی دستی صندلی |
| `app/api/bookings/confirm/route.ts` | جدید — ثبت نهایی رزرو |
| `app/trips/[tripId]/seats/page.tsx` | جدید — اولین بار wired به داده واقعی |
| `app/trips/[tripId]/checkout/page.tsx` | جدید |
| `app/trips/[tripId]/confirmation/page.tsx` | جدید |
| `components/transport/seat-selection.tsx` | افزایشی — mock (`buildCoach`/`getTripById`) حذف، به props واقعی وصل شد |
| `components/transport/checkout-form.tsx` | افزایشی — همان، + فیلد کد تخفیف اضافه شد |
| `components/transport/booking-confirmation.tsx` | افزایشی — همان |
| `package.json` | افزایشی — وابستگی جدید `server-only` |

**روی دیتابیس زندهٔ Supabase (اجرا شده):**
1. دو ستون افزایشی `booking_passengers.national_id` و `booking_passengers.gender` (+ نوع `passenger_gender`).
2. سه تابع Postgres: `hold_seats`, `release_seats`, `confirm_booking` — با `revoke/grant` صریح، فقط `service_role` اجازهٔ اجرا دارد.

**فایل‌های عمداً دست‌نخورده:** `lib/booking-data.ts` (توابع mock مثل `getTripById`/`buildCoach`/`bookingReference` عمداً حذف نشدند — اگر جای دیگری هنوز به آن‌ها ارجاع می‌دهد نشکند؛ فقط دیگر توسط سه صفحهٔ بازنویسی‌شده استفاده نمی‌شوند).

## ۲. یک متغیر محیطی جدید لازم دارید

```
SUPABASE_SERVICE_ROLE_KEY=...
```
از Supabase Dashboard → Settings → API → `service_role` (نه `anon`!) بردارید و در `.env.local` اضافه کنید. **هرگز** `NEXT_PUBLIC_` نگذارید — این کلید فقط باید سمت سرور در دسترس باشد. بعد از افزودن، `pnpm install` را هم دوباره بزنید (به‌خاطر وابستگی جدید `server-only`).

## ۳. تصمیمات فنی و دلیل هرکدام

1. **قفل صندلی اتمیک با تابع Postgres (نه چند کوئری جدا از JS).** یک `UPDATE ... WHERE status='available' ... RETURNING` که اگر تعداد ردیف‌های واقعاً به‌روزشده با تعداد درخواستی نخواند، `raise exception` می‌کند و کل عملیات (طبق رفتار استاندارد Postgres) خودکار rollback می‌شود. این دقیقاً همان چیزی است که بخش ۴.۲ سند مادر به‌عنوان «رزرو اتمیک صندلی» خواسته.
2. **این سه تابع صراحتاً از anon/authenticated گرفته و فقط به `service_role` داده شدند** (`revoke`/`grant` در پایان مایگریشن). بدون این کار، هرکسی می‌توانست مستقیماً از REST API عمومی Supabase این توابع را صدا بزند و بدون عبور از منطق برنامه، صندلی قفل/رزرو جعلی بسازد.
3. **صندلیِ `held` با `held_until` گذشته، «عملاً خالی» حساب می‌شود** (هم در نمایش نقشهٔ صندلی، هم در محاسبهٔ `seatsLeft` فاز ۴.۱، هم در خودِ `hold_seats`) — به‌جای نیاز به یک cron جداگانه برای آزادسازی. یک تابع سبک `release_seats` هم برای آزادسازی فوری (وقتی کاربر از صفحهٔ پرداخت برمی‌گردد) اضافه شد.
4. **پارامتر `date` از URL این سه صفحه حذف شد** (`/trips/[tripId]/seats`, `.../checkout`, `.../confirmation` دیگر `?date=` نمی‌گیرند) — چون `service_date` از خودِ رکورد `trips` (بر اساس `tripId`) خوانده می‌شود، نه از URL. این یک ساده‌سازی نسبت به نسخهٔ mock قبلی است که تاریخ را جدا در URL نگه می‌داشت.
5. **تخفیف سطح باشگاه مشتریان (`tier_discount_amount`) در این فاز همیشه صفر است.** طبق تصمیم شما، اگر کوپن جمع‌ناپذیر با سطح باشد باید به مشتری خطا داده شود — اما این تعارض عملاً **الان قابل‌وقوع نیست**، چون هنوز هیچ حساب کاربری/ورود مسافری (فاز ۴.۵) وجود ندارد که «سطح» او را بشناسیم؛ هر رزرو در این فاز از نظر دیتابیس یک رزرو مهمان است (`customer_id = null`). این قانون باید در فاز ۴.۵ (وقتی customer_id واقعی به رزرو وصل می‌شود) در تابع `confirm_booking` پیاده‌سازی شود — الان کدنویسی‌اش بی‌مصرف می‌بود.
6. **کوپن (`coupon_discount_amount`) کاملاً کار می‌کند و به مسافر مهمان هم اجازه دارد** — چون کد تخفیف نیازی به حساب کاربری ندارد. یک فیلد «کد تخفیف (اختیاری)» به فرم پرداخت اضافه شد (در طراحی v0.dev اصلاً نبود، ولی چون کل زیرساخت `coupons` طبق فاز ۳.۱ برای همین‌جا ساخته شده بود، بدون این فیلد کاملاً غیرقابل‌استفاده می‌ماند).
7. **هر دو روش پرداخت (آنلاین/آفلاین) فعال‌اند** طبق تصمیم شما؛ رزرو آنلاین هم با `payments.status='pending'` ثبت می‌شود (چون HesabPay هنوز وصل نیست — فاز ۶.۳). یک یادداشت شفاف («درگاه هنوز کامل وصل نشده») زیر گزینه‌های کارت/موبایلی نشان داده می‌شود تا برای کاربر واقعی هم گیج‌کننده نباشد.
8. **صفحهٔ تأییدیه (`/trips/[tripId]/confirmation`) فقط با `ref` کار می‌کند، نه `ref+phone`.** چون بلافاصله بعد از پرداخت باز می‌شود (مثل صفحهٔ موفقیت اکثر درگاه‌های پرداخت)، نه یک ابزار عمومی «پیگیری رزرو». صفحهٔ واقعی پیگیری رزرو (فاز ۴.۳، `BookingLookup`) باید طبق طراحی فاز ۳.۲ حتماً هم کد رهگیری هم شمارهٔ تماس را بخواهد.
9. **`contact_name` رزرو = نام مسافر اول.** فرم فعلی UI (ساختهٔ v0.dev) فیلد جداگانه‌ای برای «نام تماس» ندارد؛ همان قرارداد ضمنی نسخهٔ mock قبلی حفظ شد تا فیلد جدیدی به فرم اضافه نشود.
10. **چک‌باکس «بار اضافه» صرفاً اطلاع‌رسانی UI است، در دیتابیس ذخیره نمی‌شود.** هیچ ستونی برایش در فاز ۳.۱ طراحی نشده و در پرامپت v0.dev هم نیامده؛ برخلاف amenities/national_id/gender، این یکی صرفاً یک یادداشت نمایشی است (نه دادهٔ عملیاتی ضروری مثل شمارهٔ تذکره)، پس فعلاً بدون ستون جدید نگه داشته شد. اگر لازم شد در دیتابیس ذخیره شود، بگویید تا مثل موارد قبلی یک ستون افزایشی اضافه کنم.
11. **`getBookingByReference` با `service_role` می‌خواند، نه anon.** طبق فاز ۳.۲ (`bookings_owner_select`)، رزروِ مهمان (`customer_id is null`) اصلاً برای anon قابل‌خواندن نیست؛ این تابع فقط از یک Server Component صدا زده می‌شود، هرگز از Client Component.

## ۳.۱. تسک کوچک جداگانه‌ای که خواسته بودید

تایپ‌های واقعی Supabase تولید و در سه کلاینت (`client.ts`, `server.ts`, `service.ts`) و `lib/supabase/queries.ts` وایر شدند. از این به بعد اشتباه نام ستون/جدول را `tsc`/build می‌گیرد، نه فقط زمان اجرا. هر وقت اسکیما تغییر کرد (فاز‌های بعد)، کافی است دوباره از من بخواهید `lib/supabase/database.types.ts` را بازتولید کنم.

## ۴. آنچه عمداً در این فاز انجام **نشده**

- **اتصال واقعی درگاه HesabPay** — فاز ۶.
- **اعمال تخفیف سطح باشگاه مشتریان** — منتظر فاز ۴.۵ (به دلیل بند ۵ بالا).
- **پیش‌نمایش زندهٔ تخفیف کوپن قبل از ثبت نهایی** (فعلاً فقط بعد از submit در صفحهٔ تأییدیه دیده می‌شود) — یک بهبود UX آینده، نه یک نیازمندی شماره‌دار سند مادر.
- **صفحهٔ پیگیری رزرو (`BookingLookup`)** — فاز ۴.۳.
- **Cron واقعی برای آزادسازی صندلی‌های منقضی** — فعلاً با انقضای منطقی (`held_until < now()`) در لحظهٔ خواندن/قفل جایگزین شده؛ کاملاً درست کار می‌کند، فقط اگر خواستید در پنل ادمین شمارش دقیق «صندلی‌های held فعلی» ببینید، ممکن است بخواهید بعداً یک sweep واقعی هم اضافه کنید.

## ۵. برای تست کامل جریان رزرو

۱. مطمئن شوید `phase-4_1-demo-seed-data.sql` (از فاز قبل) روی دیتابیس اجرا شده — بدون آن هیچ سفر واقعی‌ای برای رزرو وجود ندارد.
۲. `SUPABASE_SERVICE_ROLE_KEY` را در `.env.local` اضافه و `pnpm install` بزنید.
۳. از صفحهٔ اصلی جست‌وجو کنید → یک سفر را باز کنید → صندلی انتخاب کنید → «ادامه به پرداخت» (این لحظه صندلی قفل می‌شود) → فرم مسافران/تماس را پر کنید → پرداخت کنید → باید به صفحهٔ تأییدیه با کد رهگیری واقعی برسید.
۴. برای تست قفل هم‌زمان: همان سفر را در دو تب مرورگر باز کنید، در هر دو یک صندلی مشترک انتخاب و همزمان «ادامه» بزنید — یکی باید خطای «چوکی هم‌زمان رزرو شد» بگیرد.

## ۶. مرحلهٔ بعد

فاز ۴.۳: صفحهٔ پیگیری رزرو (کد رهگیری + شمارهٔ تماس، بدون نیاز به حساب کاربری).
